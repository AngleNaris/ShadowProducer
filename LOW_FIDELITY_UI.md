# ShadowProducer 低保真 UI 基线

> 状态：低保真阶段基线 v0.8  
> 日期：2026-08-18  
> 上游：`PRODUCT_REQUIREMENTS.md`、`TECHNICAL_ARCHITECTURE.md`  
> 可交互原型：`low-fidelity/index.html`

---

## 1. 阶段目标

本阶段固定 ShadowProducer 的信息架构、页面职责、上下文切换、核心流程、页面状态和 Mock 契约。

本阶段不做：

- 真实账号、数据库、对象存储或后台队列。
- FFmpeg、模型供应商或 Agent Orchestrator 接入。
- 最终品牌色、字体、插画、动效和精细组件样式。
- 完整脚本编辑器、生产级日历引擎或视频逐帧同步实现；当前月历为确定性 Mock 交互。

低保真原型只使用确定性 Mock 数据。所有交互结果刷新后可恢复到同一初始状态，不产生外部副作用。

## 2. 核心界面模型

ShadowProducer 同时存在三个作用域：

1. **个人作用域**：当前团队内的个人任务、日历、便签/笔记、个人联系人和 Agent。
2. **团队作用域**：团队成员、统一资源库和作品集；资源库包含团队联系人、成员共享联系人与团队素材。
3. **项目作用域**：脚本、分镜、制片拆解、拍摄计划、通告、项目资源入口和审片。

团队和项目不作为同时常驻的全局上下文。界面按当前页面建立一种空间：

```text
团队选择空间：进入应用前选择一个团队
当前团队空间：一个团队内的个人工作台和团队管理
项目空间：当前团队中的一个项目
```

进入应用先显示团队选择页；用户明确选择团队后，才进入当前团队工作区。当前团队顶部菜单只能返回团队选择页完成切换，不能直接替换当前页面上下文。

## 3. 全局壳层

### 3.1 桌面端

```text
┌──────────────────────────────────────────────────────────────────────┐
│ Brand │ Read-only Location │ Search │ Notice │ Agent │
├────────────┬───────────────────────────────────────────────┬─────────┤
│ Scope Nav  │ Main Workspace                                │ Agent   │
│            │                                               │ Drawer  │
└────────────┴───────────────────────────────────────────────┴─────────┘
```

- 顶栏显示当前团队；进入项目空间后，在顶部菜单栏显示唯一的自定义项目菜单。团队与项目菜单使用相同的按钮、箭头、展开态和选项列表样式，页面标题区不再重复显示项目选择器。
- 项目菜单面向几十个项目设计：顶部搜索名称、状态或角色，可切换全部/进行中/已归档，当前项目与最近访问优先，结果列表在固定高度内独立滚动。
- 顶部当前团队菜单提供“切换团队”和“团队设置”；切换团队必须返回团队选择页。
- Agent 是全局右侧抽屉，继承当前团队作用域；写操作另选唯一目标项目。
- 通知、后台任务和待审批数量只计算当前团队事项。

### 3.2 窄屏

- 团队页和项目页的局部空间选择器在需要时纵向排列。
- 左侧导航收为底部或可展开导航。
- Agent 抽屉占满可用宽度。
- 审片页面优先保留播放器、时间码和评论输入；评论列表移到下方。
- 双版本对比在窄屏采用上下排列，不挤压成两个不可读小窗。

## 4. 页面地图

### 4.1 个人

| 路由 | 页面 | 职责 |
|---|---|---|
| `/team-select` | 团队选择 | 进入应用前选择当前工作团队 |
| `/team/:teamId/dashboard` | 看板 | 当前团队内的任务看板与我的排期 |
| `/team/:teamId/calendar` | 我的日历 | 当前团队内的个人日程与相关安排 |
| `/team/:teamId/notes` | 便签 / 笔记 | 当前团队可见的项目笔记与个人便签 |
| `/team/:teamId/personal-contacts` | 个人联系人 | 个人所有联系人的创建、编辑与团队共享 |

### 4.2 团队

| 路由 | 页面 | 职责 |
|---|---|---|
| `/t/:teamId` | 团队概览 | 团队动态、近期拍摄、项目和成员 |
| `/t/:teamId/resources` | 团队资源库 | 团队联系人、成员共享联系人、团队素材、上传与项目筛选 |
| `/t/:teamId/portfolio` | 团队作品集 | 多项目批准成片的组织与发布 |
| `/t/:teamId/settings` | 团队设置 | 成员、权限、模型数据策略和审计 |

旧的 `/t/:teamId/contacts` 与 `/t/:teamId/assets` 深链接分别兼容映射到资源库的“联系人”和“素材”分类。

### 4.3 项目

| 路由 | 页面 | 职责 |
|---|---|---|
| `/t/:teamId/p/:projectId` | 项目中心 | 项目完成度、阶段链、关键节点和来源明确的动态追踪 |
| `/scripts` | 脚本与分镜 | 文档、版本、场次、镜头和协作评论 |
| `/breakdown` | 制片拆解 | Agent 候选项、来源定位和人工确认 |
| `/schedule` | 拍摄计划 | 拍摄日、场次、人员和资源冲突 |
| `/call-sheets` | 通告 | 草稿、发布、变更、通知和确认 |
| `/resources` | 项目资源 | 打开团队资源库，并按当前项目过滤联系人或素材 |
| `/reviews` | 审片 | 审片任务、版本、评论、标注和批准 |
| `/members` | 项目成员 | 项目成员、角色和访问范围 |

### 4.4 外部与公开

| 路由 | 页面 | 职责 |
|---|---|---|
| `/r/:token` | 客户审片 | 免注册、受控版本审阅 |
| `/@teamSlug` | 公开作品集 | 团队公开作品展示 |

## 5. 首批线框顺序

### P0-A：壳层和上下文

1. 应用壳层。
2. 团队选择页与当前团队上下文。
3. 当前团队个人工作台。
4. 项目中心。
5. Agent 全局抽屉。

### P0-B：前期生产闭环

1. 脚本与分镜。
2. 制片拆解候选审阅。
3. 制片清单。
4. 拍摄计划。
5. 通告草稿、确认和发布结果。

### P0-C：素材与审片闭环

1. 团队素材库与上传队列。
2. 审片任务详情。
3. 两版本同步对比。
4. 客户分享设置。
5. 客户免注册审片。

### P0-D：团队资产沉淀

1. 个人联系人共享。
2. 团队资源库中的联系人和素材。
3. 团队作品集。
4. 审计记录。

## 6. 关键页面决策

### 6.1 看板、日历与便签

采用“当前团队任务看板 + 我的排期甘特”的单列工作区；便签/笔记迁移到独立个人页面，Agent 保留为全局顶栏入口和右侧抽屉。

- 进入工作台前必须已选择团队；工作台不显示其他团队任务。
- 个人导航将 Dashboard 显示为“看板”；看板标题使用“繁星 + 问候”，不重复显示“个人工作台”和团队范围标签。
- 看板只显示与当前用户相关的当前团队任务，按待开始、进行中、等待他人、已完成分列。
- 日历使用固定 6 周、42 个日期格的月视图，支持上月、下月、回到今天、选择日期和查看当日日程。
- 月历事件复用首页排期 Mock，并保持个人、团队协同和项目执行的跳转目标；其他团队事项不显示。
- 便签/笔记页面采用筛选栏、搜索框和笔记网格，支持全部、置顶、个人便签和项目笔记四种筛选；其他团队项目笔记不显示。
- 搜索覆盖标题、摘要与元信息，并提供无匹配结果空态；笔记详情弹窗保持可用。
- Agent 不在工作台重复显示摘要面板；用户从全局 AI 入口进入同权 Agent 抽屉。
- 顶栏通知只统计当前团队事项；切换团队后重新计算。
- 首页甘特只显示当前团队的个人日程、团队协同和项目执行任务；生产排期和拍摄通告的详细操作位于项目空间。
- 任务卡点击后进入当前团队对应的项目页面。
- 所有使用统一工作区标题栏的面板提供折叠按钮；折叠状态在当前原型会话的页面往返和局部重绘中保持。
- 窄屏下看板列和月历允许自身横向滚动，月历自动将所选日期移入可视区域；便签筛选横向滚动、笔记列表改为单列，不产生页面级横向溢出。

### 6.2 脚本与分镜

脚本和分镜使用两个一级 Tab，不再把脚本文档与分镜文档混在同一个三栏浏览器中。

```text
脚本：版本轨道 │ 协作文本编辑器 │ 在线成员、评论与来源
分镜：视图工具栏 │ 镜头卡片/表格/Layout 节奏预演 │ 当前镜头详情
```

- 脚本正文使用可编辑的连续纸张工作区，提供撤销、重做、基础文字格式、选区评论、自动保存反馈和在线协作者状态。
- 当前版本可编辑；历史版本明确只读，并保留提交人和时间。
- 评论绑定当前脚本版本和内容位置，可进入回复与解决流程。
- 上传的 PDF、DOCX 或图片作为不可变来源版本。
- 系统可以生成可编辑的结构化场次和镜头草稿。
- 原始文档预览和结构化数据并列存在，不用结构化内容覆盖原文件。
- 分镜以同一份镜头数据为基础，支持卡片、表格和 Layout 节奏预演三种视图。
- 三种视图都支持选择镜头，并在统一详情区显示缩略图、景别、时长、镜头参数、运动、内容、备注和脚本来源。
- Layout 将镜头按时长自动排成连续时间线，在 16:9 画布中播放分镜画面、动作内容和台词；播放头、画布、时间线高亮和右侧镜头详情始终指向同一个当前镜头。
- Layout 支持播放/暂停、拖动定位、前后镜头跳转和单镜头时长调整；时长变化会自动推动后续镜头并更新总时长，用于实际模拟和比较分镜节奏。
- 当前低保真不提供素材剪切、多轨合成、转场、调色、特效、混音、关键帧或成片导出，Layout 不是完整剪辑工作区。
- 当前低保真仅验证协作编辑与视图切换的信息结构、状态反馈和交互契约，不实现生产级实时协同算法。

### 6.3 制片拆解

制片拆解使用按部门分类的连续工作区，不把所有候选项混在一张总表中。大类至少包括：

- 演员与角色。
- 场景与场地。
- 美术与道具。
- 服化造型。
- 摄制设备（摄影、灯光、录音与特殊支撑）。
- 特殊执行与保障。

每个大类独立显示待确认数量和适合该部门的专业准备字段。候选项必须同时显示：

- 候选需求、类型、规格或数量。
- 专业准备要求与负责部门。
- 来源文档与版本。
- 原文位置。
- Agent 置信提示。
- 当前处理状态。
- 关联负责人、联系人、供应商、拍摄日和通告。

确认、驳回、合并和拆分属于显式操作。勾选只表示加入当前操作选择，不得直接改变确认状态。批量确认只处理当前大类中明确选中的候选，确认前展示目标项目、大类、影响数量和后续影响。

### 6.4 日历与排期

- 我的日历只保留当前团队内的个人日程与相关安排。
- 项目拍摄计划和通告属于项目空间；团队空间不提供生产甘特。
- 项目“执行计划”使用较大粒度表达从前期收口、主体拍摄、补拍到素材移交的整体时间窗口、责任人、阶段进度、关键路径和风险，不承载逐场次集合时间。
- “通告表”按拍摄日独立成表并保留版本。每张表包含总集合、开机与预计收工、天气和日照、基地停车、主场地、最近医院、当日场次与页数、演员个人通告、部门集合、特殊器材、安全提示、交通餐饮、关键联系人、次日预告和确认状态。
- 执行计划可使用横向排期容器；通告表保持可打印的宽表结构。窄屏只允许排期或通告文档内部横滑，不得造成页面级横向溢出。

### 6.5 审片与版本对比

审片入口默认是项目内的文件 / 文件夹浏览器，不直接打开固定视频：

- 左侧提供项目审片文件夹树，主区以操作系统资源管理器式列表展示文件夹和已上传视频。
- 列表至少显示名称、文件类型、意见数量、更新时间和审阅状态；点击文件夹进入下一级，点击具体视频才进入审阅工作台。
- 文件浏览器处理应用内已上传资源，不直接映射或操作用户本地文件系统。
- 上传中、处理中、失败、隔离、回收站和无权限必须保留明确状态。

打开视频后的审阅工作台分为三个稳定区域：

1. 中央为当前视频或双版本播放器。
2. 右侧列出当前版本的所有意见，并支持全部、未解决和我的意见筛选；点击意见跳转到对应时间码。
3. 底部集中放置播放、定位、时间线、时间码意见、区域标注、画面批注和提交入口。

双版本模式采用并排同步：

```text
Version A Player │ Version B Player
────────────── Shared Logical Time ──────────────
```

- 窄屏上下排列。
- 清楚标明两个版本、时长、帧率和当前逻辑时间。
- 评论列表按当前选中版本过滤。
- 不可靠对齐时显示警告，不宣称逐帧一致。
- 切换查看单版本是辅助模式；滑动差分不进入 P0。
- 返回文件浏览器后保留当前文件夹，避免每次审阅后从项目根目录重新定位。

### 6.6 联系人共享

联系人分处两个明确的管理位置：

1. 个人空间的“个人联系人”：归个人账号所有，默认仅本人和同权个人 Agent 可见；创建、编辑和共享都只在这里完成。
2. 团队空间的“资源库 / 联系人”：统一展示团队公用联系人与成员共享联系人；可新建团队联系人，但不能编辑他人拥有的个人联系人。

成员共享联系人仍归原成员所有，团队只读取明确开放的字段。个人联系人未共享给当前团队时，不得出现在团队资源库。

“当前项目引用”不是联系人来源，只作为资源库中的项目关联筛选。

共享流程分四步：

1. 选择团队或指定项目。
2. 选择允许共享的字段。
3. 设置是否允许继续关联到新项目。
4. 查看影响后确认。

撤销共享与删除联系人是两个不同命令。撤销后，既有业务引用和快照继续存在，但不能建立新引用。

### 6.7 团队作品集

- 作品集入口只出现在团队导航。
- 候选来源只显示项目中已经批准的明确成片版本。
- 公开发布和替换已发布版本都进入统一高风险确认流程。
- 项目归档不导致已发布作品失效。

## 7. Agent 交互

### 7.1 同权模型

个人 Agent 的权限与发起用户本人完全相同：

- Agent 可见内容等于用户当前可见内容。
- Agent 可执行命令等于用户当前可执行命令。
- Agent 没有独立团队角色、项目角色或管理员能力。
- 对话期间权限发生变化时，实际执行按最新权限重新判断。

界面固定显示：

```text
授权主体：繁星
当前作用域：北岸影像 / 冬夜咖啡
执行身份：个人 Agent
```

### 7.2 抽屉结构

```text
Scope Header
Conversation
Plan / Sources / Tool Steps
Approval
Execution Result
Audit Link
Composer
```

- 默认继承当前团队作用域。
- 允许用户明确发起跨团队只读查询，但不会静默切换当前团队。
- 写操作目标不唯一时必须选择唯一项目。
- 只展示当前页面、意图和权限下可用的能力，不展示全量底层工具。

### 7.3 统一风险交互

| 等级 | 示例 | 交互 |
|---|---|---|
| 读取 | 查询任务、总结反馈 | 直接执行 |
| 草稿 | 创建通告草稿、作品文案草稿 | 执行后可撤销 |
| 批量变更 | 批量确认拆解项 | 展示数量和影响范围 |
| 高风险 | 发布通告、创建外链、邀请、权限修改、公开作品、彻底删除 | 必须明确确认 |

确认界面必须显示：

- 目标团队和项目。
- 将执行的命令。
- 影响对象和数量。
- 外部接收者或公开范围。
- 是否可撤销。
- 当前用户是否仍有权限。

## 8. 页面状态矩阵

所有核心页面必须覆盖：

- Loading。
- Empty。
- Error。
- No permission。
- Offline / degraded。
- Processing。
- Partial success。
- Approval required。

专项状态：

| 模块 | 必须补充的状态 |
|---|---|
| 工作台 | 当前团队部分失败、私人占用、未选择团队 |
| 脚本 | 解析失败、历史版本、版本冲突、来源过期 |
| 拆解 | 无候选、待确认、来源缺失、部分确认 |
| 联系人 | 字段遮蔽、撤销共享、历史引用、疑似重复 |
| 素材 | 上传中、处理中、取消、重试、隔离、回收站 |
| 审片 | 代理处理中、版本不足、不可对齐、链接撤销或过期 |
| Agent | 无作用域、运行中、待审批、越权、取消、部分成功 |
| 作品集 | 无批准成片、待发布、已发布、待下架 |

## 9. 确定性 Mock Fixture

低保真原型固定使用以下账号和数据：

- 当前账号：繁星。
- 团队一：北岸影像，团队所有者。
- 团队二：午夜制片厂，团队成员。
- 当前项目：冬夜咖啡，角色为制片。
- 第二项目：水星香水，角色为后期。
- 外部项目：河岸纪录片，只拥有项目访问权。
- 脚本：`冬夜咖啡_拍摄稿` v7。
- 分镜：`车站段落分镜` v4。
- 审片：`30s 主片` v11 与 v12。
- 联系人：个人页面固定包含已分享和未分享的个人联系人；团队资源库只包含当前团队的团队联系人和已授权成员共享记录。

原型状态开关固定支持：

```text
normal
loading
empty
no-permission
offline
partial
expired
```

## 10. 低保真验收

1. 用户能明确区分团队选择、当前团队和项目功能归属。
2. 进入应用必须先选择团队；当前会话一次只在一个团队中工作。
3. 看板固定包含当前团队任务看板和我的排期；月历与可搜索筛选的便签/笔记位于独立个人页面，Agent 通过全局入口随时可用。
4. 顶部菜单切换团队时必须回到 `/team-select`，选择后再进入新团队工作台。
5. 所有项目页面都通过顶部菜单栏中的同一个项目选择器切换当前项目，页面内容区不出现重复项目选择控件。
6. 团队只负责组织、成员、权限、公共资源和资产；生产排期只在项目空间。
7. 脚本到拆解再到通告能串成一个连续流程。
8. 个人联系人只在个人页面管理；共享确认后按字段出现在团队资源库，未共享记录不可见。
9. 审片能表达版本、时间码评论、标注和双版本对比。
10. 客户审片界面不暴露团队导航。
11. 作品集只位于团队作用域。
12. Agent 抽屉在任何内部页面可用并继承当前团队作用域。
13. Agent 明确显示“权限与繁星本人一致”，高风险动作仍要求确认。
13. 每个核心页面至少能演示正常、空、错误或无权限中的相关状态。
14. 桌面与窄屏均不存在文字溢出、控件重叠或无法操作的关键入口。

通过低保真评审后，下一阶段只做高保真视觉与交互细化；真实服务接入仍需等待 UI Contract Freeze。
