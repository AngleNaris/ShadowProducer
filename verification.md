# ShadowProducer 阶段验证记录

> 日期：2026-08-14
> 阶段：需求基线、技术栈基线、交互原型 UI v0.8

## 1. 文档检查

- `PRODUCT_REQUIREMENTS.md`：UTF-8 无 BOM；24 个连续主章节；代码围栏配对。
- `TECHNICAL_ARCHITECTURE.md`：UTF-8 无 BOM；30 个连续主章节；代码围栏配对。
- `LOW_FIDELITY_UI.md`：UTF-8 无 BOM；10 个连续主章节；代码围栏配对。
- 个人 Agent 已明确使用发起用户账号作为授权主体；Agent 身份只增加执行归因和审计信息，不产生独立权限。
- UI 与 Agent 共用业务命令、执行时重新鉴权、高风险确认和审计规则。

## 2. 技术版本检查

2026-08-13 通过包仓库和官方发行信息复核了技术基线，并修正了过时的分支声明。

架构文档只固定稳定主版本或次版本范围；初始化真实仓库时仍需再次核对安全公告并由锁文件固定精确版本。

## 3. 静态原型检查

- `low-fidelity/index.html` 的内联 JavaScript 通过 `node` 语法解析。
- 原型包含团队选择页、当前团队个人工作台、项目生产页面、全局 Agent 抽屉、客户受限审片页和 7 种状态开关。
- 原型使用确定性 Mock 数据，不依赖数据库、对象存储、FFmpeg、模型供应商或后台服务。
- 文件保持 UTF-8 无 BOM。
- UI 已重构为中性浅色画布 + 克莱因蓝强调的工作台：直角细边框、克莱因蓝（#002FA7）仅作强调色、紧凑侧栏、顶栏搜索与统一线性图标，不使用大面积彩色背景。
- 顶栏显示当前团队；未选择团队时只显示团队选择入口，不能直接进入普通工作区。
- 当前团队菜单提供返回团队选择页的切换入口；团队切换不会在当前页直接替换上下文。
- 原型页面使用 `#/team-select`、`#/team/:teamId/:view` 和 `#/project/:teamId/:projectId/:view` Hash 语义。
- 工作台改为当前团队范围内的任务看板和 `我的排期` 甘特；便签/笔记迁移到 `#/team/:teamId/notes` 独立个人页面，个人 Agent 摘要面板已删除，Agent 仍通过全局 AI 入口打开。
- 个人导航的 Dashboard 文案改为“看板”；看板头部仅保留“繁星，下午好”，删除“个人工作台”和团队范围标签。
- `#/team/:teamId/calendar` 改为 6 周、42 格月历，支持月份切换、回到 `2026-08-14`、日期选择、当日议程和事件跳转。
- `#/team/:teamId/notes` 改为便签应用式工作区，支持关键词搜索、全部/置顶/个人便签/项目笔记筛选与空结果状态。
- 顶栏“客户端”入口已删除；客户受限审片的 Mock 页面与内部结构仍保留，等待后续确定正式入口。
- 当前团队菜单的按钮宽度改为内容宽度，桌面验证宽度为 `198px`，不再以整块中间顶栏作为 hover 区域。
- 所有 `workspace-panel` 标题栏会自动获得折叠按钮；折叠状态在当前原型会话的页面往返和局部重绘中保持。
- 团队级别不提供生产甘特；首页甘特只显示当前团队的个人日程、团队协同和项目执行任务，脚本、拆解、拍摄排期、通告和审片详情仍位于项目空间。
- 看板和通知只计算当前团队中与当前用户相关的事项。
- 联系人页面已按个人联系人、团队公用、成员共享三个所有权来源分区；当前项目引用改为二级筛选。
- 三类联系人使用独立 Mock 数据、来源说明、表格字段和详情操作；个人联系人共享弹窗绑定当前联系人和当前团队。
- 直接打开团队或项目深链接时，如果当前会话尚未选择团队，会先回到 `#/team-select`；刷新页面会重置会话门禁。
- Agent 从个人或团队空间发起写操作时必须选择唯一项目，目标选择不会改变用户当前浏览空间或看板筛选。

## 4. 真实浏览器回归

桌面与平板视口：1440 × 900、1024 × 768。

- 选择北岸影像后，工作台显示任务看板和我的排期；便签/笔记从个人导航进入独立页面，Agent 从全局 AI 入口打开。
- 顶部当前团队菜单的“切换团队”会完整返回 `#/team-select`，再选择另一团队进入新的工作台。
- 390 × 844 下团队选择卡片与便签列表为单列；页面没有横向溢出，看板列保持自身横向滚动。

窄屏视口：390 × 844。

- 390 × 844 下团队选择页、工作台、独立便签页和团队菜单均能正常渲染；工作台看板、甘特和便签页不发生文字或容器裁切。
- 看板列和首页甘特在窄屏保持各自内部横向滚动，页面和工作区不产生页面级横向溢出。
- 首页项目筛选切换到“冬夜咖啡”后只保留该项目排期；点击“车站夜戏执行”进入 `#/project/north/winter-coffee/schedule`，不会跳转到其他团队项目。
- 首页不再显示重复的“快速记录”、 “优先处理 / 下一项”焦点区，以及任务、便签和 Agent 的重复解释文案。

本轮浏览器批注回归：1255 × 912、390 × 844。

- Dashboard 中 `#guestModeButton`、个人 Agent 摘要和便签面板均不存在；只保留 2 个主面板，且 2 个面板都有折叠按钮。
- Dashboard 个人导航显示“看板”，标题为“繁星，下午好”，头部不存在 `.eyebrow` 与 `.scope-line`。
- 折叠“我的任务”后任务筛选和看板内容隐藏，`aria-expanded` 变为 `false`；跳转便签页再返回 Dashboard 后仍保持折叠状态。
- `#/team/north/calendar` 渲染 42 个日期格，`2026-08-14` 标记为今天；上月、下月、今天、日期选择、议程更新和事件跳转均通过。
- `#/team/north/notes` 实际显示 2 条当前团队可见记录；搜索“雨效”得到 1 条，四种筛选与空结果状态均通过，详情弹窗正常打开。
- 390 × 844 下笔记为单列；月历保持局部横向滚动并自动将所选日期居中。
- 1255 × 912 与 390 × 844 均无页面级横向溢出；浏览器控制台为 0 errors、0 warnings。

## 5. 视觉证据

- `output/playwright/ui-refactor-final-1440x900.png`
- `output/playwright/ui-refactor-final-390x844.png`
- `output/playwright/ui-refactor-final-mobile-guest.png`
- `output/playwright/ui-refactor-desktop-snapshot.md`
- `output/playwright/ui-refactor-console.txt`
- `output/playwright/gantt-dashboard-1440x900.png`
- `output/playwright/gantt-dashboard-1024x768.png`
- `output/playwright/gantt-calendar-component-390x844-final.png`
- `output/playwright/contacts-shared-1440x900.png`
- `output/playwright/contacts-shared-390x844-fixed.png`
- `output/playwright/scope-redesign-final-1440x900.png`
- `output/playwright/scope-redesign-agent-mobile-390x844.png`
- `output/playwright/scope-redesign-final-snapshot.md`
- `output/playwright/scope-redesign-console.txt`
- `output/playwright/dashboard-signals-1440x900.png`
- `output/playwright/dashboard-signals-empty-1440x900.png`
- `output/playwright/dashboard-signals-1024x768.png`
- `output/playwright/dashboard-signals-390x844.png`
- `output/playwright/dashboard-signals-1440-snapshot.md`
- `output/playwright/dashboard-signals-console.txt`
- `output/playwright/dashboard-continuous-panel-1440x900.png`
- `output/playwright/dashboard-continuous-panel-1024x768.png`
- `output/playwright/dashboard-continuous-panel-390x844.png`
- `output/playwright/dashboard-continuous-panel-agent-1440x900.png`
- `output/playwright/dashboard-continuous-panel-1440-snapshot.md`
- `output/playwright/dashboard-continuous-panel-1024-snapshot.md`
- `output/playwright/dashboard-continuous-panel-390-snapshot.md`
- `output/playwright/dashboard-continuous-panel-console.txt`
- `output/playwright/dashboard-feedback-v07-1255x912.png`
- `output/playwright/notes-page-v07-1255x912.png`
- `output/playwright/notes-page-v07-390x844.png`
- `output/playwright/dashboard-v08-1255x912.png`
- `output/playwright/calendar-v08-1255x912.png`
- `output/playwright/notes-v08-1255x912.png`
- `output/playwright/notes-v08-390x844.png`
- `output/playwright/calendar-v08-390x844.png`

## 6. 当前边界

- 高保真 Next.js 工作台已经存在；脚本协作编辑是第一条接入真实 API 与 PostgreSQL 的功能链路，其余工作区仍以确定性 Fixture 为主。
- 脚本正文、版本修订、评论、参与者和写权限已经由 Fastify API 返回；实时光标、在线状态和 WebSocket 协作尚未实现。
- 日历、资源库、审片媒体、Worker 与 Agent 业务命令仍未接入真实服务，不应将其 UI 交互视为生产能力。
- 下一步应沿用本轮契约、应用服务、权限反查、幂等和审计边界，逐条迁移其余垂直业务链路。

## 7. 临时进程与清理

本轮 UI 风格重构为真实浏览器验证启动了一个本机静态服务：

- 根 PID `5096`，监听子进程 `46912`，控制台子进程 `29440`。
- 启动时间：2026-08-13 15:08:26 +08:00。
- 端口：`8766`。
- 用途：仅服务 `low-fidelity/`，用于桌面、平板和手机浏览器回归。

验证结束后已按 PID 所有权终止完整进程树。最终复核结果：

- PID `5096`、`46912`、`29440` 均不存在。
- 本机端口 `8766` 无监听。
- 原先占用端口 `8765` 的现有进程未被修改或终止。
- 没有有意保留的持续进程或测试服务。

本轮联系人与甘特重构另启动了一个本机静态服务：

- 根 PID `3208`，监听子进程 `13772`，控制台子进程 `21740`。
- 启动时间：2026-08-13 16:06:01 +08:00。
- 端口：`8767`。
- 用途：仅服务 `low-fidelity/`，用于 1440 × 900、1024 × 768 和 390 × 844 浏览器回归。

验证结束后已按 PID 所有权终止完整进程树。最终复核结果：

- PID `3208`、`13772`、`21740` 均不存在。
- 本机端口 `8767` 无监听。
- 原先占用端口 `8765` 的 PID `21932` 仍正常监听，未被修改或终止。
- 本轮没有有意保留的持续进程或测试服务。

本轮空间切换与登录落点重构继续使用一个独立静态服务：

- 根 PID `29644`，监听子进程 `47432`。
- 启动时间：2026-08-13 17:56:25 +08:00。
- 端口：`8768`。
- 用途：验证“全部工作”、看板筛选、团队/项目局部空间选择、Hash 恢复、Agent 唯一写入目标和移动端甘特。

验证结束后已按 PID 所有权终止完整进程树。最终复核结果：

- PID `29644`、`47432` 均不存在。
- 本机端口 `8768` 无监听。
- 原先占用端口 `8765` 的 PID `21932` 仍正常监听，未被修改或终止。
- 本轮没有有意保留的持续进程或测试服务。

本轮首页动态行动提示重构使用独立静态服务：

- 根 PID `43364`，监听子进程 `22672`。
- 启动时间：2026-08-13 20:11:44 +08:00。
- 端口：`8769`。
- 用途：验证行动提示默认态、逐条隐藏、零事项无占位、业务跳转、通知铃行为以及 1440 × 900、1024 × 768、390 × 844 响应式表现。

验证结束后按 PID 所有权终止完整进程树，并复核：

- PID `43364`、`22672` 均不存在。
- 本机端口 `8769` 无监听。
- 原先占用端口 `8765` 的 PID `21932` 未被修改或终止。
- 本轮没有有意保留的持续进程或测试服务。

本轮首页清理与甘特接入使用独立静态服务：

- 端口：`8772`。
- 用途：验证首页任务看板、当前团队甘特、项目筛选、甘特任务跳转，以及 390 × 844 响应式结构。
- 内联 JavaScript 通过 `node` 语法解析。
- IAB 中通过 DOM/CUA 卡片交互完成团队选择；直接访问团队深链接会按预期回到 `#/team-select`。

本轮浏览器批注修改使用独立 Playwright 会话和本机静态服务：

- Playwright 会话：`shadowproducer-feedback-0814`，根 PID `25092`；其 Chrome 子进程树仅用于本轮桌面与移动端回归。
- 静态服务：根 PID `18188`，监听子进程 PID `19736`，端口 `18771`，仅绑定 `127.0.0.1` 并服务 `low-fidelity/`。
- `file://` 首次验证被浏览器安全策略拒绝，因此改用上述本机静态服务；未修改已被其他进程占用的端口 `8771`。
- 验证结束后 Playwright 会话及完整 Chrome 子进程树已关闭，PID `18188`、`19736` 已终止，端口 `18771` 已释放。
- 本轮 `.playwright-cli` 快照与临时日志目录已删除；3 张验收截图已迁移到 `output/playwright/`。
- 没有有意保留的持续进程、浏览器会话或测试服务。

本轮 v0.8 日历与便签升级直接验证 `file://` 原型，没有启动静态服务或监听端口：

- 使用 Codex 自带 Playwright 运行时调用本机 Chrome，记录的浏览器根 PID 为 `34480`、`32348`、`54260`。
- 每个浏览器均在一次性脚本的 `finally` 中关闭；验收后复核上述 PID 均不存在。
- `playwright-cli --help` 在当前 Windows/Node 组合触发工具自身断言退出，因此降级为同版本 Playwright Node API；该探测命令已退出，未留下新会话。
- 没有有意保留的持续进程、浏览器会话、静态服务或监听端口。

## 8. 2026-08-18 资源库与联系人信息架构验证

本轮将个人联系人移入个人空间，并把团队联系人与团队素材合并为团队资源库。已完成以下验证：

- 内联 JavaScript 通过 `new Function(...)` 编译，结果为 `INLINE_SCRIPT_OK`。
- HTML 结构栈、CSS 大括号、静态 ID、UTF-8 无 BOM 检查通过。
- 旧的 `contacts` / `assets` 团队路由可映射到资源库对应分类。
- 资源入口可携带联系人/素材分类和项目筛选；普通资源库入口会清空项目筛选。
- 北岸资源库初始包含已共享的个人联系人 `lin`，不包含未共享的 `lu` 和仅共享到其他团队的 `sun`。
- 个人联系人按字段共享后会立即进入当前团队资源库，未授权字段继续显示为“字段未共享”。
- 团队资源库可创建团队联系人；新记录归当前团队所有，并回到资源库联系人分类。
- 已移除旧 `state.contactTab` / `data-contact-tab` 事件路径，创建、编辑、共享和资源标签事件均有对应处理。

浏览器限制：

- 已接管用户现有的本地 `file://` 原型标签页，但浏览器安全策略拒绝重新加载该本地页面。
- 本轮未绕过策略、未切换到其他浏览器通道，因此当前代码未取得新的真实浏览器截图；交互闭环由本地 DOM 渲染与实际函数源码断言覆盖。

资源清理：

- 本轮没有启动静态服务、浏览器子进程、Worker 或其他持续进程。
- 没有创建临时文件，也没有有意保留的监听端口或后台任务。

## 9. 2026-08-18 顶部项目选择器验证

本轮将所有项目空间中的项目切换控件统一迁移到顶部菜单栏：

- 页面标题区不再生成 `localScopeSwitcher`、`localProjectSelect` 或其他页面级项目选择器。
- 顶部项目选择器只在项目空间显示，个人空间和团队空间保持隐藏。
- 选择器只列出当前团队项目；从“冬夜咖啡”切换到“城市慢行”时保留当前模块，例如继续停留在“脚本与分镜”。
- 项目切换后的 Hash 已验证为 `#/project/north/city-walk/scripts`。
- 内联 JavaScript 编译、HTML 结构、CSS 大括号、静态 ID 与 UTF-8 无 BOM 检查通过。
- 桌面顶部栏使用团队入口与项目选择器并列布局；窄屏时项目选择器移动到顶部栏第二行并占满可用宽度。

浏览器限制与资源清理：

- 本轮继续遵守现有 `file://` 页面重载限制，未绕过浏览器安全策略，因此没有生成新的浏览器截图。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程，也没有创建临时文件。

## 10. 2026-08-18 顶部团队菜单视觉优化验证

本轮将顶部团队菜单从占满中间区域的横向条幅收紧为团队/项目并列的工作范围控件：

- 团队菜单在桌面端限制为紧凑宽度，不再通过 `flex: 1` 占满顶部栏。
- 团队与项目控件统一为 44px 高的方角组合，沿用现有颜色变量、边框和字体层级。
- 团队菜单只显示团队名称与团队角色，不再重复当前页面或“项目空间”等上下文信息。
- 旧的字符箭头已替换为现有 `icon-chevron-down` SVG；菜单展开时箭头旋转并显示明确的焦点状态。
- `teamContextTrigger` 已补充 `aria-haspopup="menu"` 与 `aria-controls="teamSwitchMenu"`。
- 620px 及以下视口中，团队菜单和项目选择器改为上下排列并占满可用宽度。

代码级验证结果：

- 内联 JavaScript 通过 `new Function(...)` 编译，结果为 `INLINE_SCRIPT_OK`。
- HTML 静态结构栈为空，CSS 大括号深度为 0。
- 静态 ID 无重复，文件为 UTF-8 无 BOM。
- 团队按钮包含 SVG 下拉图标，旧 `.team-context-trigger::after` 字符箭头不存在。
- 团队菜单无障碍属性与项目选择器响应式规则均存在。

浏览器限制与资源清理：

- 已接管用户现有的 `file://` 原型标签页并尝试刷新，但浏览器安全策略明确拒绝本地文件重载，同时禁止换用其他浏览器或间接方式绕过。
- 因此本轮没有新的真实浏览器截图；用户手动刷新现有标签页后即可看到新版顶部团队菜单。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程，也没有创建临时文件。

## 11. 2026-08-18 项目菜单与项目中心重构验证

本轮统一顶部团队/项目切换体验，并将项目中心改为进度与动态工作视图：

- 删除原生 `topProjectSelect`，改为与团队菜单一致的自定义项目按钮和 `topProjectMenu`。
- 两个顶部菜单共用方角按钮、双层上下文文字、SVG 箭头、展开状态、菜单头与列表交互。
- 项目菜单只列出当前团队项目，当前项目使用左侧强调线和“当前项目”状态标识。
- 切换项目继续保留当前项目模块路由；点击页面外部或按 `Escape` 会关闭两个上下文菜单。
- 项目中心渲染函数中已移除 `stats-grid`、指标卡、模块缩略卡和“模块入口”。
- 新项目中心只保留一个连续面板：左侧为整体完成度、阶段链和关键节点，右侧为当前项目动态追踪。
- 动态条目明确显示项目/来源模块、操作者和时间；关键节点与动态均可进入对应业务模块。
- 四个现有项目均具备对应的进度、阶段和关键节点 Mock 数据。

代码级验证结果：

- 内联 JavaScript 通过 `new Function(...)` 编译，结果为 `INLINE_SCRIPT_OK`。
- HTML 静态结构栈为空，CSS 大括号深度为 0。
- 静态 ID 无重复，`low-fidelity/index.html` 为 UTF-8 无 BOM。
- `topProjectSelect` 与 `.top-project-switcher select` 均不存在。
- `renderProject()` 同时包含“项目进度”和“动态追踪”，且不包含 `stats-grid`、`moduleCards` 或“模块入口”。
- `winter-coffee`、`city-walk`、`mercury-perfume`、`river-doc` 均存在进度配置。

浏览器限制与资源清理：

- 浏览器安全策略此前已明确禁止刷新当前 `file://` 页面，并禁止通过其他浏览器或间接方式绕过，本轮未重复尝试。
- 因此本轮没有新的真实浏览器截图；用户手动刷新现有标签页后可验收菜单统一与项目中心新结构。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程，也没有创建临时文件。

## 12. 2026-08-18 大规模项目菜单验证

本轮将顶部项目菜单从短列表升级为可处理几十个项目的项目查找器：

- 支持按项目名称、状态和当前角色搜索。
- 支持“全部 / 进行中 / 已归档”状态筛选。
- 当前项目固定优先，最近访问项目排在普通项目之前，并保留最近 5 项。
- 通过项目菜单、项目卡、任务或动态跳转进入项目时，都会更新同一份最近访问顺序。
- 结果列表使用固定最大高度和独立纵向滚动，不再随项目数量无限拉长菜单。
- 显示当前结果数与团队项目总数，并提供搜索无结果空态。
- 打开菜单时自动聚焦搜索框；方向键可在结果中移动，搜索框回车打开首个匹配结果，`Escape` 关闭并返回项目触发器。
- 移动端继续使用左右 12px 的可用宽度，不依赖固定桌面菜单宽度。

规模验证使用 50 个模拟项目执行纯逻辑测试，结果如下：

- 50 个项目均可进入排序结果。
- 当前项目保持第 1 位，两个最近访问项目紧随其后。
- 搜索“项目 42”只返回唯一目标项目。
- 5 个模拟归档项目全部进入归档筛选。
- 45 个非归档项目全部进入进行中筛选。

基础验证：

- 内联 JavaScript 通过 `new Function(...)` 编译，结果为 `INLINE_SCRIPT_OK`。
- CSS 大括号深度为 0，静态 ID 无重复。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程。

## 13. 2026-08-18 脚本与分镜工作区验证

本轮将原先混合的“文档 / 版本、场次 / 镜头、属性 / 来源 / 评论”三栏页面拆分为两个一级工作区：

- `脚本` Tab 提供当前版本可编辑正文、历史版本只读切换、撤销/重做、基础文字格式、选区评论、在线协作者、未解决评论、不可变来源和自动保存状态反馈。
- 正文修改会立即进入“有未保存修改”状态，并在确定性 Mock 延时后恢复为“已自动保存”；编辑内容在当前原型会话内保留。
- `分镜` Tab 使用同一份 5 镜头数据提供卡片、表格和 Layout 节奏预演三种视图。
- 三种视图共享当前镜头选择；镜头详情统一显示缩略图、景别、时长、镜头参数、运动、内容、备注和脚本来源。
- Layout 按镜头时长生成连续时间线和 16:9 预演画布，画面内容与台词随播放头切换；播放、暂停、拖动定位、前后镜头跳转和时长调整均有对应交互。
- 播放或拖动跨越镜头边界时，画布、轨道高亮、当前镜头状态与右侧镜头详情同步更新。
- 修改任一镜头时长后，后续镜头起止位置和总时长重新计算；本视图只承担分镜节奏模拟，不包含完整剪辑软件能力。
- 表格镜头行支持键盘 `Enter` / `Space` 选择；编辑器图标按钮提供明确的 `title` 和 `aria-label`。
- 桌面采用连续三列工作区；中等宽度将协作侧栏移到下方；窄屏将版本轨道、镜头详情和视图控件重排为局部可滚动结构。

代码级验证结果：

- 内联 JavaScript 通过 `new Function(...)` 编译，结果为 `INLINE_SCRIPT_OK`。
- CSS 大括号深度为 0，`low-fidelity/index.html` 为 UTF-8 无 BOM。
- `data-scripts-tab`、`data-script-version`、`data-script-editor`、`data-storyboard-view` 和 `data-storyboard-shot` 交互契约均存在。
- 脚本 Tab 与卡片、表格、Layout 节奏预演三种分镜视图均完成确定性渲染；生成 HTML 的标签栈闭合且不存在嵌套按钮。
- 初始 5 个镜头的确定性时间段为 `0-4`、`4-9`、`9-12`、`12-16`、`16-22` 秒，总时长为 `22` 秒；在每个镜头边界和序列终点执行当前镜头查找均得到预期镜头。
- 将 `12-02` 从 `5` 秒调整为 `7` 秒后，镜头起点重排为 `0`、`4`、`11`、`14`、`18` 秒，总时长更新为 `24` 秒，证明后续镜头会随时长修改自动移动。
- Animatic 生成结果包含 16:9 画布、播放位置输入、画面轨、台词轨和带当前镜头标识的详情检查器，结果为 `ANIMATIC_CONTRACTS_OK`。
- 静态 ID 无重复，旧 `.three-panel`、`.panel-column`、`.tree-item` 和 `.scene-item` 页面契约已删除。

浏览器限制与资源清理：

- 当前内置浏览器仍受既有 `file://` 页面刷新安全策略限制；对已打开原型页的刷新请求被明确拒绝，本轮未改用其他浏览器或静态服务绕过，因此没有生成新的浏览器截图。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程，也没有创建临时文件。

## 14. 2026-08-18 专业制片拆解分类验证

本轮将单张混合候选表改为按制片部门组织的连续拆解工作区：

- 固定大类为演员与角色、场景与场地、美术与道具、服化造型、摄制设备、特殊执行与保障。
- 左侧分类导航分别显示每个大类的待确认数量；右侧只呈现当前大类候选，不再把场地、演员、道具和设备混在同一张总表中。
- 每条候选显示需求类型与规格、专业准备要求、负责部门、来源文档与原文、Agent 置信提示、处理状态和来源查看入口。
- 桌面使用带内部隔线的连续双栏工作区；中窄屏将分类导航改为局部横向滚动，候选表保持自身滚动，不产生额外页面级横向结构。
- 勾选只加入当前操作选择，不再直接改变候选确认状态。
- 批量确认只处理当前大类中明确选中的候选；确认弹窗显示目标团队和项目、执行命令、影响数量与进入正式制片清单后的影响。
- 确认完成后，当前大类、分类计数、页面待确认总数和侧栏计数同步更新；其他大类的选择不会被当前确认操作带入。

代码级验证结果：

- 内联 JavaScript 编译结果为 `INLINE_SCRIPT_OK`，CSS 大括号深度和最小深度均为 0。
- 六个大类与六条确定性候选均具有唯一分类，结果为 `BREAKDOWN_TAXONOMY_OK`。
- 演员与角色、场景与场地分别生成独立且闭合的 HTML，不包含其他大类候选，结果为 `BREAKDOWN_RENDER_OK`。
- 跨类别选择后，确认弹窗只包含当前大类选中的候选，结果为 `BREAKDOWN_SELECTION_SCOPE_OK`。
- 来源查看结果包含不可变来源版本、原文位置和 Agent 判断，结果为 `BREAKDOWN_SOURCE_TRACE_OK`。
- Checkbox 事件只修改 `selectedBreakdown`，不直接修改 `acceptedBreakdown`，结果为 `CHECKBOX_IS_SELECTION_ONLY_OK`。
- 静态 ID 无重复，所有修改文件保持 UTF-8 无 BOM。

浏览器限制与资源清理：

- 当前内置浏览器的 `file://` 刷新仍受安全策略限制，本轮没有绕过该限制，因此未生成更新后的浏览器截图。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程。

## 15. 2026-08-18 执行计划与专业通告表验证

本轮将原先按拍摄日平铺的日程页拆分为两个一级工作区：

- `执行计划` 面向项目整体，按 8/10–8/23 展示前期准备、拍摄执行、补拍与收尾、后期交付四个阶段，并呈现整体进度、当前阶段、负责人、关键路径和风险。
- `通告表` 面向每日现场执行，以一张拍摄日对应一份独立通告表的方式组织 8/19、8/20、8/21 三份确定性 Mock。
- 通告表覆盖组讯、开拍、收工、天气、日出日落、基地、拍摄地、医院、场次与页数、演员到场、部门通告、安全提示、交通后勤、关键联系人和次日预告等专业影视制作字段。
- 日期列表用于切换不同拍摄日的通告表；两个一级 Tab 保持各自清晰的职责边界。
- 页面沿用连续工作台、直角边框与内部隔线；窄屏下执行阶段、通告摘要和明细表会重排为可读的单列结构。

代码级验证结果：

- 内联 JavaScript 解析通过，结果为 `InlineJavaScriptParse=PASS`。
- 执行计划确定性渲染通过，结果为 `ExecutionRender=PASS`。
- 通告表渲染与日期选择通过，结果为 `CallSheetRenderAndSelection=PASS`。
- 通告表数量为 `3`；选择 `sd-06` 后正确渲染 8/20 通告表。
- `data-schedule-tab` 与 `data-call-sheet` 交互契约存在；Tab 和面板 ID 均唯一。
- 生成的通告表 HTML 不包含 `undefined` 或 `null`。
- 旧 `.schedule-grid` 与 `.shoot-day` 页面契约已删除。

浏览器限制与资源清理：

- 当前内置浏览器拒绝控制或刷新本地 `file://` 页面，并明确禁止使用替代浏览器或本地服务绕过，因此本轮没有新的桌面或移动端截图。
- 真实浏览器中的视觉布局、响应式和点击路径仍需用户手动刷新当前标签页后验收。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程，也没有创建临时文件。

## 16. 2026-08-18 审片文件浏览器与审阅工作台验证

本轮将原先直接打开固定视频的审片页拆分为两个层级：

- 默认进入项目内的文件 / 文件夹浏览器，左侧为文件夹树，主区按资源管理器式列表展示名称、类型、意见数量、更新时间和状态。
- 点击文件夹进入对应视频集合；点击具体视频后才进入审阅工作台，返回时保留当前文件夹。
- 审阅工作台中央显示单版本或双版本视频，右侧固定列出当前视频的所有意见，底部集中放置播放、前后定位、时间轴、时间码意见、区域标注和画面批注。
- 右侧意见支持全部、未解决和我的意见筛选；点击意见会定位到对应时间码。
- 文件夹内搜索可即时隐藏不匹配的文件；版本切换会保持当前文件夹内的版本范围。
- 播放按钮会实际推进原型时间码，到达视频末尾后停止；15 秒视频使用 15 秒时间轴，不再沿用固定 30 秒上限。
- 每个列表中声明有意见的视频都具备匹配的确定性意见数据，不再出现列表显示有意见而工作台为空的矛盾。

代码级验证结果：

- 内联 JavaScript 解析通过，结果为 `InlineJavaScriptParse=PASS`。
- 文件浏览器与文件夹渲染通过，`30s 主片` 文件夹包含 4 个可打开视频。
- 文件意见数量与工作台渲染一致，结果为 `ReviewAssetOpinionCounts=PASS`。
- 15 秒视频的当前时间、滑杆上限和总时长均正确收敛到 15 秒，结果为 `ReviewShortDurationClamp=PASS`。
- 双版本模式生成 2 个播放器，结果为 `ReviewComparePlayers=2`。
- 播放计时会推进并可停止，结果为 `ReviewPlaybackAdvance=PASS`、`ReviewPlaybackStop=PASS`。
- CSS 大括号平衡、静态 ID 唯一、图标引用完整；修改文件保持 UTF-8 无 BOM。

浏览器限制与资源清理：

- 当前内置浏览器仍拒绝控制或刷新本地 `file://` 页面，本轮没有通过替代浏览器或本地服务绕过，因此没有新的桌面或移动端截图。
- 真实浏览器中的布局、文件夹点击、视频进入和窄屏重排仍需用户手动刷新当前标签页后验收。
- 本轮没有启动静态服务、浏览器子进程、Worker、监听端口或其他持续进程，也没有创建临时文件。

## 17. 2026-08-24 高保真工作台迁移验证

本轮按 `kasidia-frontend-law` 将已确认的低保真项目空间迁移到 Next.js 高保真工作台：

- 抽取统一 `WorkspaceShell`，侧栏、顶部团队范围、可搜索项目切换器、移动端菜单与 Agent 抽屉共用一套组件和响应式契约。
- 项目切换器支持名称搜索、进行中/已归档筛选、最近项目、当前项目优先和固定高度滚动列表，可承载几十个项目而不无限拉长顶部菜单。
- 项目概览改为项目进度、阶段链、关键节点和来源明确的项目动态，不再使用 KPI 卡片或模块入口卡片。
- 脚本与分镜拆分为一级 Tab；脚本提供当前版本编辑、历史版本只读、协作者、评论与自动保存反馈。
- 分镜共用同一份镜头数据提供卡片、表格和 Layout 节奏预演；Layout 根据镜头时长自动计算时间线，可播放、定位、逐镜头跳转并随时长调整重排。
- 审片默认进入文件/文件夹浏览器，打开视频后进入审阅工作台，右侧集中意见，底部集中播放与批注控制。
- shadcn 基础组件统一为零圆角；相同语义控件复用同一组件契约。
- 页面切换、菜单、抽屉与内容状态使用成对进入/退出动画；Agent 抽屉按 `Escape` 关闭后焦点返回触发按钮。

工程验证：

- `pnpm --filter @shadowproducer/web format`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。

真实浏览器验证：

- 已验证 `390 × 844`、`768 × 1024`、`1440 × 900`。
- 三种视口均无页面级横向溢出，浏览器控制台为 `0 warnings / 0 errors`。
- 脚本当前版本、历史版本、编辑与自动保存状态通过。
- 分镜卡片、表格、Layout、播放、镜头跳转和时长驱动的时间线重算通过。
- 审片文件浏览器、进入审阅工作台、返回文件夹路径通过。
- 项目搜索、状态筛选、最近项目和归档项目切换通过。

视觉证据：

- `output/playwright/highfi-project-desktop-final.png`
- `output/playwright/highfi-project-mobile.png`
- `output/playwright/highfi-storyboard-layout-desktop.png`
- `output/playwright/highfi-storyboard-layout-tablet.png`
- `output/playwright/highfi-storyboard-layout-mobile.png`

预览服务：

- 根 PID `30572`，Next.js 监听 PID `19420`。
- 启动时间：2026-08-24 09:00:18 +08:00。
- 地址：`http://127.0.0.1:3211/`，最终 HTTP 检查为 `200`，页面包含 `ShadowProducer` 标题。
- 该服务由本轮有意保留，继续用于右侧浏览器面板的人工检查；当前未清理 PID 或释放端口。

## 18. 2026-08-24 全量低保真功能迁移收口

本轮完成低保真 12 个可达工作区及共享入口在高保真 Next.js 工作台中的迁移收口：

- 个人空间：看板、我的日历、便签 / 笔记、个人联系人。
- 团队空间：团队概览、资源库、作品集。
- 项目空间：项目概览、脚本 / 分镜、制片拆解、拍摄与通告、审片。
- 共享入口：团队选择、Hash 深链接、团队 / 项目切换、移动端导航、Agent 抽屉与高风险确认。
- 旧 `contacts` / `assets` 入口继续映射到团队资源库。

交互补齐：

- 日历日期格使用真实右键菜单，提供“新建日程”和“打开当天列表”；双击日期打开当天全部安排，日程条目可直接进入对应项目模块。
- 日历事件改为完整日期键，跨月查看不会错误复用 8 月同日事件。
- 资源库移除原生 `select`，项目筛选统一使用 Radix 菜单；联系人和素材具备无结果空态、清除筛选和详情弹窗。
- 脚本编辑器的撤销、重做、快捷键、添加评论和版本记录均已接入确定性 Mock 状态，不再存在空转工具按钮。
- 审片意见数量改为动态计算；新增意见会同步更新待处理计数和时间轴标记，空筛选具有明确反馈。
- 修正手机端脚本工作区的最小内容宽度和网格压缩问题；窄屏改为纵向工作流，正文编辑区保持 `560px` 可用高度且页面无横向溢出。

工程验证：

- `pnpm --filter @shadowproducer/web format`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。

真实浏览器验证：

- 13 个入口（团队选择 + 12 个工作区）均完成实际 Hash 导航与非空渲染检查。
- 日历右键菜单、当天列表、日程跳转通过。
- 脚本撤销 / 重做、评论提交、版本记录通过。
- 资源库统一项目筛选、搜索空态、联系人详情、素材详情通过。
- 审片 15 秒版本时长约束、版本切换、评论动态计数和返回文件夹保留通过。
- 项目查找器搜索、`Escape` 关闭和焦点返回通过；Agent 抽屉 `Escape` 关闭并将焦点返回触发按钮。
- `375 × 812`、`768 × 1024`、`1024 × 768`、`1440 × 1000`、`1920 × 1080` 和等效 200% 缩放共 36 个高密度页面组合全部通过；无页面级横向溢出、无 `undefined` / `null` 泄漏。
- 浏览器控制台为 `0 errors / 0 warnings`。

视觉证据：

- `output/playwright/highfi-full-migration-desktop.png`
- `output/playwright/highfi-full-migration-mobile-script.png`

预览服务：

- 最终根 PID `35700`，Next.js 监听 PID `26316`，子进程 PID `7460`、`30516`、`32460`。
- 启动时间：2026-08-24 10:26:09 +08:00。
- 地址：`http://127.0.0.1:3211/`，HTTP `200`，页面标题包含 `ShadowProducer`。
- 旧预览进程树已按记录 PID 精确终止并确认端口释放；最终服务按用户要求有意保留，用于右侧浏览器面板人工检查。

## 19. 2026-08-24 顶部范围与 Kasidia 全站审计

本轮将团队和项目范围统一收敛到顶部栏，并按 Kasidia Frontend Law 修正共享基础：

- 顶部团队与项目使用同一套 `ScopeSwitcherTrigger + Popover + Command` 契约，均支持搜索、键盘关闭、焦点归还和移动端紧凑布局。
- 项目切换器保留最近项目、全部项目和状态筛选，可处理几十个项目；团队切换会同步更新团队默认项目。
- 侧栏项目摘要、“当前范围设置”和独立团队选择运行时入口已移除；侧栏只承担页面导航。
- 新增信号琥珀 `support` 辅助色，用于时间线、播放头、标注和次级提示；成功、风险、分类与媒体工作区颜色全部改为低饱和语义 token。
- 页面内 `amber`、`emerald`、`sky`、`blue` 等直接高饱和 Tailwind 色已清除；媒体工作区硬编码色已收敛为共享 media token。
- 共享图标按钮和顶部主要控件补足 44px 命中尺寸；脚本编辑器、分镜时间线、项目卡和审片时间码标记补齐可见焦点。

工程验证：

- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- `pnpm lint`：通过。
- 静态色彩扫描仅保留全局语义 token、应用图标品牌色和项目 ID 文本中的 `blue-hour`，页面组件不存在旧高饱和工具类或硬编码颜色。

真实浏览器验证：

- 12 个工作区在 `1440 × 1000` 和 `375 × 812` 下逐页检查，均无页面级横向溢出，顶部团队/项目切换器始终可见。
- `768 × 1024`、`1024 × 768`、`1920 × 1080` 与 `720 × 500` 高缩放近似视口检查通过。
- 项目搜索成功定位并打开“纸月亮”；团队搜索成功切换“午夜制作”，URL、团队和默认项目同步更新。
- 项目 Popover 和 Agent Sheet 均可用 `Escape` 关闭，并将焦点归还原触发按钮。
- 分镜 Layout 实际进入并验证时间线、播放头和 range 控件使用 `support`；视频审阅实际进入并验证批注标记、播放控件和意见侧栏。
- 浏览器控制台为 `0 errors / 0 warnings`；最终页面恢复为 `#/team/north/dashboard`。

预览服务：

- 本轮接管并精确终止旧预览根 PID `30516`；本轮中间预览根 PID `19420`、`36752` 已在重新构建后精确终止。
- 最终有意保留根 PID `7852`、Next.js 监听 PID `32876`，端口 `3211`。
- 地址：`http://127.0.0.1:3211/#/team/north/dashboard`；用于右侧浏览器面板人工检查。
- 日志：`output/runtime/preview-3211.out.log`、`output/runtime/preview-3211.err.log`；错误日志没有运行时错误。

## 20. 2026-08-28 功能层首条垂直切片

本轮将“脚本协作编辑”从组件内存状态迁移为首条真实持久化链路：

- 新增 `packages/contracts`，以 TypeBox 统一脚本工作区、版本更新、评论创建和错误响应契约。
- 新增 `packages/application`，脚本查询和写命令统一经过项目访问校验；API 不信任前端传入的团队范围。
- 新增 PostgreSQL 迁移，覆盖账号、团队成员、项目成员、脚本文档、版本、评论、幂等回执和审计日志。
- 新增 Fastify API：脚本工作区读取、带 `expectedRevision` 的版本更新、带幂等键的评论创建。
- Web 使用 TanStack Query 接入 API；当前版本 650ms 自动保存，历史版本只读，网络失败可重试，版本冲突不会静默覆盖。
- 参与者与写权限来自数据库；外部只读项目不会被前端误显示为可编辑。

自动验证：

- `pnpm typecheck`：5 个工作区包全部通过。
- `pnpm lint`：5 个工作区包全部通过。
- `pnpm test`：Fastify Inject 7 项通过，覆盖成功读取、缺失身份、越权、契约校验、乐观并发冲突和评论幂等。
- `pnpm build`：Next.js 生产构建与 API 严格类型构建通过。
- Compose 配置检查通过；PostgreSQL 17 容器健康，迁移与幂等种子执行成功。

真实浏览器验证：

- 路由：`#/project/north/winter-coffee/scripts`。
- 修改正文后显示“有未保存修改”，650ms 后显示“已自动保存”；刷新后正文仍存在，修订从 1 增至 2。
- 新增评论后未解决数从 2 增至 3；刷新后评论仍存在。
- 外部写入先将修订提升到 3，再提交旧修订时界面显示“检测到新版本”；载入新版后旧草稿不会覆盖服务端内容。
- `375 × 812`、`768 × 1024`、`1024 × 768`、`1440 × 1000`、`1920 × 1080` 下脚本编辑器均可见，页面级横向溢出均为 0。
- 浏览器控制台为 `0 errors / 0 warnings`。

数据库证据：

- 浏览器验证期间 `winter-coffee / v7` 修订从 1 增至 3、评论从 2 增至 3，证明正文与评论均真实持久化。
- 验证后通过正式 API 将正文恢复为确定性种子内容，最终修订为 4、正文长度为 136 字符。
- 本轮创建的验证评论按精确 UUID 删除；最终项目恢复为 2 条种子评论，未影响其他记录。
- 审计日志保留 3 次 `script.updated` 和 1 次 `script.comment.created`，用于追踪本轮写操作。

有意保留的本地服务：

- Web：既有进程树未被本轮重启或终止，监听 PID `29124`，端口 `3211`。
- API：本轮启动根 PID `18012`，监听 PID `27728`，端口 `3220`；用途为脚本功能联调。
- PostgreSQL：容器 `shadowproducer-postgres`，容器 ID 前缀 `1443598c0d70`，端口 `5433`，健康状态 `healthy`。
- 以上三项按用户要求继续保留，用于内置浏览器人工检查。

## 21. 2026-08-28 基本功能闭环扩展

本轮将个人生产力、联系人与制片主链从高保真 Mock 扩展为可持久化的基础功能：

- 个人任务、日历和便签通过统一 Workspace API 读写 PostgreSQL，写操作包含团队权限、幂等回执、乐观并发和审计。
- 个人联系人支持创建、编辑、删除和字段级团队共享；团队资源库同时呈现团队联系人和成员共享联系人，未共享字段不会返回给团队。
- 制片拆解支持查询与批量确认；确认操作使用 `expectedRevision` 防止旧页面静默覆盖新状态。
- 通告表支持查询、新建草稿、编辑核心现场字段和发布；新建操作具备幂等语义，编辑与发布具备 revision 冲突处理。
- 审片文件和时间码意见改由 PostgreSQL 提供；意见支持新增、解决和重新打开。尚未实现的上传、新建文件夹与对象存储入口保持禁用，不再显示假成功。
- API server 已正式装配 `ProductionService`、`PostgresProductionRepository` 与联系人服务，生产路由不再只存在于测试注入路径。

自动验证：

- `pnpm db:migrate`：通过，迁移与确定性种子可重复执行。
- `pnpm typecheck`：通过。
- `pnpm lint`：通过。
- `pnpm test`：4 个测试文件、19 项测试全部通过；覆盖权限拒绝、契约校验、幂等重放、乐观锁冲突、字段级联系人共享和生产主链写操作。
- `pnpm build`：通过。

真实 API 与 PostgreSQL 验证：

- 种子数据读取结果为拆解项 6 条、通告表 3 份、审片文件 5 项、审片意见 4 条、个人联系人 3 条。
- 通告表幂等重放成功；发布状态由 `草稿 rev1` 更新为 `已发布 rev2`，旧 revision 返回 `409`。
- viewer 写操作返回 `403`；审片意见由 `open rev1` 更新为 `resolved rev2`；对应审计记录正常生成。
- API 冒烟产生的临时数据、回执和审计均按精确 ID 清理，确定性种子数量恢复。

内置浏览器验证：

- 制片拆解：确认“顾遥”后页面即时显示“已确认”，刷新后仍保留；验收完成后通过正式 API 恢复为“待确认”。
- 通告表：将 8 月 21 日标题临时改为“室内咖啡店 · 浏览器验证”，刷新后仍回显；随后通过页面恢复原始标题。
- 审片：新增唯一时间码意见后刷新仍存在，标记已解决后刷新状态仍保留；临时意见及其创建/更新审计按精确 UUID 清理，意见计数恢复为 3 条待处理、1 条已解决。
- 联系人：创建个人联系人后刷新仍存在；只共享姓名、职业和公司后，团队资源库可见该联系人且电话、邮箱不返回；删除后个人列表和团队投影同步消失。临时联系人、共享、回执和审计均按精确 ID 清理。
- 审片工作台在当前内置侧栏视口无页面级横向溢出；首屏审片画面使用优先加载后，全新控制台时间窗口为 `0 errors / 0 warnings`。

当前边界：

- 身份仍由开发期 `x-shadow-account-id` Header 注入，Better Auth 尚未接入。
- 素材上传、对象存储、媒体转码、客户审片链接、实时协作传输与 Agent 编排尚未实现，不能视为生产就绪。

有意保留的本地服务：

- Web：监听 PID `29124`，端口 `3211`。
- API：watch 根 PID `6256`，当前监听子进程 PID `13332`，端口 `3220`，健康端点 `/healthz`。
- PostgreSQL：容器 `shadowproducer-postgres`，宿主端口 `5433`。
- 三项均非本轮启动，按用户要求继续保留用于内置浏览器检查。

## 22. 2026-08-28 团队资源库真实上传闭环

本轮将团队资源库从组件内存状态迁移为 PostgreSQL 与 S3 兼容对象存储支持的基础功能链：

- 新增素材、文件夹、上传意图、上传完成和元数据更新契约；应用服务统一执行团队写权限、项目范围、文件大小与 revision 校验。
- 新增 `asset_folders`、`team_assets` 迁移和 PostgreSQL repository；文件夹创建、上传意图、上传完成均写入幂等回执与审计。
- 新增 S3 兼容存储适配器和本地 MinIO；上传使用短期 Presigned PUT，下载由 API 鉴权后签发短期地址。
- 资源库使用 TanStack Query 接入真实列表与 mutation；导入、文件夹、收藏、评分、标签、备注、移动和归档刷新后均可回读。
- 对象存储就绪检查提前到素材记录创建之前，避免存储不可用时留下新的半成品记录。
- MinIO 不支持逐 bucket `PutBucketCors` 时只降级该明确的 `NotImplemented` 响应；本地允许来源由 `MINIO_API_CORS_ALLOW_ORIGIN` 显式配置。
- 素材更新时间统一由 PostgreSQL `now()` 生成，避免宿主与容器时钟漂移导致 `updatedAt` 早于 `createdAt`。

自动验证：

- `pnpm lint`：通过。
- `pnpm typecheck`：5 个工作区包全部通过。
- `pnpm test`：5 个测试文件、25 项测试全部通过；资源 API 6 项覆盖权限、文件夹幂等、上传完成、存储不可用不落库、元数据 revision 冲突和大小上限。
- `pnpm build`：Next.js 生产构建与 API 严格类型构建通过。

内置浏览器与对象存储验证：

- 从可见“导入素材”入口上传 109 字节文本文件；数据库状态从 `uploading rev1` 变为 `ready rev2`，MinIO 生成对应不可变对象。
- 刷新后素材仍存在；收藏、4 星、`车站` 标签、验收备注和移动到新文件夹均刷新回读成功，最终 revision 为 7。
- 经鉴权下载后的 SHA-256 与源文件一致；缺少账号 Header 的内容请求返回 `401`。
- 从页面归档后素材即时消失，刷新后仍不可见；当前侧栏视口页面级横向溢出为 0，全新日志时间窗口为 `0 errors / 0 warnings`。
- 首屏资源预览不再依赖已删除的旧 `asset1` Mock ID，主预览和首张可见卡片显式使用 eager 加载。

验收清理：

- 临时素材 UUID、文件夹 UUID、MinIO object key、3 条命令回执和 9 条审计记录均按精确标识删除。
- 上传失败留下的早期 `uploading` 验收行、对应回执和审计也按精确标识删除。
- 验收源文件与下载校验文件已删除；最终恢复为北岸影像 6 个活动素材、7 个活动文件夹，临时素材、文件夹、回执和审计计数均为 0。

当前边界：

- 当前是单文件、单次 PUT 的开发闭环，单文件上限 100 MB；Multipart、断点续传、失败会话清理和配额尚未实现。
- 尚未实现 checksum 入库、病毒扫描、FFprobe、缩略图、代理媒体、转码 Worker 与 CDN；确定性种子的预览图仍是开发期占位资源。
- 身份仍由开发期 `x-shadow-account-id` Header 注入，不能视为生产就绪。

有意保留的本地服务：

- Web：监听 PID `29124`，端口 `3211`。
- API：watch 根 PID `35696`，当前监听子进程 PID `19536`，端口 `3220`，健康端点 `/healthz`。
- PostgreSQL：容器 `shadowproducer-postgres`，容器 ID 前缀 `1443598c0d70`，宿主端口 `5433`，状态 `healthy`。
- MinIO：容器 `shadowproducer-minio`，容器 ID 前缀 `cafa71bd324a`，API `9100`、Console `9101`，状态 `healthy`。
- MinIO 本轮因 CORS 环境变量变更被精确重建并按用户要求继续保留；其余临时命令均已退出，未启动外部 Chrome 或额外浏览器进程。

## 23. 2026-08-28 团队作品集真实持久化切片

本轮将团队作品集从前端 Mock 状态迁移为 PostgreSQL 支持的内部策展功能：

- 新增作品集、作品内容和已通过候选契约；应用服务统一执行团队读写权限、幂等和 revision 冲突处理。
- 新增 `team_portfolios`、`portfolio_items` 迁移和 PostgreSQL repository；作品内容引用真实 `assetId + reviewFileId`，不复制对象地址或伪造本地 ID。
- 候选查询严格要求同团队项目、视频审片版本状态为“已通过”、素材状态为 `ready` 且未归档。
- 新增作品集列表、已通过候选、创建作品集和添加内容 API，并在 API server 中正式装配 `PortfolioService`。
- 作品集前端使用 TanStack Query 接入真实 API；移除本地文件上传、`Date.now()` 假 ID、固定假公开链接和仅修改组件 state 的发布行为。
- 当前明确只实现团队内部作品集；公开发布仍需要独立发布资产、CDN、权限确认和撤销链路，不伪装为已完成能力。

自动与真实数据验证：

- `pnpm db:migrate` 可重复执行；`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 均通过。
- 真实 API 最终返回北岸影像 1 个作品集、1 个内容条目、1 个已通过且关联素材的候选版本。
- 内置浏览器创建临时作品集并添加 `冬夜咖啡 / v11 / asset-winter-main-v11`，刷新后作品集与内容仍存在，证明不是本地 state 假写入。
- 临时作品集 `817c0a04-bd11-40c0-b168-2509f88fc2a8`、级联内容、2 条幂等回执和 2 条审计记录已按精确标识删除；最终恢复为 1 个作品集和 1 个内容条目。
- 当前内置浏览器视口无页面级横向溢出。

## 24. 2026-08-28 审片批准与作品集候选联动

本轮补齐审片版本从“审阅中”进入“已通过”及撤销批准的正式业务命令：

- 新增 `approve` 与 `revoke-approval` 显式命令 API；命令使用项目写权限、`expectedRevision`、幂等键、事务内审计和严格状态转换。
- 批准只允许可审阅的视频版本，撤销只允许“已通过”版本；“处理中”、文件夹、重复批准和重复撤销均不会被静默接受。
- 撤销批准将版本恢复为“审阅中”；作品集新增候选立即移除该版本，但已存在的作品集条目继续保留具体版本引用，避免历史记录断裂或静默替换。
- 审片详情顶部新增“批准版本 / 撤销批准”控件，复用现有 Button 与 Lucide 图标体系；提交中禁用，409 冲突会刷新列表缓存，状态变化同步失效作品集候选查询。
- 只有当前显示版本可以改变批准状态，历史版本模拟视图和“处理中”状态会禁用该操作。

工程验证：

- `pnpm db:migrate`：通过且确定性种子可重复执行。
- `pnpm lint`：5 个工作区包全部通过。
- `pnpm typecheck`：5 个工作区包全部通过。
- `pnpm test`：6 个测试文件、31 项测试全部通过；新增覆盖批准、幂等重放、幂等键复用冲突、无效重复批准、撤销、stale revision、viewer 拒绝和缺失身份。
- `pnpm build`：Next.js 生产构建与 API 严格类型构建通过。
- `git diff --check`：通过；仓库仍保持原有全部未跟踪工作区状态，未重置或清理用户文件。

内置浏览器与真实 PostgreSQL 验证：

- 在 `冬夜咖啡 / v11` 上执行撤销批准，按钮即时切换为“批准版本”；刷新后列表仍显示“审阅中”。
- 撤销后作品集候选由 1 条变为 0 条，但 `portfolio-winter-coffee` 的既有内容仍为 1 条。
- 重新批准后按钮即时切换为“撤销批准”；刷新后列表仍显示“已通过”，作品集候选恢复为 1 条。
- 验收产生的 2 个精确幂等回执和审计 ID `30`、`31` 已删除；`main-v11` 恢复为“已通过 / revision 1”，相关临时 receipt、audit 均为 0，作品集内容仍为 1 条。
- 默认 `1236px` 视口和 `375 x 812` 手机视口均无页面级横向溢出，批准控件可见，页面没有 `undefined` 文本泄漏。
- 本轮浏览器验证时间窗口控制台为 `0 errors / 0 warnings`。

当前边界：

- 审片文件仍缺少“从资源库 ready 视频创建审片版本”的正式入口；资源上传后还不能自行生成新的审片版本。
- 批准记录当前通过审计日志追踪，没有在审片文件响应中额外暴露批准人和批准时间。
- 对外公开作品集、客户审片链接、媒体转码和生产身份认证仍未实现。

有意保留的本地服务：

- Web：监听 PID `29124`，端口 `3211`。
- API：watch 根 PID `35696`，当前监听子进程 PID `36776`，端口 `3220`，健康端点 `/healthz`。
- PostgreSQL：容器 `shadowproducer-postgres`，宿主端口 `5433`，状态 `healthy`。
- MinIO：容器 `shadowproducer-minio`，API `9100`、Console `9101`，状态 `healthy`。
- 以上服务按用户要求继续保留，未启动外部 Chrome 或额外浏览器进程。

## 25. 2026-08-28 从资源库创建审片版本

本轮补齐资源库视频进入正式审片流程的入口；本节取代第 24 节“审片文件仍缺少正式创建入口”的旧边界：

- 审片文件新增不可变 `assetId` 引用和创建契约，使用资源库已有对象，不重复上传或复制对象地址。
- 新增 `POST /v1/projects/:projectId/review-files`；应用服务统一执行项目写权限、幂等和领域错误映射。
- 仅允许同项目、未归档、状态为 `ready`、具有真实 object key 的视频素材创建审片版本；无效来源返回 `404`，同一素材重复创建返回 `409`。
- 新版本以 revision 1 和“待审阅”状态创建，并写入 `review-file.created` 审计及幂等回执；后续直接复用现有意见、批准、撤销批准和作品集候选链路。
- 审片页“添加版本”使用居中的无圆角 Dialog，展示尚未使用的项目视频；名称与版本自动推导且可编辑，并覆盖加载、空态、错误、选择和提交中状态。

自动验证：

- `pnpm db:migrate`：通过且确定性种子可重复执行。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过。
- `pnpm test`：6 个测试文件、33 项测试全部通过；新增覆盖创建、幂等重放、幂等键复用冲突、重复素材、无效来源和权限拒绝。

内置浏览器与真实存储验证：

- 在 MinIO 中创建真实临时 MP4，并通过“添加版本”选择该资源，创建 `review-acceptance / v13`。
- 刷新后新版本仍存在；在页面批准后再次刷新，状态仍为“已通过”。
- 作品集候选由 1 项增至 2 项，作品集添加内容 Dialog 可见 `review-acceptance · v13`，证明资源、审片和作品集链路已连通。
- Dialog 在当前视口精确居中，无页面级横向溢出；按 Escape 关闭后焦点恢复至“添加版本”。
- 本轮浏览器验证时间窗口控制台为 `0 errors / 0 warnings`。

验收清理：

- 临时素材 `ad0bfd9f-91e4-4988-b6cc-5214470456da`、审片文件 `14959949-78dc-4de2-8a5a-e703dced6654`、对象 key `teams/north/originals/f6c9b212-0c33-48c3-a09c-81e50b778413` 均已按精确标识删除。
- 相关命令回执、审计记录、临时 MP4 和目录均已删除；MinIO 对象确认不存在。
- 审片列表恢复为 5 个确定性种子版本，作品集候选恢复为原始种子状态，临时素材、审片文件、回执和审计计数均为 0。

当前边界：

- 当前入口复用资源库的单文件上传能力；Multipart、断点续传、失败会话清理、FFprobe、缩略图和转码 Worker 仍未实现。
- 批准人和批准时间仍只存在于审计记录中，尚未作为审片文件字段返回。
- 客户审片链接、实时协作传输、对外公开作品集和生产身份认证仍未实现，不能视为生产就绪。

## 26. 2026-08-29 审片真实媒体播放闭环

本轮将审片画面从固定图片和模拟计时器切换为资源库对象支持的真实视频播放：

- 新增经团队读权限校验的短期内容地址响应；复用既有 S3 预签名下载能力，不新增播放器或存储依赖。
- 审片工作台按 `reviewFile.assetId` 获取播放地址，使用原生 `<video>` 加载媒体，并由现有自定义控件驱动播放、暂停、时间线、静音和全屏。
- 播放时间、时长和分辨率来自真实媒体事件；意见时间码跳转同步设置视频位置。
- 未关联素材或对象内容不可用时显示明确状态，播放控件禁用，不再用固定图片伪装播放。

工程验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过。
- `pnpm test`：6 个测试文件、34 项测试全部通过；新增覆盖 ready 素材短期内容地址的鉴权 API。
- `git diff --check`：通过；仓库继续保持原有整体未跟踪状态，未重置或清理用户文件。

内置浏览器与真实对象存储验证：

- 上传真实 3 秒 H.264 MP4，通过资源库创建 `review-playback-acceptance / v14` 审片版本。
- 视频从 MinIO `127.0.0.1:9100` 预签名地址载入，浏览器报告 `readyState 4`、`640 × 360`、时长 3 秒。
- 播放后 `currentTime` 实际前进且暂停状态正确，静音控件同步到媒体元素；刷新后版本与媒体仍能重新载入。
- 当前页面横向溢出为 0，验收时间窗口控制台为 `0 errors / 0 warnings`。

验收清理：

- 临时素材 `934d1ba5-e80a-46b6-a9a9-6345826f1b1c`、审片版本 `d4327f05-ab91-4692-92c2-3b65b02c0965`、对象 key `teams/north/originals/9c75bdb6-b437-492c-9cad-7e901f5b6eaf` 已按精确标识删除。
- 3 条命令回执、审计 ID `36`、`37`、`38`、本地临时 MP4 和目录均已删除；对应数据库行、对象、回执和审计复核为 0。
- 审片列表恢复为 5 个确定性种子版本，内置浏览器保留在审片列表供继续检查。

当前边界：

- 仍直接播放上传原件；FFprobe、审阅代理、转码 Worker、波形、缩略图和长视频预签名续期尚未实现。
- 确定性种子中的旧审片文件大多没有真实 object key，会显示不可播放状态；新上传并创建的版本具备真实播放能力。

## 27. 2026-08-29 审片批准元数据

本轮把当前有效批准信息写入审片版本本身，审计日志继续保留历史：

- `review_files` 新增可空的批准账号与批准时间；迁移可重复执行，并使用现有账号外键。
- 批准与状态、revision 在同一事务中写入 `approvedBy / approvedAt`；撤销批准时同步清空，避免展示过期批准信息。
- 审片文件列表通过账号关联返回批准人显示名，刷新后仍可回读；审片顶部以紧凑文本显示“批准人 · 日期时间 通过”。
- 未新增审批历史表或依赖；历史动作继续由既有 `audit_logs` 记录。

工程验证：

- `pnpm db:migrate`、`pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过。
- `pnpm test`：6 个测试文件、34 项测试全部通过；批准、批准后 GET 回读、撤销和撤销后 GET 回读均覆盖批准元数据。

内置浏览器与真实 PostgreSQL 验证：

- 在 `冬夜咖啡 / v12` 批准后，页面显示“繁星 · 8/29 11:23 通过”；刷新并重新进入版本后信息仍存在。
- PostgreSQL 同步读回 `main-v12 / 已通过 / revision 2 / 繁星 / 2026-08-29 03:23:58.928+00`。
- 撤销后即时与刷新回读均为“审阅中”，批准人和批准时间为空。
- 验收审计 ID `39`、`40` 和两条精确幂等回执已删除；`main-v12` 恢复为“审阅中 / revision 1 / 2026-08-27 18:15:13.605+00”，对应审计与回执复核为 0。
- 清理后页面横向溢出为 0，浏览器控制台为 `0 errors / 0 warnings`。

有意保留的本地服务：

- Web：PID `29124`，端口 `3211`。
- API：当前监听 PID `20452`，端口 `3220`，`/healthz` 返回 `status: ok`。
- PostgreSQL：容器 `shadowproducer-postgres`，宿主端口 `5433`，状态 `healthy`。
- MinIO：容器 `shadowproducer-minio`，API `9100`、Console `9101`，状态 `healthy`。
- 以上服务按用户要求继续保留；本轮未启动额外持续进程或外部浏览器。

## 28. 2026-08-29 审片 A/B 真实版本对比

本轮完成 P0 基础版本对比，并复用现有审片与真实媒体链路：

- 同名视频版本可分别选择为主审版本 A 和对比版本 B；两路各自获取经鉴权的短期媒体地址，不使用模拟版本或固定图片伪装播放。
- 两个原生 `<video>` 共用播放、暂停、静音、时间线和全屏容器；播放时校正超过 0.15 秒的漂移。
- 时长不同时使用较长版本作为共享时间线；较短版本结束后，较长版本继续播放，运输状态和时间码保持正确，两路结束后可同步从起点重播。
- 意见、批注和批准仍只绑定主审版本 A，避免在对比视图中产生版本归属歧义。
- 对比画面从右侧进入并沿原路径退出；375px 窄屏纵向堆叠，`sm` 起并排；意见栏到 `xl` 才切到右侧，避免 1024px 下过早压缩主画面。
- 工作区根节点补充 `min-width: 0`，移动端标题隐藏重复信息，时间码保持单行；沿用现有 Button、DropdownMenu、IconButton、Lucide、Motion 与语义颜色，未新增依赖或私有控件体系。

工程验证：

- `pnpm lint`：5 个工作区包全部通过。
- `pnpm typecheck`：5 个工作区包全部通过。
- `pnpm test`：6 个测试文件、34 项测试全部通过。
- `pnpm build`：Next.js 生产构建与 API 严格类型构建通过。

内置浏览器与真实对象存储验证：

- 通过正式上传和审片版本 API 创建 `review-compare-acceptance / v21` 与 `v20`，分别关联真实 MinIO H.264 MP4。
- 两路均报告 `readyState 4`、`640 × 360`；A 时长 4 秒，B 时长 5 秒，界面分别显示真实时长与分辨率。
- 共享起播后两路漂移约 `0.002s`；同步重播漂移约 `0.0002s`。A 在 4 秒结束后 B 继续到 5 秒，控件保持播放中，时间码继续推进，B 结束后恢复暂停态。
- 共享暂停和静音同步到两路媒体；时间线保持较长版本的 5 秒范围。全屏按钮仍指向整个 A/B 画面容器，但内置浏览器未提供可靠的 DOM 全屏状态信号，因此未把全屏状态升级为独立自动断言。
- 刷新后两条版本仍从 PostgreSQL 回读，两路媒体重新达到 `readyState 4`。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 均无页面级横向溢出；1024px 修正后每路画面约为 `366x206`。
- 验收时间窗口浏览器控制台为 `0 errors / 0 warnings`。

验收清理：

- 临时素材 `46659262-3aac-4235-8ddc-e197c9be9b47`、`23c4ba0f-82ae-45e5-aecd-5e6bd56816d5`，审片版本 `c4237c42-31c4-4159-9491-c2f197a19d26`、`ea8dc2a5-9a7c-4546-ba33-815972d444d6` 已按精确标识删除。
- MinIO object key `teams/north/originals/e8b90881-00db-49b1-9feb-3f9c3b620d79`、`teams/north/originals/13231727-492e-47e8-932d-8356aaf9403a`，6 条命令回执、审计 ID `41` 至 `46`、本地 MP4 与验收目录均已删除。
- 对应数据库行、对象、回执、审计和本地目录复核为 0；浏览器刷新后恢复为 5 个确定性种子项并保留在审片列表。

当前边界：

- 当前是客户端同步双原生播放器，不包含服务端像素差分、代理媒体、波形、转码或长视频预签名续期。
- 客户免注册审片链接、生产身份认证和对外公开作品集仍涉及权限与安全架构，不在本切片中伪装完成。

## 29. 2026-08-30 P0 功能链路收口

本节取代前述“尚未实现”的历史边界；当前已完成并装配以下真实能力：

- Better Auth 会话认证、域账号映射、未登录 `401`、未 provisioned `403`；production 无条件禁用开发身份 Header，浏览器可信来源由 `BETTER_AUTH_TRUSTED_ORIGINS` 显式配置。
- 可设置密码和期限的免注册客户审片链接；链接撤销后 session 立即失效，访客评论、批准与关键动作记录审计。
- 团队作品集从已批准成片公开发布、公开页读取媒体、撤回及撤回后拒绝访问。
- S3 Multipart 分片上传、服务端上传会话、可信 SHA-256 校验和刷新后续传；大文件续传不依赖组件内存状态。
- 独立 FFmpeg Worker 轮询 PostgreSQL 任务，提取媒体元数据、生成缩略图与 H.264/AAC Fast Start 审阅代理；失败保留原始素材并记录可重试状态。
- 脚本版本写入后通过鉴权 SSE 广播，其他标签自动回读；旧 revision 返回 `409`，客户端可读取服务器最新版本。
- Agent 使用持久化 intent 执行白名单命令，统一绑定 actor、作用域、请求哈希、过期时间、幂等回执和审计。作品集公开为高风险动作，必须 `preview -> confirm -> execute`；重复执行只回放已存结果。

工程验证：

- `pnpm lint`：5 个工作区包全部通过。
- `pnpm typecheck`：5 个工作区包全部通过。
- `pnpm test`：9 个测试文件、48 项测试全部通过。
- `pnpm build`：Next.js 16.3.2 生产构建与 API 严格类型构建通过。
- `git diff --check`：通过。

真实浏览器与持久化验证：

- 内置浏览器真实退出后回到登录页；重新登录成功回到团队选择。开发身份 Header 未开启时直接请求受保护 API 返回 `401`。
- Agent 创建任务后刷新仍由 PostgreSQL 回读；高风险作品集预览不写业务数据，确认框精确居中，确认后作品集公开，公开页刷新仍可读，媒体 Range GET 返回 `206`。
- 作品集恢复基线后，公开地址返回 `404`，公开页显示“这个作品集未公开或已经撤回”。
- 团队和项目 12 个关键路由均能进入工作区且无加载错误；清理后北岸看板恢复 4 项任务。
- Web `/` 返回 `200`，API `/healthz` 返回 `{ "status": "ok" }`；PostgreSQL 与 MinIO 容器保持 healthy。

验收清理：

- Agent 验收产生的 4 个 intent、2 个命令回执、1 个任务、审计 ID `93` 至 `102` 均按精确标识删除；作品集恢复为 `团队可见 / revision 1 / public_slug NULL`。
- Multipart 与媒体 Worker 验收的 2 个素材、1 个审片版本、2 个上传会话、媒体元数据、处理任务、5 个命令回执、审计 ID `85` 至 `90` 及 4 个 MinIO 原始/派生对象均已删除。
- 清理后上述数据库行、回执、审计和对象复核均为 0；脚本协作验收临时审计已在此前清理，`winter-coffee / v7` 保持 revision 4。

当前边界：

- P0 脚本协作采用 revision + SSE 自动回读；多人同时修改同一段文本的自动合并、在线光标和更完整通知偏好属于 P1。
- Agent 当前开放 `list_tasks`、`create_task`、`publish_portfolio` 三条命令，覆盖读取、普通写入和高风险外发的统一执行链；新增业务命令继续复用同一白名单、权限、确认和审计机制，不提供任意方法调用。
- production 部署必须设置至少 32 字符的 `BETTER_AUTH_SECRET`、实际 Web origin 与对象存储凭据；本轮未执行外部部署。

有意保留的本地服务：

- Web：端口 `3211`，监听 PID `27220`。
- API：watch 根 PID `7320`，当前监听 PID `16304`，端口 `3220`。
- Media Worker：根 PID `1860`，worker PID `14476`。
- PostgreSQL：容器 `shadowproducer-postgres`，宿主端口 `5433`，状态 `healthy`。
- MinIO：容器 `shadowproducer-minio`，API `9100`、Console `9101`，状态 `healthy`。
- 以上持续进程均为本轮开始前已有并按用户要求保留；本轮未启动额外持续进程或外部 Chrome。

## 30. 2026-08-30 P1 脚本并发冲突解决

本轮进入 P1，先补齐现有 revision + SSE 协作链路中最直接的数据覆盖风险：

- 自动保存遇到 `VERSION_CONFLICT` 时保留本地草稿，不再用服务器内容覆盖编辑器。
- 冲突弹窗并列显示可编辑的本地草稿和只读的服务器最新版本；服务器读取失败提供内联错误与重试。
- 用户可以暂不处理、采用服务器版本，或编辑本地内容后基于服务器最新 revision 提交合并稿；再次发生并发写入时仍由原有乐观锁拒绝，不静默覆盖。
- 冲突关闭后工具栏保留明确入口；Escape 和关闭操作把焦点返回“处理冲突”按钮。
- 复用现有 Dialog、Textarea、Button、Lucide 与语义颜色，没有新增依赖、私有弹窗或 CRDT。

工程验证：

- 聚焦 API 测试证明旧 revision 返回 `409`、服务器正文不被覆盖，随后可基于最新 revision 提交合并稿。
- `pnpm lint`、`pnpm typecheck`：5 个工作区包全部通过。
- `pnpm test`：9 个测试文件、48 项测试全部通过。
- `pnpm build`：Next.js 16.3.2 生产构建与 API 严格类型构建通过。

内置浏览器双标签页验证：

- 两个真实标签从 revision 4 同时编辑；先到写入成为 revision 5，后到写入收到冲突并自动打开居中比较弹窗。
- 弹窗同时显示两份不同正文；关闭后本地草稿保持不变，重新打开仍可继续处理。
- 编辑并保存合并稿后 revision 变为 6，另一标签通过 SSE 自动回读相同正文。
- `375x812` 下弹窗宽 `343px`、页面 scroll width `375px`；两块比较区纵向排列，三项操作均在首屏内且高度 `44px`。
- 桌面弹窗双栏对齐；移动端和桌面控制台均为 `0 errors`。Escape 关闭后焦点两次稳定返回“处理冲突”。

验收清理与边界：

- 两轮验收产生的 9 条脚本写入回执及审计 ID `103` 至 `111` 已按精确标识删除并复核为 0。
- `winter-coffee / v7` 已恢复为原正文、revision 4、原 `updated_at`；刷新后浏览器从 PostgreSQL 读回相同基线。
- 本切片不引入 Yjs、在线光标或自动三方合并；“另存为新版本”当前没有正式业务命令，将作为独立 P1 版本语义切片实现，避免绕过版本、权限、幂等和审计契约。

## 31. 2026-08-30 P1 Agent 创建笔记命令

本轮把 `create_note` 加入 Agent 显式白名单，并复用现有工作区笔记业务链：

- 项目作用域创建项目笔记，团队作用域创建个人便签；标题与正文从明确的“创建笔记/便签”请求中解析，不提供任意方法分派或 JSON 命令编辑器。
- preview 持久化 intent 并校验团队读取范围；execute 继续由 `WorkspaceService.createNote()` 校验团队写权限与项目访问权。
- 业务写入使用 `agent-${intentId}` 作为正式 `note.create` 幂等键；重复执行回放已存结果，不重复创建笔记。
- Web Agent 显示确定的创建结果并失效 `workspace-notes` 查询；笔记页提供可直接执行的“创建便签”建议。
- 新增 `014_agent_create_note.sql`，在已执行 `013` 的数据库上增量扩展 action 约束；未修改历史迁移语义。

工程验证：

- 聚焦 Agent API 测试覆盖 write 风险、actor 绑定、错误 actor `404`、创建结果与重复执行只写一次。
- `pnpm db:migrate`、`pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过。
- `pnpm test`：9 个测试文件、49 项测试全部通过。
- `git diff --check`：通过。

内置浏览器与真实 PostgreSQL 验证：

- 在 `冬夜咖啡 / 脚本` 中输入“创建笔记：P1 Agent 验收 20260830：确认车站雨效防滑与双套防水方案。”，Agent 回显“笔记已创建”。
- 笔记页即时显示项目、标题和正文；进入详情后字段一致，整页刷新后仍能读回唯一记录。
- 浏览器控制台为 `0 errors`，API `/healthz` 与 Web `/` 均返回 `200`。

验收清理：

- 精确删除 intent `0617e4fc-95e1-45b3-b1f8-2672de8118bd`、回执 `agent-0617e4fc-95e1-45b3-b1f8-2672de8118bd`、笔记 `815e0ecf-0873-40d8-9434-fddbf9cf1ed0` 与审计 ID `112` 至 `114`。
- 数据库四类记录均复核为 0；浏览器刷新后验收标题计数为 0，确定性种子笔记未改动。
- 本切片未新增依赖、持续进程或外部浏览器；既有 Web、API、Media Worker、PostgreSQL 与 MinIO 按用户要求继续运行。

## 32. 2026-08-30 P1 脚本另存为新版本

本轮补齐脚本版本语义，并继续复用现有权限、乐观锁、SSE、PostgreSQL、审计和 UI 组件：

- 当前协作稿可另存为服务端生成的下一个 `vN`；新版本从 revision 1 开始，旧 current 原子转为只读历史且正文不变。
- 请求同时校验 `expectedVersionId + expectedRevision`；过期基线返回 `409 VERSION_CONFLICT`，对话框保留版本说明与正文，可载入最新基线后继续创建。
- `script_write_receipts` 新增可空请求哈希；新写入 receipt 均保存 SHA-256 指纹，同一幂等键换 payload 返回 `409 IDEMPOTENCY_KEY_REUSED`，不再静默回放旧正文。
- 非当前标签收到新 current 的 SSE 后，仅在仍查看旧 current 且没有本地草稿时自动跟随；主动查看历史版本不会被强制切走。
- 新版本对话框复用现有居中 Dialog、Textarea、Button 与 Lucide；提交中禁止关闭，正文或说明变化会生成新的幂等键。

工程验证：

- 聚焦 `apps/api/src/app.test.ts`：12 项通过，覆盖创建、完整 payload 回放、幂等键换 payload、viewer 拒绝、过期基线和旧版本不可变。
- `pnpm test`：9 个测试文件、50 项测试全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm db:migrate`：全部通过。
- `015_script_write_receipt_hash.sql` 已在真实 PostgreSQL 执行；新建、更新 receipt 的 `request_hash` 均非空。

内置浏览器与真实 PostgreSQL 验证：

- 从 v7 revision 4 创建 v8 revision 1；v7 立即成为只读历史，正文 MD5 保持 `c0cf77f0967a5af1e0ec478da39c2567`。
- 第二标签通过 SSE 自动切到 v8，正文和“当前 v8 · 修订 1”一致；刷新后仍从 PostgreSQL 回读相同版本。
- 保持新版本弹窗打开时，第二标签把 v8 更新到 revision 2；首次创建正确返回过期基线错误，冲突草稿未丢失，载入最新基线后成功创建 v9 revision 1，两标签同步一致。
- `375x812` 下弹窗为 `343x597`，四边均在视口内，页面 scroll width 为 `375px`；关闭后焦点回到“另存为新版本”，两个标签控制台均为 `0 errors / 0 warnings`。

验收清理与边界：

- 精确删除验收 v8/v9、3 条脚本 receipt 与审计 ID `116` 至 `118`，并复核对应计数全部为 0。
- `winter-coffee` 已恢复到 v7 current、revision 4、原 `updated_at` 与原正文 MD5；内置浏览器刷新后无 v8/v9。
- 本切片未引入多脚本文档、CRDT、自动合并或新的前端依赖；数据库 current 指针外键与多查询一致快照作为后续数据完整性加固项，不混入本次版本闭环。

## 33. 2026-08-30 P1 制片拆解原子批量确认

本轮把制片拆解的多条逐项 `PATCH + Promise.all` 改为一次正式批量命令：

- `POST /v1/projects/:projectId/breakdown/confirm` 只接受当前分类、候选 ID/revision 和幂等键；重复候选、空集合和超量请求在边界拒绝。
- 应用层统一校验项目写权限并规范化候选顺序；viewer、跨项目候选、跨分类候选、已确认候选和过期 revision 均不会产生部分写入。
- PostgreSQL 在一个事务中锁定并验证全部候选，再用单条更新统一确认；成功同时写命令回执和一条包含完整 item/revision 列表的批次审计。
- 同一幂等键与相同请求返回已存结果；同键换 payload 返回 `409 IDEMPOTENCY_KEY_REUSED`。
- Web 不再循环调用单项接口；失败保留当前分类选择和幂等键，可直接重试。切换分类会清空隐藏选择，并提供当前分类“全选待确认”。
- 继续复用现有 Button、Dialog、StatusBadge、原生 checkbox、Lucide 和零圆角语义 token，没有新增依赖或通用批处理框架。

工程验证：

- 聚焦拆解 API：11 项通过，覆盖成功、幂等回放、同键换 payload、整批冲突回滚、重复项、跨分类、跨项目和 viewer 拒绝。
- `pnpm test`：9 个测试文件、53 项测试全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过。
- `git diff --check`：通过。

内置浏览器与真实 PostgreSQL 验证：

- 选择同一分类两条候选后，确认弹窗明确显示“冬夜咖啡 · 演员与角色 · 2 条候选”。
- 弹窗保持打开时把其中一条 revision 从 1 改为 2；提交返回 `409`，另一条仍为 `待确认 / revision 1`，数据库没有命令回执或审计，证明整批回滚。
- 恢复 revision 后直接重试成功，两条同时成为 `已确认 / revision 2`；数据库写入唯一批量回执与审计 ID `119`，页面刷新后仍从 PostgreSQL 回读已确认状态。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 均无页面级横向溢出；三条候选行高度稳定为 `80px`，窄屏表格只在受控数据区横向滚动。
- `375x812` 下确认弹窗为 `343x352`，中心偏差为 0、四边均在视口内；取消后焦点返回“确认所选”，分类切换后隐藏选择清空。
- 验收期间内置浏览器控制台为 `0 errors / 0 warnings`。

验收清理与运行态：

- 临时候选 `p1-breakdown-batch-e2e-a/b`、批量命令回执、审计 ID `119` 已按精确标识删除并复核为 0。
- 正式 `br1` 至 `br6` 均恢复原状态；审计恢复为 `max=84 / count=46`，浏览器刷新后临时候选计数为 0。
- Web `3211`、API `3220`、Media Worker、PostgreSQL `5433` 与 MinIO `9100/9101` 均继续运行；本轮未启动额外持续进程或外部浏览器。
- 本切片不扩展需求类型、数量、准备要求和结构化来源字段；这些专业数据模型作为后续独立 P1 切片，避免与原子确认混写。

## 34. 2026-08-30 P1 专业制片拆解字段与候选修改

本轮把拆解候选从展示性文本升级为可持续维护的专业制片数据：

- `breakdown_items` 新增需求类型、数量、准备要求、Agent 判断、来源文档、来源版本和原文位置；旧 `source` 字段继续保留兼容。
- 状态扩展为 `待确认、待安排、待采购或租赁、已联系、已确认、已完成、不需要、已取消` 八态。
- 单项 `PATCH` 支持候选内容、需求类型、规格、数量、准备要求、负责部门和状态，并统一 trim、拒绝空更新、空白字段与过期 revision。
- 普通编辑不能直接写入 `已确认`；正式确认仍必须经过第 33 节的原子批量确认命令。来源文档、版本、位置与 Agent 判断保持只读追溯。
- 表格按“候选/类型、规格/数量、准备要求、部门、来源、Agent 判断、状态、编辑”组织；所有行固定为 `96px`，状态与编辑列固定在数据区右侧。
- 编辑使用现有零圆角 Dialog、Input、Textarea、DropdownMenu、Button、StatusBadge 和 Lucide，没有新增依赖或页面私有控件体系。

工程验证：

- `pnpm db:migrate`：`016_breakdown_professional_fields.sql` 在真实 PostgreSQL 执行成功，八态 CHECK 生效，确定性种子回填结构化专业字段。
- `pnpm test`：9 个测试文件、55 项测试全部通过；拆解 API 13 项覆盖结构化字段、trim、空更新、空白字段、viewer、过期 revision、直接确认拒绝和八态工作流。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 固定 ID 临时候选通过编辑弹窗一次保存全部专业字段；服务器去除首尾空白，revision 从 1 增至 2，刷新后仍从 PostgreSQL 回读相同内容。
- 弹窗保持打开时由数据库把 revision 提升至 3；提交旧 revision 返回 `409`，数量草稿 `6 人` 保留，未产生第二条审计。
- 关闭冲突弹窗后焦点返回原“编辑”按钮；来源核对明确拆分文档、版本、位置、原文摘录、Agent 判断和置信度。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 页面 scroll width 均等于 viewport；专业表格只在自身数据区横向滚动。
- `375x812` 下编辑弹窗左右边距均为 `16px`，内容高度超过可用区域时由弹窗自身滚动；页面无横向溢出。
- 内置浏览器最终控制台为 `0 errors / 0 warnings`。

验收清理与运行态：

- 临时候选 `p1-breakdown-structured-e2e-20260830` 与审计 ID `120` 已按精确标识删除并复核为 0。
- 审计恢复为 `max=84 / count=46`，拆解命令回执为 0；`br1` 保持 `待确认 / revision 3`，`br2-br6` 保持 `待确认 / revision 1`。
- Web `3211`（PID `27220`）、API `3220`（根 PID `7320`、监听 PID `21784`）、Media Worker（PID `18192/14476`）、PostgreSQL `5433` 与 MinIO `9100/9101` 按用户要求继续运行。
- 本轮未启动额外持续进程或外部浏览器；合并与拆分涉及实体重写和来源继承，留作下一独立 P1 切片。

## 35. 2026-08-30 P1 制片拆解候选合并与拆分

本轮完成专业拆解候选的实体改写闭环：

- 新增 `017_breakdown_lineage.sql`，通过 `parent_item_id` 与 `merged_into_item_id` 保留拆分父子关系和合并去向；迁移重复执行成功。
- 合并要求至少两条同项目、同分类、仍待确认的候选，并显式选择不可变来源基准；事务创建一个新候选，将源候选标记为已取消并统一指向新结果。
- 拆分要求一条仍待确认的候选和至少两个子候选；事务创建子候选并继承来源、原文、Agent 判断与置信度，将父候选标记为已取消。
- 两个命令均锁定源记录、校验全部 expected revision、整笔回滚，并使用正式命令回执实现相同请求重放和同键换 payload 拒绝。
- 审计分别使用 `breakdown.merged` 与 `breakdown.split`，记录源项、结果项和 revision；来源与 Agent 字段继续保持只读。
- Web 复用现有 Button、Dialog、Input、Textarea、DropdownMenu 与 Lucide；合并来源基准显式可选，拆分支持 2 至 20 个稳定草稿项，冲突后可载入最新 revision 且不丢草稿。

工程验证：

- 聚焦拆解 API：16 项通过，覆盖合并、拆分、trim、lineage、来源继承、幂等重放、同键换 payload、过期 revision 整笔不变、跨分类和 viewer 拒绝。
- `pnpm test`：9 个测试文件、58 项测试全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 连续执行两次成功，证明 `017` 迁移幂等。

内置浏览器与真实 PostgreSQL 验证：

- 两条候选合并后刷新仍显示新结果和两条“已并入”关系；数据库确认新结果继承所选基准来源、`confidence 93`、`待确认 / revision 1`，两条源候选均为 `已取消 / revision 2`。
- 拆分弹窗编辑草稿后将父项 revision 从 1 提升到 2；首次提交返回 `409`，草稿保持不变，“载入最新版本”后错误清除且草稿仍在，第二次提交成功。
- 数据库确认拆分父项为 `已取消 / revision 3`，两个子项为 `待确认 / revision 1`，均保留 `parent_item_id`、来源文档和 Agent 判断。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 均无页面级横向溢出，三项操作按钮在视口内；窄屏表格继续只在受控数据区滚动。
- `375x812` 下拆分弹窗为 `343x780`，四边均在视口内、内容纵向滚动且无横向溢出；取消后焦点返回“拆分”按钮。
- 内置浏览器最终控制台为 `0 errors / 0 warnings`。未启用开发身份头的直接 API 请求返回 `401`，真实验收改用内置浏览器现有登录会话，没有放宽认证配置。

验收清理与运行态：

- 精确删除 6 条验收候选/结果、2 条命令回执及审计 ID `121/122`；临时候选、回执和对应审计均复核为 0。
- 正式 `br1` 保持 `待确认 / revision 3`，`br2-br6` 保持 `待确认 / revision 1`，全部 lineage 为空。
- Web `3211`、API `3220`、Media Worker、PostgreSQL `5433` 与 MinIO `9100/9101` 按用户要求继续运行；本轮未启动额外持续进程或外部浏览器。
- 本切片没有新增依赖、关系操作历史表、自动文本合并或撤销工作流；现有 lineage 与审计已覆盖当前可追溯要求。

## 36. 2026-08-30 P1 真实执行排程与资源冲突检测

本轮把执行计划从静态展示改为可持久化、可计算的项目排程：

- 新增执行阶段与资源分配表；时间以 ISO instant 存储，并保留原始 IANA 时区。
- 提供阶段查询、幂等创建和 revision 更新 API；项目成员权限、审计与既有错误契约继续复用。
- 人员、演员、场地和设备按规范化资源标识检测重叠；结束时间等于下一项开始时间不冲突。
- 冲突检测覆盖同团队跨项目排程，当前用户无权读取的项目只返回遮蔽摘要。
- 执行计划 UI 支持加载、错误、空态、重试、增改、显示时区切换、资源输入和冲突定位；未新增时间库或自动排产优化器。

工程验证：

- `pnpm test`：9 个测试文件、60 项测试全部通过，覆盖权限、幂等、revision、时区、半开区间和跨项目遮蔽。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过。
- `pnpm db:migrate` 连续执行两次成功；真实 PostgreSQL 回读 4 个确定性阶段和 6 条资源分配。

内置浏览器与真实 PostgreSQL 验证：

- 页面从数据库显示 `前期准备、拍摄执行、补拍与收尾、后期交付` 4 个阶段和 0 项冲突。
- 创建两条 9 月 10 日时间重叠且演员资源相同的临时阶段后，页面返回唯一 1 项资源冲突，并明确列出资源和两个阶段。
- 第二条阶段 revision 从 1 更新到 2；备注与进度刷新后仍从 PostgreSQL 回读。
- 显示时区从 `Asia/Shanghai` 切换到 `UTC` 后，09:00-12:00 正确显示为 01:00-04:00，10:00-13:00 正确显示为 02:00-05:00。
- 新增和编辑弹窗关闭后，焦点均返回“新增阶段”；控制台为 `0 errors / 0 warnings`。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 均无页面级横向溢出，标题、主工作区和执行计划保持可见。

验收清理与运行态：

- 精确删除临时阶段 `4258dfa0-59ce-4dac-818a-05d90fd2cdba`、`d8f156de-847f-4292-ac96-4dac83ee4fc7`、2 条资源分配、2 条命令回执及审计 ID `123-125`，并复核对应计数全部为 0。
- 确定性 `exec-*` 阶段保持 4 条；浏览器刷新后恢复 4 个阶段、0 项冲突和 `Asia/Shanghai` 显示。
- Web `3211`、API `3220`、Media Worker、PostgreSQL `5433` 与 MinIO `9100/9101` 按用户要求继续运行；本轮未启动额外持续进程或外部浏览器。
- 内置浏览器当前不暴露可验证的页面缩放能力，200% zoom 未作为通过项记录；其余 Kasidia 视口矩阵已完成。

## 37. 2026-08-30 P1 专业通告单正式发布与变更历史

本轮把通告表的普通状态更新升级为可追溯的正式发布闭环：

- 新增 `019_call_sheet_publications.sql`，正式版本保存不可变完整快照、发布序号、发布人和发布时间；已有“已发布”种子通告自动补为历史 v1。
- 新增独立 `POST /v1/projects/:projectId/call-sheets/:itemId/publish` 命令；发布要求项目写权限、expected revision、幂等键和用户确认，普通 `PATCH` 不再接受“已发布”状态。
- 已发布通告再次编辑时自动回到“待确认”，同时保存 before/after 快照、基准发布版本、变更摘要、操作人和时间；历史发布快照不更新。
- 新增历史读取 API 和“发布记录”界面；正式发布使用确认 Dialog，发布后编辑必须填写变更摘要，查询和命令错误均有局部反馈/重试入口。
- 继续复用现有命令回执、项目权限、审计、Button、Dialog、StatusBadge、Lucide 和零圆角设计，没有新增依赖或另造版本框架。

工程验证：

- `pnpm db:migrate`：`019` 在真实 PostgreSQL 执行成功，现有 `sd-05` 回填为 v1，标题为“车站夜戏”。
- `pnpm test`：9 个测试文件、61 项测试全部通过；新增覆盖直接 PATCH 发布拒绝、viewer 拒绝、幂等重放、过期 revision、发布后待确认、v1/v2 快照和变更历史。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 既有“车站夜戏”的发布记录显示 v1、历史迁移、原始 revision 和发布时间。
- 创建临时草稿后，把当前真实成员角色从 `editor` 临时降为 `viewer`；确认发布返回明确 403“无权访问当前项目”，数据库未写发布结果。角色精确恢复为 `editor` 后，同一确认成功。
- 页面刷新后临时通告仍为“已发布”；编辑标题和地点并填写“拍摄地点调整”后，状态自动变为“待确认”，历史显示基于 v1 的 before/after 标题。
- 再次确认发布后，界面和数据库均显示 v2、v1 两个正式快照；v1 保持原标题/revision 2，v2 保存修订标题/revision 4，证明历史没有被静默覆盖。

验收清理与运行态：

- 精确删除临时通告 `aa380ebd-045f-446d-84b7-a07416aae4d6`、其 2 个发布快照、1 条变更、3 条命令回执及审计 ID `126-129`；对应计数全部为 0，成员角色复核为 `editor`。
- 浏览器刷新后临时通告消失，原 `sd-05/sd-06/sd-07` 与回填的 `sd-05` v1 保持可用。
- Web `3211`（PID `27220`）、API `3220`（监听 PID `2712`）、Media Worker（`34972 → 18192 → 14476`）、PostgreSQL `5433` 与 MinIO `9100/9101` 按用户要求继续运行；本轮未启动额外持续进程或外部浏览器。
- 重要变更通知的收件人、发送与确认状态仍是后续独立切片；本轮只完成正式发布不可变性与变更历史，不把通知系统混入当前事务。

## 38. 2026-08-30 P1 Agent 通告单创建与正式发布

本轮把通告单接入既有 Agent `preview -> confirm -> execute` 生命周期：

- 新增 `create_call_sheet` 与 `publish_call_sheet` 两个正式 Agent action；创建草稿为普通写操作，正式发布为必须确认的高风险操作。
- Agent 直接复用 `ProductionService.createCallSheet/publishCallSheet`，继续沿用项目权限、expected revision、幂等回执、不可变发布快照和审计事务，没有复制业务写入逻辑。
- 发布预览按当前项目读取待发布通告，要求唯一标题或 ID，并把目标 revision 冻结在 intent；找不到或存在歧义时返回明确选择错误。
- 预览额外校验团队与项目作用域关系，避免请求体中的团队范围与实际项目不一致。
- Web Agent 支持自然语言创建指定日期通告和正式发布指定通告；执行后失效通告列表，发布后同时失效对应发布历史。
- 新增 `020_agent_call_sheet_commands.sql` 扩展数据库 action CHECK；没有新增 endpoint、通知系统、依赖或并行命令框架。

工程验证：

- `pnpm db:migrate`：`020` 在真实 PostgreSQL 执行成功。
- `pnpm test`：9 个测试文件、65 项测试全部通过；Agent API 新增覆盖创建与结果回放、发布确认、团队/项目越界拒绝及缺失目标错误。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- Agent 创建 `9 月 3 日 / P1 Agent 验收 1830` 后，通告列表立即出现草稿；页面刷新后仍从 PostgreSQL 回读。
- 首次发起正式发布后选择取消，通告仍保持 `草稿 / revision 1`，没有 publication 或发布回执。
- 再次发起后确认执行，Agent 返回“已正式发布为 v1”，列表状态变为“已发布”，发布记录显示 v1、revision 2、发布人和时间。
- 请求发布不存在的标题时，Agent 面板显示“未找到匹配的待发布通告表”，没有出现确认弹窗，也没有创建 intent。
- 最终页面控制台为 `0 errors / 0 warnings`。

验收清理与运行态：

- 精确删除临时通告 `7bbbc9e2-4bdc-4687-b90f-11d959562620`、发布快照 `debb98b9-ee9e-40ba-b48c-2b1769615edd`、3 条 Agent intent、2 条命令回执及审计 ID `130-137`。
- 对应通告、publication、intent、receipt、audit 计数全部复核为 0；`sd-05/sd-06/sd-07` 三条种子通告保持完整，浏览器刷新后临时记录消失。
- Web `3211`（PID `27220`）、API `3220`（监听 PID `988`）、Media Worker（`34972 -> 18192 -> 14476`）、PostgreSQL `5433` 与 MinIO `9100/9101` 继续运行；本轮未启动外部浏览器或额外持续进程。
- 本切片没有加入通告通知、外发或收件确认；正式发布命令和版本历史已经提供这些能力后续接入所需的稳定业务边界。

## 39. 2026-08-30 P1 多人脚本并发与评论线程

本轮把脚本协作从单客户端评论升级为可并发验证的线程闭环：

- 新增 `021_script_comment_threads.sql`，评论获得一级回复关系、请求哈希、revision 与更新时间；旧评论按兼容默认值继续可读。
- 根评论支持回复、解决和恢复；回复限制为一级，状态更新使用 expected revision，缺失、越权、过期和幂等键换 payload 均返回明确错误。
- 评论创建改为数据库唯一约束下的并发去重；相同请求并发重放，不同内容复用同一键返回 `409`，不再把唯一键竞态泄漏为 `500`。
- 评论状态复用既有 `command_receipts`，脚本版本写入在等待数据库锁后重新读取回执；相同并发请求稳定获得一次真实写入和一次 replay。
- SSE 统一广播版本与评论轻量事件，并增加 25 秒 heartbeat；前端收到事件后回拉 workspace，不复制评论领域模型到事件 payload。
- 右侧评论区显示作者、时间、回复和可恢复的已解决线程，并使用原生纵向滚动；复用现有 Button、Dialog、Textarea、Lucide 与零圆角样式，没有新增依赖。

工程验证：

- `pnpm db:migrate`：`021` 在真实 PostgreSQL 执行成功，确定性评论完整回读。
- `pnpm test`：9 个测试文件、66 项测试全部通过；API 新增覆盖评论哈希、空白拒绝、回复、解决/恢复、revision 冲突和评论 SSE。
- `pnpm --filter @shadowproducer/api test:postgres`：6 项真实 PostgreSQL 测试通过，覆盖并发 PATCH、并发新版本、相同键回放、换 payload 拒绝、viewer 和评论线程并发。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 创建临时根评论与两条回复后，作者、时间、层级和未解决计数立即正确；解决后线程保留并出现“恢复”，恢复后计数返回。
- 第二个内置浏览器标签无需刷新即可看到新回复；根评论再次解决后，第二标签同步出现“恢复”，证明评论创建与状态事件均生效。
- `1329x912` 下页面无横向溢出，评论栏 computed `overflow-y: auto`，2 个评论操作区均未越出面板，评论卡片圆角均为 `0px`。
- API 健康检查返回 `{"status":"ok"}`，刷新后临时线程消失、2 条确定性评论保持完整。

验收清理与运行态：

- 精确删除临时根评论 `b4888306-8693-46fa-bdec-fea9f353bd5d`、回复 `00a962af-d244-4035-9d2b-cb3d4f48b651` 与 `95655543-6e1c-4e91-9022-e12f16e7f0cf`、3 条状态回执和 6 条审计；对应计数全部复核为 0。
- PostgreSQL 集成测试临时 team/account/project 计数均复核为 0；第二个验收标签页已关闭。
- Web `3211`（PID `27220`）、API `3220`（监听 PID `36784`）、Media Worker、PostgreSQL `5433` 与 MinIO `9100/9101` 继续运行；本轮没有启动外部浏览器或额外持续服务。
- 多 API 实例之间的广播、在线光标 presence、三方文本 diff 和 CRDT 未混入当前切片；单实例评论线程已经达到 P1 基本可用，扩展能力按实测协作规模再引入。

## 40. 2026-08-30 P1 脚本一致性快照与三方冲突定位

本轮消除脚本工作区混合数据库快照与冲突整段覆盖风险：

- PostgreSQL workspace 的文档指针、版本、评论和协作者改为在一个 `REPEATABLE READ` 事务内顺序读取，避免并发切版时 `currentVersionId` 与唯一当前版本来自不同提交时点。
- 自动保存请求保留实际编辑基线；冲突时以基线、本地草稿和服务器最新修订生成三方行级比较。
- 不重叠修改自动合并，同一位置的不同修改只标记对应行并要求用户选择；相邻但不重叠的修改不会被吞入冲突块。
- 合并稿仍通过现有 expected revision、项目写权限、幂等回执、审计和 SSE 写入，没有新增合并 API、依赖或另一套权限逻辑。
- 冲突弹窗复用现有 Dialog、Button、Textarea、StatusBadge 与 Lucide；关闭后焦点返回仍存在的冲突触发器，否则返回脚本正文。
- 行级 LCS 设置计算上限；超出时安全退化为较大的人工确认区段，不静默覆盖。真实规模证明需要更细粒度时再引入专业 diff 引擎。

工程验证：

- `pnpm test`：API 9 个测试文件、66 项测试和 Web 原生 5 项三方合并测试全部通过。
- `pnpm --filter @shadowproducer/api test:postgres`：7 项真实 PostgreSQL 测试通过；新增 6 轮切版与 144 次并发 workspace 读取，所有快照均保持唯一当前版本与文档指针一致。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 两个标签从同一 `v7 / revision 5` 基线并发编辑；服务器相邻修改与本地独立修改自动保留，只有同一行显示为待确认。
- 选择服务器冲突段后，合并稿同时保留服务器冲突内容、服务器相邻修改和本地独立修改；保存后两个标签无需刷新同步到 `revision 7`。
- 关闭冲突弹窗后焦点返回“脚本正文”；`1329x912` 下弹窗位于视口内、页面和弹窗横向溢出均为 0、内部纵向滚动、按钮为 44px 高、圆角为 `0px`。
- 内置浏览器 viewport override 未实际改变 `1329x912` 页面尺寸，因此未把移动端矩阵记录为通过；当前控制台为 `0 errors / 0 warnings`。

验收清理与运行态：

- 精确删除本轮 5 条脚本写入回执及审计 ID `172-176`；`winter-coffee / v7` 的正文、revision 和更新时间恢复为原始 `revision 4 / 2026-08-27 16:04:37.050Z`，回执恢复为原 3 条。
- PostgreSQL 集成测试临时 account/team/project 计数均为 0；第二个内置浏览器标签已关闭，最终两个标签回读原始正文和 `revision 4`。
- Web `3211`（PID `27220`）和 API `3220`（监听 PID `33864`）继续运行，API `/healthz` 返回 `{"status":"ok"}`，Web 返回 `200`；PostgreSQL 与 MinIO 保持 healthy。
- 本轮没有启动外部浏览器或额外持续服务；未引入 CRDT、实时光标或多实例事件总线。

## 41. 2026-08-30 P1 Agent 日程创建与时间调整

本轮把个人日历接入 Agent 的正式创建与更新时间命令：

- 新增 `create_calendar_event` 与 `update_calendar_event` Agent action；两者复用现有 `WorkspaceService`、团队/项目权限、乐观锁、命令 intent 和审计，不复制日历写入逻辑。
- 更新预览按当前团队、可选项目和标题或 ID 选择唯一日程；不存在或多条匹配时返回 `AGENT_CALENDAR_EVENT_SELECTION_REQUIRED`，不创建含糊 intent。
- 预览冻结目标 item ID 与 expected revision；执行时只移动开始时间并保留原持续时长，权限或 revision 已变化时由现有业务服务拒绝。
- Web Agent 支持中文日期、相对日期、时间和全天日程解析；未识别请求不再静默退化为任务查询。
- Agent 创建或更新时间后统一失效当前团队的日历查询，并返回对应成功文案；没有新增依赖或第二套日历状态。

工程验证：

- `pnpm db:migrate`：`022` 在真实 PostgreSQL 执行成功，数据库 action CHECK 明确包含 `create_calendar_event` 与 `update_calendar_event`。
- `pnpm test`：API 9 个测试文件、69 项测试和 Web 9 项原生测试全部通过；新增覆盖中文更新解析、唯一选择、持续时长、revision 增长及 execute replay 不重复更新。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- UI 创建唯一项目日程后，Agent 接受“把日程改到 9 月 1 日下午 4 点”，面板返回明确成功结果，日历缓存立即刷新。
- 页面完整刷新后仍能读回该日程；PostgreSQL 显示 `2026-09-01 08:00:00+00 / Asia/Shanghai`，即本地 16:00，revision 从 1 增至 2。
- intent 状态为 `consumed`，结果类型为 `calendar_event_updated`；同时存在 `calendar-event.created`、`agent.previewed`、`calendar-event.updated`、`agent.executed` 四条审计。

验收清理与运行态：

- 精确删除临时日程 `d3dfce6f-55ba-4086-957e-e41b9f6c7b4b`、Agent intent `f4e1dcec-8951-41cb-9044-675601f3bc55`、1 条创建回执与 4 条审计；event、intent、receipt、audit 计数全部复核为 0。
- 浏览器刷新后临时日程计数为 0；确定性日历数据保持完整。
- Web `3211`（PID `27220`）、API `3220`（监听 PID `32196`）、PostgreSQL `5433` 与 MinIO `9100/9101` 按用户要求继续运行；本轮没有启动外部浏览器或额外持续进程。
- 本切片只支持移动日程并保留原持续时长；标题、可见性、结束时间和批量调整继续由现有日历编辑能力处理，待真实 Agent 用例证明需要再扩展命令字段。

## 42. 2026-08-30 P1 团队联系人 CSV 批量导入导出

本轮把团队资源库联系人从单条维护扩展为可追踪的批量 CSV 闭环：

- CSV 支持 UTF-8 BOM、中英文表头别名、CRLF、逗号、成对引号和双引号转义；拒绝未知或重复表头、列数错位、空姓名、超长字段、完全重复行、空文件和超过 500 条的批次。
- 浏览器只提交校验后的结构化联系人；API 再次执行整批校验，并在一个 PostgreSQL 事务内写入全部联系人、一个批次幂等回执和每位联系人的审计记录，任一失败不会留下半批数据。
- 导入复用团队写权限；viewer 返回 `403`。相同批次键与相同载荷稳定 replay，不同载荷复用同一键返回 `409`。
- 导出使用原生 CSV 和浏览器下载，不增加依赖；只包含 `source=team` 的团队自有联系人，成员私人共享联系人不会进入文件。
- 资源库复用现有 Button、Dialog、DataTableViewport 与 Lucide；导入、导出保持次级样式，新建团队联系人仍是唯一主操作。

工程验证：

- `pnpm test`：API 9 个测试文件、71 项测试和 Web 12 项原生测试全部通过；新增覆盖 CSV 边界、团队导入 replay、viewer 拒绝、空姓名和重复行整批拒绝。
- `pnpm --filter @shadowproducer/api test:postgres`：2 个真实 PostgreSQL 测试文件、9 项测试全部通过；联系人批次新增覆盖单回执、逐条审计、重放不重复和同键异载荷冲突。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 通过内置浏览器选择一条含逗号字段的 CSV，居中预览正确显示 5 个字段；确认后立即出现团队联系人和明确成功反馈，重新打开资源库后仍从 PostgreSQL 回读。
- 导出产生 UTF-8 CSV，包含 3 条团队自有联系人和正确的逗号引号转义；文件不包含 2 条成员共享联系人。
- PostgreSQL 同时读回验收联系人、批次回执和 `team-contact.created` 审计；审计 metadata 明确包含 `source=csv`、批次 ID 和行号。
- `375x812` 下弹窗完整位于视口内，页面横向溢出为 0；`768x1024` 下只有联系人表格 viewport 内部横向滚动；`1024x768`、`1440x1000`、`1920x1080` 下操作按钮均在视口内且页面无横向溢出。最终控制台为 `0 errors / 0 warnings`。

验收清理与运行态：

- 精确删除临时联系人 `4293a123-36ab-43bb-826a-c8c395b2724f`、批次回执 `536974da-635d-4d2b-a435-798d83549e49`、审计 `211` 和本轮 3 个临时 CSV；contact、receipt、audit、project link 计数全部复核为 0，浏览器硬刷新后临时联系人计数为 0。
- Web `3211` 与 API `3220`、PostgreSQL 和 MinIO 继续运行；本轮没有启动外部浏览器或额外持续服务。
- 本切片没有扩展个人联系人、vCard 或独立供应商实体；这些能力在对应 P1 业务模型冻结后继续推进。

## 43. 2026-08-30 P1 团队供应商实体、CSV 与制片拆解关联

本轮把资源库中的供应商从联系人文本扩展为独立、可审计的制片资源：

- 新增团队供应商、供应商项目、供应商联系人和拆解项供应商关联表；供应商支持 revision 并发保护、软删除，拆解关联使用独立 junction，不把多供应商关系压成单值字段。
- 团队供应商 CRUD、CSV 批量导入和导出复用团队读写权限、幂等回执、请求哈希与审计；viewer 只能读取，跨团队联系人或不可访问项目会被拒绝。
- CSV 支持 UTF-8 BOM、中英文表头别名、CRLF、逗号和引号转义；拒绝未知或重复表头、空名称、超长字段、重复行、空文件和超过 500 条的批次。
- 资源库新增“供应商”Tab，提供搜索、创建、编辑、删除确认、联系人/项目多选、CSV 导入预览和导出，并覆盖 loading、error 与 empty 状态。
- 制片拆解表格新增供应商列；只有“已确认”的拆解项可以关联项目可用供应商，未确认项和不可用供应商由服务端业务错误明确拒绝。

工程验证：

- `pnpm db:migrate` 连续执行两次成功，供应商迁移和 deterministic seed 保持幂等。
- `pnpm test`：Web 15 项原生测试、API 9 个测试文件 75 项测试全部通过；包含供应商 CSV、权限、导入 replay、revision 冲突、拆解确认前置条件和供应商可用性。
- `pnpm --filter @shadowproducer/api test:postgres`：2 个真实 PostgreSQL 测试文件、12 项测试全部通过；覆盖供应商批量回执、逐条审计、关联持久化、同键异载荷拒绝和 revision 保护。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 通过内置浏览器创建供应商并关联“远景雨效”和“冬夜咖啡”，刷新后回读；随后编辑服务范围并增加“城市慢行”，再次刷新后回读。
- CSV 导入预览和确认成功，刷新后仍从 PostgreSQL 回读；导出按钮执行时无控制台错误，CSV 内容与可重新导入性由原生单元测试覆盖。内置浏览器未暴露 Blob 下载事件，因此没有把下载文件落盘计为浏览器强证据。
- 拆解项“顾遥”从 `待确认 / revision 3` 确认为 `已确认 / revision 4`，随后关联验收供应商并刷新回读为 `revision 5`；未确认和不可用供应商失败路径由 API 测试覆盖。
- `375x812` 下页面无横向溢出、供应商编辑弹窗完整位于视口内；`768x1024` 下页面无横向溢出，只有供应商表格 viewport 内部横向滚动；控制台为 `0 errors / 0 warnings`。

验收清理与运行态：

- 精确删除临时供应商 `ec768ccd-efa1-4459-8ab9-f5a2f2f8a002`、`56237dcf-e3f4-4be0-bdee-fd248a49b561`，3 条关联、3 条回执、审计 `269-273` 和临时 CSV；supplier、link、receipt、audit 计数全部复核为 0。
- `br1` 精确恢复为 `待确认 / revision 3 / 2026-08-27 18:25:55.251+00`；浏览器刷新后资源库仅保留 2 条种子供应商，“顾遥”恢复为未关联待确认。
- Web `3211`（PID `27220`）、API `3220`（PID `7976`）、PostgreSQL `5433` 与 MinIO `9100/9101` 继续运行；数据库与对象存储均为 healthy。本轮没有启动外部浏览器或额外持续服务。
- 当前切片只完成供应商目录、关系和拆解准备闭环；询价、合同、付款与采购审批未混入本轮，等真实制片流程需求冻结后再扩展。

## 44. 2026-08-30 P1 高级脚本版本影响分析

本轮为脚本工作区增加可追溯的只读版本影响分析：

- 新增脚本版本影响契约与 `GET /v1/projects/:projectId/script-version-impact`；查询复用项目读权限，不写入分析结果或业务数据。
- 应用层按行执行 LCS 对比并输出变更区段；计算量超过 `2,000,000` cells 时返回一个宽范围和 `processingStatus=limited`，避免大脚本请求阻塞服务。
- 只把 `sourceVersion` 精确等于基线版本、且状态不是 `待确认` 或 `已取消` 的拆解项标为可能受影响，不对分镜、排期和通告做模糊推断。
- 脚本工具栏新增版本影响分析弹窗，包含双版本选择、loading、error、empty、变化片段、正式拆解项和覆盖限制；选择版本后查询结果自动刷新。
- 修正共享 Dialog 的焦点记录边界；影响分析关闭后明确把焦点还给触发按钮。弹窗使用动态视口高度约束，保持零圆角与既有 Radix、Button、Dropdown、Lucide 组件体系。

工程验证：

- `pnpm test`：Web 15 项原生测试、API 9 个测试文件 76 项测试全部通过；影响分析新增覆盖成功、viewer 可读、越权、缺失版本、同版本拒绝、正式拆解项筛选和覆盖限制。
- `pnpm --filter @shadowproducer/api test:postgres`：2 个真实 PostgreSQL 测试文件、12 项测试全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器验收：

- 初始 `v6 -> v7` 显示 3 个变化区段；把基线改为 `v5` 后自动刷新为 `v5 -> v7` 和 2 个变化区段。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 五档视口下，弹窗均完整位于视口内，页面和弹窗均无水平溢出；需要时只在弹窗内部纵向滚动。
- 打开后焦点进入基线版本选择器；关闭动画完成后焦点返回 `版本影响分析` 按钮。控制台为 `0 errors / 0 warnings`。
- 当前真实数据没有符合规则的正式拆解项，界面正确显示空态与三条覆盖限制，没有伪造下游影响。

验收清理与运行态：

- 本切片和浏览器验收均为只读，没有创建临时数据库记录、对象存储文件或本地验收制品，无需数据清理。
- Web `3211`（PID `27220`）、API `3220`（PID `13724`）、PostgreSQL `5433` 与 MinIO `9100/9101` 继续运行；API `/healthz` 返回 `ok`，Web 返回 `200`，数据库与对象存储均为 healthy。
- 本轮没有启动外部浏览器或额外持续服务；内置浏览器临时标签已关闭。
- 当前尚无脚本场次、分镜镜头、排期和通告之间的稳定结构化关联键，因此本切片不声称能判定这些下游对象的影响；建立权威关联模型后再扩展分析范围。

## 45. 2026-08-30 P1 团队审计查询与可见范围

本轮把既有 `audit_logs` 接成团队成员可用的只读审计查询闭环：

- 新增团队审计列表契约与 `GET /v1/teams/:teamId/audit-logs`；复用现有审计表、团队/项目权限和 Web API client，没有迁移、依赖或第二套日志模型。
- 团队成员可以查看团队级记录；项目级记录只返回当前成员已加入的项目。显式项目筛选继续执行项目读权限，不能借筛选参数越权读取。
- 查询支持项目、动作、操作者、开始日期、结束日期和稳定分页；页大小上限为 100，排序固定为 `created_at DESC, id DESC`。
- PostgreSQL 查询直接补齐操作者和项目名称，避免浏览器逐行追加请求；日期参数在应用边界校验，非法范围返回明确错误。
- 团队侧边栏新增“审计记录”，页面复用现有 Button、Dropdown、Table 与原生日期输入；页面主体负责纵向滚动，表格只负责受控横向滚动。
- 删除 `WorkspaceShell` 共享的 `min-h-[640px]`，避免短视口和 200% 缩放时工作区超出浏览器底部；应用外框仍由 `h-svh` 固定在可用视口内。

工程验证：

- `pnpm test`：API 9 个测试文件、77 项测试和 Web 15 项原生测试全部通过；新增覆盖团队审计查询、筛选、越权项目拒绝和分页。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件、15 项测试全部通过；审计查询新增覆盖团队/已加入项目可见性、组合筛选和稳定分页。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 北岸影像审计页从当前 PostgreSQL 回读 45 条可见记录；动作、操作者和日期筛选均通过，冬夜咖啡项目筛选返回 35 条记录。
- 项目下拉菜单关闭时反向返回触发点并恢复焦点；分页从 `1/2` 切换到 `2/2` 后显示不同真实记录。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 和 `720x500`（200% 缩放等效）六档视口均无文档级水平或垂直溢出，应用外框底部与视口底部一致。
- 六档视口下筛选控件最小高度均为 44px；页面主体可滚动到底部分页，窄视口的审计表只在自身内部横向滚动。`720x500` 下主体滚动范围为 1370px，分页按钮完整落在视口内。
- 临时 viewport override 已恢复；最终内置浏览器为 `1329x912`，页面文档尺寸与视口一致。

验收清理与运行态：

- 本切片和浏览器验收均为只读，没有创建临时数据库记录、对象存储文件或本地验收制品，无需数据清理。
- Web `3211`（PID `27220`）与 API `3220`（PID `7360`）按用户要求继续运行；API `/healthz` 返回 `200 / {"status":"ok"}`，Web 返回 `200`。
- PostgreSQL `5433` 与 MinIO `9100/9101` 容器均为 healthy；本轮未启动额外持续进程或外部浏览器。
- 本切片没有加入导出、归档、告警或可变审计备注；当前只提供权限安全的只读检索，真实审计工作流证明需要后再扩展。

## 46. 2026-08-30 P1 专业站内通知与通告确认

本轮把通告发布和重要变更接入真实、按成员隔离的站内通知闭环：

- 新增 `notifications` 与 `notification_preferences`；通知按收件人、团队和项目权限读取，列表固定只返回最近 50 条。
- 首次发布通告和已发布通告的重要修改在原 PostgreSQL 事务内创建通知，项目成员中排除操作人；偏好关闭时不会为该成员创建对应通知。
- 收件人可以标记全部已读或确认单条通知；确认同时写入 `notification.acknowledged` 审计，重复操作保持稳定。
- Web 顶部通知入口改为真实查询，覆盖 loading、error、empty、未读和已确认状态；Bell 仅在存在未读通知时使用强调色，不使用红点或角标。
- 通知偏好提供“通告发布”和“重要变更”两个开关；点击通知会进入对应项目的排期与通告页面。
- 本切片复用现有项目成员权限、通告事务、API client、Radix Popover、Button、Checkbox 与 Lucide，没有增加依赖或另一套身份逻辑。

工程验证：

- `pnpm test`：API 78 项测试、Web 15 项原生测试全部通过；新增覆盖收件人隔离、项目权限、偏好抑制、已读、确认和导航所需契约。
- `pnpm --filter @shadowproducer/api test:postgres`：16 项真实 PostgreSQL 测试全部通过；新增覆盖发布与重要变更的事务内通知、去重、偏好和审计。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：全部通过；迁移 `024_notifications.sql` 已在本地 PostgreSQL 成功应用。

内置浏览器与真实 PostgreSQL 验证：

- Bell 只在存在未读通知时显示强调色；没有红点或数量角标。确认通知后完整刷新，已确认状态仍由 PostgreSQL 回读，并存在对应审计记录。
- 点击通知成功进入 `#/project/north/winter-coffee/schedule`；偏好菜单正确显示两个已启用设置，验收过程中没有修改用户偏好。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 和 `720x500` 六档视口均无页面横向溢出；默认控件高度为 44px，退出动画完成后焦点返回 Bell。
- 最终内置浏览器 viewport 已恢复为 `1329x912`，控制台为 `0 errors`。

验收清理与运行态：

- 精确删除验收通知及其 `notification.acknowledged` 审计；再次查询 `notifications` 和 `notification.*` 审计均为 0。一次未认证的失败请求没有创建通告或回执。
- Web `3211`（PID `27220`）与 API `3220`（PID `34004`）继续运行；API `/healthz` 返回 `200 / {"status":"ok"}`，Web 返回 `200`。
- PostgreSQL `5433` 与 MinIO `9100/9101` 容器均为 healthy；本轮没有启动外部浏览器或额外持续进程。
- 当前只实现站内持久通知；邮件、系统推送、SSE、Outbox 和投递 Worker 未加入，等真实实时性或外部渠道需求明确后再扩展。

## 47. 2026-08-31 P1 权限模板与成员分配

本轮把团队与项目的细粒度权限接成可持久化、可审计的管理闭环：

- 团队管理员可以创建团队或项目权限模板、编辑自定义模板，并为团队成员分别分配团队权限与项目权限。
- 系统模板保持只读；当前用户不能修改自己的团队或项目权限，UI 和 API 均执行相同约束。
- 所有编辑和分配都携带 revision；并发修改返回明确冲突提示，重新载入后会清除旧错误并回读最新 revision。
- 权限分配会生成对应团队或项目通知；点击通知先写入已读时间，再进入当前团队权限页。
- 权限页面复用现有 Radix、Button、Dropdown、Dialog、Lucide 和统一权限契约，没有引入依赖或第二套权限状态。
- `WorkspaceApp` 保留当前团队的内存引用，修复内置浏览器无法使用 `sessionStorage` 时从权限入口意外退回团队选择页的问题。

工程验证：

- `pnpm test`：API 81 项、Web 15 项全部通过。
- `pnpm --filter @shadowproducer/api test:postgres`：17 项真实 PostgreSQL 测试全部通过。
- Web Lint、Web/API TypeScript 与 Next.js 生产构建均直接执行通过，未依赖 Turbo 缓存。

内置浏览器与真实 PostgreSQL 验证：

- 创建团队模板与项目模板后完整刷新仍可回读；自定义模板编辑和团队/项目成员分配均成功持久化。
- 当前用户的团队与项目权限控件均不可编辑。
- 第二个内置浏览器标签页并发更新后，旧页面提交显示“权限模板已在其他位置更新，请重新载入后再试”；点击“重新载入”后错误消失并回读 revision `3`。
- 权限分配生成真实通知；点击固定 ID 的验收通知后写入 `read_at`，并跳转到 `#/team/north/permissions`。
- 新建模板弹窗居中，初始焦点正确；Escape 关闭后焦点返回“新建模板”触发器。桌面视口 `1329x912` 下控制台无 error 或 warning。
- 本轮没有完成移动端与 200% 缩放实测，因此不把响应式矩阵计为通过。

验收清理与运行态：

- 精确删除两个验收自定义模板、2 条回执、审计 `552-557` 和 3 条通知。
- 宋岚团队权限恢复为 `north:team-admin / revision 1`；冬夜咖啡项目权限恢复为 `north:project-manager / revision 1`。
- 清理后 `north` 保留 6 条系统模板；权限审计、权限通知和权限回执均为 0。
- Web `3211`（PID `27220`）与 API `3220`（PID `39576`）继续运行；API `/healthz` 返回 `{"status":"ok"}`，Web 返回 `200`。本轮没有启动新持续进程。

## 48. 2026-08-31 P1 脚本在线状态与并发协作

本轮在现有 PostgreSQL revision、三方合并和 SSE 同步链路上补齐轻量多人协作状态：

- 脚本工作区返回当前账号；新增临时 presence 契约，包含在线成员、当前版本、选区起止位置和编辑状态。
- API 复用现有 SSE 连接维护进程内 presence；连接和断开会更新成员状态，未连接客户端不能发布状态，只读成员不能声明正在编辑。
- Web 显示实时连接状态、在线人数、当前用户和远端成员所在版本、光标行列；编辑器选区以 140ms 节流上报。
- 正文仍由 PostgreSQL revision 和幂等回执持久化；并发写入继续进入现有三方合并对话框，没有引入 WebSocket、CRDT、迁移、依赖或持久化 presence 表。

工程验证：

- `pnpm test`：API 9 个测试文件 82 项、Web 15 项全部通过；API 新增覆盖连接、断开、只读编辑状态拒绝与 presence 广播。
- 3 个真实 PostgreSQL 测试文件 17 项全部通过；覆盖脚本并发、幂等、权限、联系人和工作区持久化。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check` 全部通过；Next.js 16.3.2 生产构建成功。

内置浏览器与真实 PostgreSQL 验证：

- 繁星与顾遥双端连接后显示 `2 人在线`；顾遥状态显示 `正在编辑 v7 · 第 3 行 · 1 列`。
- 顾遥写入 revision 5 后繁星页面实时刷新；繁星自动保存 revision 6 后顾遥读取成功。
- 两个 UI 标签页同时基于 revision 6 编辑：第一个标签保存成功，第二个进入三方冲突对话框；对话框包含基线、本地和服务端正文，初始焦点进入合并文本框。
- `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 和 `720x500` 六档视口的文档级水平溢出均为 0，临时 viewport override 已恢复。

验收清理与运行态：

- 精确删除 3 条验收写回执和审计 `596-598`；`v7` 恢复为原始正文、revision `4`、时间戳 `2026-08-27 16:04:37.05+00`，原有 3 条历史回执保持不变。
- 临时 SSE 客户端 PID `15108`、执行会话、顾遥认证 cookie 和第二个内置浏览器标签页均已清理；主标签刷新后显示 `实时已连接 / 1 人在线 / 当前 v7 · 修订 4`，不含验收标记。
- Web `3211`（PID `27220`）与 API `3220`（PID `33452`）继续运行；API `/healthz` 返回 `ok`，Web 返回 `200`。PostgreSQL 与 MinIO 保持原运行状态。
- presence 当前只覆盖单 API 进程；出现多实例部署需求时再接跨进程事件总线。字符级共同输入、离线多主合并等需求明确后再评估 CRDT。

## 49. 2026-08-31 P1 Agent 审片意见与作品集草稿

本轮把两项已有业务能力接入全局 Agent 的统一 preview、execute、权限、幂等与审计链路：

- 新增 `create_review_comment`：按当前团队和项目校验作用域，只允许唯一匹配的视频审片文件；同名不同版本可用“名称 + 版本”精确选择。
- 审片意见沿用现有 `review.write` 权限、时间码、文件版本、幂等键和 `review-comment.created` 审计，不新增评论表或第二套写入逻辑。
- 新增 `create_portfolio`：创建团队可见草稿，沿用 `portfolio.write`、`portfolio.create:<teamId>` 回执和 `portfolio.created` 审计；公开发布仍是独立高风险动作。
- 自然语言解析支持带文件名称、版本和时间码的审片意见，以及标题、分类、年份和简介的作品集草稿；缺少时间码或目标不唯一时拒绝猜测。
- Agent 面板复用现有 Sheet、消息反馈和查询失效；新增结果不需要新入口、组件、依赖或持久化模型。

工程验证：

- `pnpm test`：API 9 个测试文件 85 项、Web 17 项全部通过；新增覆盖解析、唯一审片目标、结果持久化重放和团队可见作品集草稿。
- 3 个真实 PostgreSQL 测试文件 17 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check` 全部通过；Next.js 16.3.2 生产构建成功。
- 迁移 `025_agent_review_portfolio_commands.sql` 连续执行两次成功；数据库 action CHECK 精确包含现有 10 个 Agent 动作。

内置浏览器与真实 PostgreSQL 验证：

- 在项目 Agent 输入“为审片文件‘冬夜咖啡_主片 v12’添加意见 00:05.500…”，面板返回“已在 00:05.500 添加审片意见”；审片列表从 4 条变为 5 条，打开 v12 后可见时间码、繁星身份和完整意见。
- 输入带标题、分类、年份和简介的作品集草稿命令后，面板明确返回“当前仅团队可见”；作品集页刷新后回读标题、`幕后纪录 · 2026`、简介和团队可见状态。
- 两个已消费 intent 通过真实 API 再执行均返回 `replayed=true`，结果类型分别为 `review_comment_created` 和 `portfolio_created`，业务数据没有重复。
- PostgreSQL 读回 2 个 consumed intent、1 条审片意见、1 个作品集草稿、1 条作品集命令回执和 6 条业务/Agent 审计。

验收清理与运行态：

- 精确删除审片意见 `7e68cea0-7f47-4bcc-b1bf-99edfed423f3`、作品集 `5a171896-092e-4843-a506-d6de0f5022df`、2 个 intent、1 条回执和审计 `637-642`；各验收计数均恢复为 0。
- 清理后冬夜咖啡恢复 4 条基础审片意见，北岸影像恢复 1 个基础作品集；内置浏览器刷新后验收内容均消失，基础内容仍可见。
- 为捕获一次探针错误的原因，临时启动 API `3221`（PID `9876`、`19616`）；确认是探针缺少 `Content-Type` 后，两个进程树和端口均已释放。
- Web `3211`（PID `27220`）与 API `3220`（PID `17508`）继续运行；API `/healthz` 返回 `ok`，Web 返回 `200`。PostgreSQL 与 MinIO 保持原运行状态。

## 50. 2026-08-31 P1 Agent 作品集内容与审片意见状态

本轮继续复用现有业务服务，把两项专业操作接入 Agent 的统一白名单与执行链路：

- 新增 `add_portfolio_content`：只选择未公开作品集和当前团队已通过的唯一成片版本，固化作品集 revision 后调用 `PortfolioService.addContent()`；不会因添加内容而静默下线公开作品集。
- 新增 `update_review_comment`：按项目、审片文件、时间码或完整意见唯一选择目标，固化 comment revision 后调用 `ProductionService.updateReviewComment()`，支持解决和重新打开。
- 两个动作沿用既有权限、revision、回执或 intent 重放、审计和查询失效；没有新增仓储、入口、组件、依赖或重复业务写入。
- 迁移 `027_agent_portfolio_review_update_commands.sql` 只更新 `agent_command_intents.action` 的 CHECK，数据库当前精确允许 12 个 Agent 动作。

工程验证：

- `pnpm test`：API 9 个测试文件 88 项、Web 19 项全部通过；新增覆盖自然语言解析、唯一目标、写入结果和已消费 intent 重放。
- 3 个真实 PostgreSQL 测试文件 17 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 成功；API `/healthz` 返回 `{"status":"ok"}`。

内置浏览器与真实 PostgreSQL 验证：

- Agent 创建临时团队可见作品集后，将已通过的 `冬夜咖啡_主片 v11` 加入其中并设为封面；作品集页刷新后回读 1 项内容、版本和封面状态。
- Agent 将 `冬夜咖啡_主片 v12` 在 `00:07.120` 的意见标记为已解决；审片工作台立即从 `3 条待处理 / 1 条已解决` 更新为 `2 / 2`，完整刷新后仍保持。
- 使用独立 Better Auth 临时会话再次执行两个 consumed intent，均返回 HTTP `200`、`replayed=true`，业务写入没有重复。
- PostgreSQL 读回临时作品集 revision `2`、唯一内容项、意见 revision `2`、3 个 consumed intent、2 条业务回执和 9 条业务/Agent 审计。

验收清理与运行态：

- 精确恢复基础意见 `c1` 为 `open / revision 1 / 2026-08-27 18:15:13.610007+00`；基础作品集 `portfolio-winter-coffee` 保持 `团队可见 / revision 1`。
- 精确删除临时作品集、内容项、3 个 intent、2 条回执、审计 `719-727`、临时认证 session 和 Cookie 文件；复核各临时计数均为 `0`。
- 内置浏览器刷新后只剩基础作品集，审片工作台恢复 `3 条待处理 / 1 条已解决`。
- 本轮没有启动新持续进程；既有 Web `3211`、API `3220`、PostgreSQL `5433` 与 MinIO `9100/9101` 保持运行。

## 51. 2026-08-31 P1 跨版本审片意见对应

本轮在既有 A/B 审片工作台中补齐可追溯、可撤销的跨版本意见对应：

- 新增独立 `review_comment_links` 关系表；原意见不复制、不覆盖、不删除，关系撤销使用 soft remove 与 revision。
- 内部 correspondence API 同时返回已确认关系和确定性候选；候选只使用时间码距离与规范化文本相似度，不自动写入。
- 确认沿用 `review.write`、幂等回执和审计；撤销沿用 `review.write`、expected revision 和审计。公开客户审片接口不暴露内部跨版本关系。
- A/B 工作台在 A 版本意见下显示 B 版本候选或已确认对应，可跳转时间码、确认或撤销；交互复用现有 Radix、Lucide、Motion 和零圆角工作站样式。
- 没有引入 AI 服务、embedding、向量库、CRDT、新依赖或第二套评论模型。

工程验证：

- `pnpm test`：API 9 个测试文件 90 项、Web 19 项全部通过；新增覆盖候选、确认、重放、撤销、revision 冲突、只读权限和原评论保留。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件、18 项测试全部通过；关系、回执与审计均实际落库。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 成功应用迁移 `028_review_comment_links.sql`。

内置浏览器与真实 PostgreSQL 验证：

- 在 v12 与 v11 分别创建同文、同时间码的临时意见，A/B 工作台显示 `建议 · B v11`；确认后显示 `1 组 A/B 对应` 和 `已对应`。
- 完整刷新回到文件列表后重新进入 v12、选择 v11，已确认关系仍可读回。
- 撤销后恢复候选；随后重建关系并模拟 revision 并发变化，旧页面显示“审片意见对应关系已在其他位置更新，请重新载入后再试”，查询刷新后再次撤销成功。
- viewer API 测试可读取候选但不能确认；跨项目或不存在评论由服务返回 `403/404`，同文件或自身对应返回明确 `400/409`。

验收清理与运行态：

- 精确删除临时评论 `0281517f-e8c8-438b-b176-ac1ad615439b`、`b9357dfa-83ee-4f86-a2bc-179e08ca4eea`、关系 `3cd9e38b-bf27-4917-b0f5-2919f0dba044`、2 条关系回执和审计 `747-752`；四类计数均复核为 `0`。
- 内置浏览器刷新后临时标记消失，`冬夜咖啡_主片 v12` 恢复基础 `4 条`意见。
- 本轮没有启动新持续进程；既有 Web `3211`（PID `27220`）与 API `3220`（PID `25168`）均返回 `200`，PostgreSQL `5433` 与 MinIO `9100/9101` 容器保持 healthy。

## 52. 2026-08-31 P1 Agent 创建执行阶段

本轮把项目执行计划接入全局 Agent 的统一业务命令链：

- 新增 `create_execution_stage`，复用 `ProductionService.createExecutionStage()`、`production.write` 权限、既有幂等回执和审计，不新增第二套排期写入逻辑。
- 自然语言命令可解析阶段名称、起止时间、负责人、状态、进度、备注与人员/场地/设备资源；资源按类型与规范化名称复用稳定 ID。
- 项目作用域、项目成员权限和只读成员限制由 API 与应用层共同执行；Web 只展示服务端预览和执行结果，并失效执行计划查询。
- 迁移 `029_agent_execution_stage_command.sql` 将新动作加入最终 action CHECK。历史迁移 `014/020/022/025/027` 的中间 CHECK 改为 `NOT VALID`，避免全量重放时已有未来动作被旧约束拒绝；最终 `029` 仍创建完整且立即验证的约束。

工程验证：

- `pnpm test`：API 9 个测试文件 92 项、Web 20 项全部通过；新增覆盖自然语言解析、持久化、重放、项目越界和只读成员拒绝。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 18 项全部通过。
- 聚焦 Agent API 测试 `29/29`，API Lint 与 TypeScript 通过；全仓 `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check` 全部通过。
- 在验收数据仍存在时连续执行两次 `pnpm db:migrate`，两次均成功完成迁移和确定性种子重放。

内置浏览器、真实 API 与 PostgreSQL 验证：

- Agent 创建“P1 Agent 冲突验收”阶段，正确解析并关联 `old-north-station` 与 `rain-rig-1`；执行计划刷新后从 PostgreSQL 回读 `5 个阶段 / 2 项冲突`。
- 使用独立 Better Auth 临时会话再次执行已消费 intent，HTTP `200`、`replayed=true`、阶段 ID 不变，业务阶段仍只有 1 条。
- 清理后内置浏览器完整刷新，执行计划恢复 `4 个阶段 / 0 项冲突`，下一阶段为“补拍与收尾”，临时阶段和冲突明细均已消失。
- 浏览器控制台只有 React DevTools 与 HMR 信息，没有 error 或 warning。

验收清理与运行态：

- 精确删除阶段 `a21b5467-ead2-4036-a75b-b6f703297557`、intent `02f1c398-c5be-4c9e-aeab-19b88d3b797d`、对应回执和审计 `793-795`；阶段、资源、intent、回执、审计计数均复核为 `0`，冬夜咖啡恢复 4 个基础阶段。
- 临时 Better Auth session 已在重放探针的 `try/finally` 中删除，复核计数为 `0`。
- Web `3211`（PID `27220`）与 API `3220`（PID `39276`）继续运行并分别返回 `200` 与 `{"status":"ok"}`；PostgreSQL `5433` 与 MinIO `9100/9101` 容器均为 healthy。
- 本轮没有启动外部浏览器或新的持续进程；既有服务按用户要求保持运行。

## 53. 2026-08-31 P1 Agent 更新执行阶段

本轮补齐执行计划创建后的修改闭环：

- 新增 `update_execution_stage`；按阶段 ID 或规范化后的唯一阶段名称选择目标，预览时固化 revision，执行时复用 `ProductionService.updateExecutionStage()`。
- 支持更新名称、起止时间、原始时区、进度、负责人、状态、备注与资源；没有提供的字段保持原值，资源仍按类型和规范化名称复用稳定 ID。
- 普通更新无需高风险确认，但继续受项目作用域、`production.write`、revision 冲突、Agent intent 消费与结果重放约束。
- 修复共享执行计划更新服务对显式 `undefined` 可选字段的合并问题；部分更新不会再把未提供的起止时间覆盖为空。
- 迁移 `030_agent_update_execution_stage_command.sql` 将最终 action CHECK 扩展为 14 项并保持 validated；`029` 变为历史 `NOT VALID` 步骤，确保已有新动作 intent 时仍可全量重放迁移。

工程验证：

- `pnpm test`：API 9 个测试文件 97 项、Web 21 项全部通过；新增覆盖中文解析、部分更新、唯一目标、空更新、消费后重放、revision 冲突、项目越界和只读成员拒绝。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 18 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 连续两次 `pnpm db:migrate` 成功；最终数据库约束 `convalidated=true`，非法 action 计数为 `0`。

内置浏览器、API 与 PostgreSQL 验证：

- 在拍摄与通告页通过 Agent 将“补拍与收尾”从 `10% / 未开始` 更新为 `17% / 进行中`，备注改为“P1 Agent 更新验收”；页面查询立即失效并显示新值。
- 首次真实浏览器验收发现部分更新把未提供时间传为 `undefined`，共享服务修复后聚焦测试 `57/57` 通过，同一浏览器命令成功执行。
- 完整刷新后仍从服务端读回新进度、状态、备注和 revision `2`；资源冲突保持 `0 项`。
- 运行中的 API 对裸开发身份头返回 `401 AUTH_REQUIRED`；随后使用相同 Fastify API、真实 application service 与真实 PostgreSQL repository 的无监听端口验收探针重放 consumed intent，返回 `200`、`replayed=true`，revision 保持 `2`。

验收清理与运行态：

- 精确恢复 `exec-pickup` 的进度、状态、备注、revision 与 `updated_at`；删除失败 intent `ea6a8d27-c6a9-483d-8acc-4ec77ee2f296`、成功 intent `e706d600-4032-42aa-b357-a59d013bcbeb` 和审计 `876-879`。
- 复核两个 intent 与四条审计计数均为 `0`；内置浏览器刷新后恢复 `46% / 4 个阶段 / 0 项冲突 / 补拍与收尾 10% 未开始`。
- 本轮没有启动外部浏览器或新的持续监听进程；既有 Web `3211`（PID `27220`）与 API `3220`（热重载后监听 PID `36580`）按用户要求继续运行。

## 54. 2026-08-31 P1 Agent 更新任务

本轮把已有任务创建能力补齐为可修改闭环：

- 新增 `update_task`；按任务 ID 或项目范围内规范化后的唯一完整标题选择目标，预览时固化 revision，执行时复用 `WorkspaceService.updateTask()`。
- 支持更新标题、截止日期、状态与目标视图；未提供字段保持原值，显式 `null` 可清空截止日期，空更新在预览阶段拒绝。
- 普通任务更新无需高风险确认，但继续受团队写权限、项目筛选、revision 冲突、Agent intent 消费和结果重放约束。
- Web Agent 可解析中文任务更新命令，执行后失效任务查询并把卡片移动到正确泳道；更具体的任务意图优先于标题中可能出现的“通告”等领域词。
- 迁移 `031_agent_update_task_command.sql` 将最终 action CHECK 扩展为 15 项并保持 validated；`030` 变为历史 `NOT VALID` 步骤，避免已有新动作 intent 阻塞全量迁移重放。

工程验证：

- `pnpm test`：API 9 个测试文件 100 项、Web 22 项全部通过；新增覆盖中文解析、唯一目标、空更新、写入、消费后重放、revision 冲突和只读成员拒绝。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 18 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 在数据库保留 pending `update_task` intent 时连续两次 `pnpm db:migrate` 成功，intent 未丢失；最终约束 `convalidated=true`，非法 action 计数为 `0`。

内置浏览器、API 与 PostgreSQL 验证：

- 在团队看板通过 Agent 将“发布车站夜戏通告”从 `待开始 / 2026-08-29 / revision 1` 更新为 `进行中 / 2026-09-02 / revision 2`；任务立即进入“进行中”泳道。
- 完整刷新后仍从服务端读回新状态与截止日期；PostgreSQL 同时读回 consumed intent、`task_updated` 结果和业务 revision `2`。
- 使用独立 Better Auth 临时会话再次执行同一 consumed intent，返回 `200`、`replayed=true`，结果仍为 revision `2`，没有第二次业务写入。

验收清理与运行态：

- 精确恢复 `task-publish-call-sheet` 的截止日期、状态、目标视图、revision 与 `updated_at`；删除验收 intent `2f902cfb-dfa8-46d7-a278-85362b90ec13`、迁移探针 intent `24bb375b-dafd-4165-b2f5-7dd582c86e0c`、审计 `920-923` 和失败探针遗留的临时认证 session。
- 复核 intent、审计、临时 session 计数均为 `0`；内置浏览器刷新后恢复 `待开始 / 2026-08-29`，团队任务总数保持 `4`。
- 本轮没有启动外部浏览器或新的持续监听进程；既有 Web `3211`（PID `27220`）与 API `3220`（热重载后监听 PID `36284`）返回 `200/ok`，PostgreSQL `5433` 与 MinIO `9100/9101` 容器保持 healthy。

## 55. 2026-08-31 P1 项目内多脚本文档

本轮把单项目单脚本扩展为项目内多文档协作：

- 保留原项目级脚本接口作为默认文档兼容入口；文档列表、创建、工作区、版本、评论、版本影响分析、SSE 与 presence 均支持显式 `documentId`。
- 版本 ID 继续保持项目级唯一，避免破坏现有版本、评论、拆解来源和深链；每个文档独立维护当前版本、正文、评论与实时协作状态。
- 迁移 `032_script_multidocument.sql` 移除项目单文档约束，增加文档类型、默认标记、创建时间，并将“唯一当前版本”索引改为文档维度。
- Web 增加文档选择、新建对话框和 `document` hash 深链；`ScriptEditor` 按 `projectId:documentId` 重挂载，切换时不会继承另一文档的草稿、冲突、定时器或 presence。
- 真实页面首次验收发现创建成功后旧查询结果会把新文档选择覆盖回默认文档；修复为先同步更新 React Query 文档列表缓存，再切换深链并后台刷新。

工程与数据库验证：

- `pnpm test`：API 9 个测试文件 103 项、Web 22 项全部通过；新增覆盖文档创建/重放、viewer 拒绝、404、版本与评论隔离、SSE 与 presence 隔离。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 20 项全部通过；新增覆盖创建回执/审计、双文档读写隔离和 viewer 不落库。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 连续两次 `pnpm db:migrate` 成功；默认文档数量异常、当前版本数量异常、当前指针错配均为 `0`，两个文档级唯一索引存在。

内置浏览器、重放与清理：

- 在冬夜咖啡脚本页创建独立文档后自动切换到 `?document=612a73ab-...`；编辑 `v8` 后显示已自动保存，完整刷新仍从服务端回读 revision `2` 正文。
- 在默认 `v7` 与验收 `v8` 间往返切换，URL、正文和评论数量保持各自文档边界；清理后刷新恢复默认文档，验收标题消失，控制台无 error 或 warning。
- 使用独立 Better Auth 临时会话重放同一创建回执，HTTP `201`、`replayed=true`、文档 ID 不变；登出失败的临时 session 随后按唯一 ID 精确删除，繁星 session 数恢复基线 `6`。
- 精确删除验收文档、版本、文档回执、保存回执和审计 `1017/1018`；五类计数均为 `0`，默认文档与当前版本不变量仍无异常。
- 本轮没有启动外部浏览器或新的持续监听进程；既有 Web `3211`（PID `27220`）与 API `3220`（热重载后 PID `14668`）继续返回 `200/ok`，PostgreSQL `5433` 与 MinIO `9100/9101` 容器保持 healthy。

## 56. 2026-08-31 P1 跨文档版本并发分配

本轮修复项目内多文档同时创建版本时的项目级 `vN` 编号竞争：

- 根因是版本编号按项目扫描生成，但事务此前只锁定目标文档；不同文档可以同时计算出同一个下一个版本 ID。
- `createVersion()` 现在先锁定所属项目行，再锁定目标文档并分配版本编号；沿用 PostgreSQL 原生事务锁，没有新增迁移、表、依赖或第二套编号器。
- 新增真实 PostgreSQL 双文档并发测试；两个版本均成功创建，项目级 ID 唯一并分别为 `v3`、`v4`，版本、回执和审计数量符合预期。

验证结果：

- 聚焦脚本 PostgreSQL 测试：`10/10` 通过。
- `pnpm test`：API `103` 项、Web `22` 项全部通过。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件、`21` 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 内置浏览器完整刷新后继续由服务端回读 `冬夜咖啡_拍摄稿 / v7`，`document` 深链保持不变，控制台无 error。
- PostgreSQL 中测试前缀的账户、团队和项目残留均为 `0`；本轮没有 schema 变化，因此没有执行迁移。
- 本轮没有启动新的持续进程；既有 Web `3211`（PID `27220`）与 API `3220`（热重载后 PID `32184`）返回 `200/ok`，PostgreSQL `5433` 与 MinIO `9100/9101` 容器保持 healthy。

## 57. 2026-08-31 P1 脚本实时连接补偿回读

本轮补齐脚本 SSE 断线窗口的最小可靠恢复策略：

- `EventSource` 每次建立连接时立即失效当前 `projectId + documentId` 的工作区查询，由 React Query 从服务端补读当前版本、正文和评论。
- 初次连接允许一次冗余回读，换取重连路径不需要维护本地事件游标、事件日志或第二套同步状态；presence 继续由服务端连接快照更新。
- 没有新增 migration、依赖、broker、定时器或自定义重连实现，继续使用浏览器原生 EventSource 重连。

验证结果：

- `pnpm test`：API `103` 项、Web `22` 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 本轮代码未触及 PostgreSQL；上一切片同一运行态的真实 PostgreSQL `21/21` 测试保持通过。
- 内置浏览器完整刷新后恢复“实时已连接”，继续从服务端回读 `冬夜咖啡_拍摄稿`，控制台无 error。
- 本轮没有创建验收数据或启动新持续进程；既有 Web `3211`（PID `27220`）与 API `3220`（PID `32184`）返回 `200/ok`，PostgreSQL 与 MinIO 继续保持 healthy。

## 58. 2026-08-31 P1 Agent 确认制片拆解

本轮把制片拆解的高风险正式确认接入 Agent 统一命令链：

- 新增 `confirm_breakdown`；预览时按项目、分类和可选项目 ID 选择当前“待确认”候选，并固化每项 revision。
- 命令标记为 `high`，必须先确认再执行；执行复用 `ProductionService.confirmBreakdown()`、既有项目权限、PostgreSQL 事务、幂等回执和审计。
- Web 支持中文命令解析，成功后失效当前项目的 `production-breakdown` 查询，不增加第二套拆解状态或写入逻辑。
- 迁移 `033_agent_confirm_breakdown_command.sql` 将最终 action CHECK 扩展为 16 项；历史 `031` 改为 `NOT VALID`，最终 `033` 依然立即验证。

工程与数据库验证：

- `pnpm test`：API 9 个测试文件 `105` 项、Web `23` 项全部通过；Agent API 新增覆盖未确认阻断、确认执行、消费后重放、单次写入与 stale revision 冲突。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 `21` 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 连续两次 `pnpm db:migrate` 成功；数据库最终约束 `convalidated=true`，定义中精确包含 16 个 action。

内置浏览器、重放与清理：

- 在制片拆解页通过 Agent 单项确认 `br3`；高风险弹窗显示唯一项目目标，执行后页面立即显示“已确认”，完整刷新后仍从服务端回读 revision `2`。
- 使用独立 Better Auth 临时会话重放同一 consumed intent，返回 `200`、`replayed=true`；拆解 revision、业务审计和回执数量均未再增长，临时会话登出返回 `200`。
- 精确恢复 `br3` 的状态、revision 与 `updated_at`；删除 intent `0e4452db-5146-4533-b61e-548f2d03cd94`、对应回执和审计 `1138-1141`。intent、回执、审计与临时 auth session 残留计数均为 `0`。
- 内置浏览器刷新后恢复“美术与道具 1 / 待确认”；本轮没有启动外部浏览器或新的持续进程。既有 Web `3211`（PID `27220`）与 API `3220`（热重载后 PID `31920`）继续返回 `200/ok`，PostgreSQL `5433` 与 MinIO `9100/9101` 容器保持 healthy。

## 59. 2026-08-31 P1 Agent 更新制片拆解

本轮把制片拆解的专业字段维护接入 Agent 统一命令链：

- 新增 `update_breakdown`；按拆解项 ID 或规范化后的唯一候选名称选择目标，预览时固化 revision，执行时复用 `ProductionService.updateBreakdown()`。
- 支持更新候选内容、需求类型、规格、数量、准备要求、负责部门和状态；未提供字段保持原值，不向 Agent 暴露供应商关联。
- 直接把状态改为“已确认”仍由既有服务返回 `BREAKDOWN_CONFIRMATION_REQUIRED`，不能绕过上一切片的正式确认流程。
- Web 自然语言解析优先识别拆解更新，避免“状态：已确认”误判成确认命令；执行成功后失效当前项目的 `production-breakdown` 查询。
- 迁移 `034_agent_update_breakdown_command.sql` 将最终 action CHECK 扩展为 17 项；历史 `033` 使用 `NOT VALID`，最终 `034` 保持立即验证。

工程与数据库验证：

- `pnpm test`：API 9 个测试文件 `108` 项、Web `25` 项全部通过；新增覆盖唯一目标、空更新、viewer 拒绝、stale revision、正式确认保护、单次写入和 consumed intent 重放。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 `21` 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 连续两次 `pnpm db:migrate` 成功；最终 action CHECK `convalidated=true`，定义中精确包含 17 个 action。

内置浏览器、重放与清理：

- 在制片拆解页通过 Agent 将 `br3` 的准备要求和负责部门更新为验收值；页面立即显示新值，完整刷新后仍从 PostgreSQL 回读 `revision 2`。
- 使用独立 Better Auth 临时会话再次执行 consumed intent，返回 HTTP `200`、`replayed=true`、`breakdown_updated / revision 2`；业务 revision 和审计数量没有再次增长，临时 session 在 `finally` 中删除。
- 精确恢复 `br3` 的准备要求、负责部门、状态、revision 与 `updated_at`；删除 intent `2c0ba63c-6219-436b-b6d2-09763065ca0e` 和审计 `1191-1193`。intent、审计、回执与临时 auth session 残留计数均为 `0`。
- 内置浏览器刷新后恢复原准备要求、“美术 / 道具”和“待确认”，控制台无 error 或 warning；本轮没有启动外部浏览器或新的持续进程。既有 Web `3211`（PID `27220`）与 API `3220`（PID `19644`）继续返回 `200/ok`，PostgreSQL `5433` 与 MinIO `9100/9101` 保持 healthy。

## 60. 2026-08-31 P1 拆解项负责人和项目任务关联

本轮补齐已确认拆解项的负责人和项目任务关系：

- `BreakdownItem` 增加单一负责人和多任务关系；负责人候选只来自当前项目成员，任务候选只来自当前项目、同团队且未删除的任务。
- 关系继续复用既有拆解更新入口、revision、项目写权限、事务与 `breakdown.updated` 审计；只有“已确认”拆解项允许修改关系。
- 新增项目级关系候选接口，避免复用只返回当前用户任务的个人工作区查询；Web 在既有编辑弹窗中使用共享选择控件，不增加第二套写入状态。
- 迁移 `035_breakdown_responsibility_tasks.sql` 为拆解项增加负责人外键，并用 `breakdown_item_tasks` 复合主键关系表保存多任务关联。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm test`：API 9 个测试文件 `109` 项、Web `25` 项全部通过；覆盖未确认状态、非法成员、跨项目任务、跨团队任务、软删除任务和 stale revision 等错误路径。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 `21` 项全部通过。
- `pnpm db:migrate` 再次成功，证明迁移和确定性种子可重复执行。

内置浏览器、回读与清理：

- 在制片拆解页正式确认 `br1 / 顾遥`，随后分配负责人“宋岚”并关联任务“发布车站夜戏通告”；页面立即显示新关系。
- 完整刷新后仍从 PostgreSQL 回读 `已确认 / revision 5 / 宋岚 / 发布车站夜戏通告`，证明关系不是前端临时状态。
- 精确删除 `br1 + task-publish-call-sheet` 关系、确认回执和审计 `1194/1195`，并恢复 `br1` 的负责人、状态、revision 与 `updated_at`；复核关系、回执和本轮审计残留均为 `0`，审计恢复 `max=84 / count=46`。
- 内置浏览器再次完整刷新后恢复 `待确认 / 未分配 / 未关联任务`；Web `3211`（PID `27220`）与 API `3220`（PID `37092`）继续返回 `200/ok`，PostgreSQL `5433` 与 MinIO `9100/9101` 保持 healthy。
- 本轮没有启动新的持续进程，既有服务按用户要求继续运行。

## 61. 2026-08-31 P1 拆解项联系人关联

本轮补齐已确认拆解项的多联系人关系：

- `BreakdownItem` 增加带来源的 `contactRefs`；合法来源仅为当前团队的团队联系人和明确共享给当前团队、允许继续关联项目的成员个人联系人。
- 团队联系人必须已经关联当前项目；私有、撤销共享、禁止继续关联、软删除及其他团队或项目的联系人不能建立新关系。
- 关系继续复用既有拆解 `PATCH`、revision、项目写权限、事务和 `breakdown.updated` 审计；未确认拆解项不能修改联系人关系。
- 读取保留历史引用的 `source + contactId`，因此联系人后续撤销共享或软删除不会破坏既有制片记录；界面会把当前已不可见引用显示为不可用联系人。
- 迁移 `036_breakdown_contacts.sql` 使用带来源的复合主键关系表，不增加多态联系人外键，也没有新增第二套写入接口或依赖。

工程与数据库验证：

- 聚焦制片 API 测试 `26/26` 通过；覆盖多联系人写入、重复引用、不可用联系人、未确认状态、stale revision 与清空关系。
- `pnpm test`：API 9 个测试文件 `110` 项、Web `25` 项全部通过。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 `21` 项全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 应用迁移后再次成功，证明迁移和确定性种子可重复执行。

内置浏览器、回读与清理：

- 在制片拆解页正式确认 `br1 / 顾遥`，随后关联成员共享联系人 `林乔` 和团队联系人 `远景雨效`；页面立即显示两人。
- 完整刷新后仍从 PostgreSQL 回读 `已确认 / revision 5` 及 `member-shared:personal-contact-linqiao`、`team:team-contact-rain-fx` 两条关系。
- 精确删除两条关系、确认回执和审计 `1294/1295`，并恢复 `br1` 的状态、revision 与负责人；关系、回执和本轮审计残留均为 `0`，审计恢复 `max=84 / count=46`。
- 内置浏览器再次完整刷新后恢复 `待确认 / 未关联联系人`；Web `3211`（PID `27220`）与 API `3220`（PID `12976`）继续返回 `200/ok`。
- 本轮没有启动外部浏览器或新的持续进程，既有服务按用户要求继续运行。

## 62. 2026-08-31 P1 拆解项通告表关联

本轮把已确认拆解项与现有通告表建立可持久化多对多关系：

- `BreakdownItem` 增加 `callSheetIds`，关系候选只来自当前项目的通告表；草稿、待确认和已发布通告表均可关联，关联不会改变通告发布状态。
- 继续复用既有拆解 `PATCH`、项目写权限、已确认状态约束、revision、PostgreSQL 事务和 `breakdown.updated` 审计；没有新增第二套接口或依赖。
- 迁移 `037_breakdown_call_sheets.sql` 使用复合主键关系表，对被引用通告表使用 `ON DELETE RESTRICT`，避免静默丢失正式制片准备记录。

工程与数据库验证：

- `pnpm test`：API 9 个测试文件 `111` 项、Web `25` 项全部通过；新增覆盖未确认阻断、重复 ID、跨项目通告表、成功写入、stale revision、清空和列表回读。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 `21/21` 通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 应用迁移后再次成功，证明迁移和确定性种子可重复执行。

内置浏览器、回读与清理：

- 在制片拆解页正式确认 `br1 / 顾遥`，随后关联 `sd-05 / 车站夜戏`和 `sd-06 / 站台出口与清晨补景`；页面立即显示两张通告表。
- 完整刷新后仍从 PostgreSQL 回读 `已确认 / revision 5 / sd-05 / sd-06`，证明关系不是前端临时状态。
- 精确删除两条 `breakdown_item_call_sheets` 关系、确认回执 `a025e6ca-6788-4995-b11f-1b683b5add99` 和审计 `1345/1346`，并恢复 `br1` 为 `待确认 / revision 3`。
- 关系、回执和本轮审计残留均为 `0`，审计恢复 `max=84 / count=46`；内置浏览器刷新后恢复“待确认 / 未关联通告”。
- 本轮没有启动外部浏览器或新的持续进程，既有服务按用户要求继续运行。

## 63. 2026-08-31 P1 独立拍摄日与制片关系

本轮将通告表上层的独立 `ShootingDay` 纳入项目制片流程：

- 新增拍摄日契约、应用服务、PostgreSQL 仓储、API 路由和迁移 `038_shooting_days.sql`；拍摄日支持创建、编辑、状态流转、revision、幂等回执和审计。
- 拍摄日与制片拆解项建立多对多关系，与通告表建立一对多关系；一个拍摄日可关联多张通告表，既有未关联通告表继续兼容。
- 写入复用项目权限和事务边界；重复日期/日序、非法关系、stale revision 和无权限访问均有明确错误响应。
- PostgreSQL 仓储对 `date` 类型统一返回 `YYYY-MM-DD`，并使用事务级 advisory lock 保证并发幂等命令不会重复执行。
- 排期页支持选择、新建、编辑和变更拍摄日状态，并在拍摄日下管理通告表及拆解关系；空态和无关联历史数据均可正常显示。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm test`：API `113/113`、Web `25/25` 全部通过。
- 迁移连续执行两次均成功，证明 `038_shooting_days.sql` 和确定性种子可重复执行。

内置浏览器、回读与清理：

- 新建 `P1 验收拍摄日 / 2026-09-04 / 第 91 天`，状态改为“已确认”，创建关联通告 `P1 验收通告`，确认并关联 `br2 / 旧站台`。
- 完整刷新后拍摄日状态、通告归属、拆解确认状态和拍摄日关系均从服务端正确回读；桌面 `1440x1000` 与移动端 `375x812` 均正常渲染，最终刷新没有新增控制台错误。
- 精确删除拍摄日 `24f7935b-4bdc-4e90-987f-acc35ec06dfa`、通告表 `a026827e-2b32-4d43-a96b-4bb8d479be1f`、1 条拆解关系、3 条命令回执和审计 `1347-1351`。
- 精确恢复 `br2` 为 `待确认 / revision 1 / updated_at 2026-08-27 18:15:13.598021+00`；所有 P1 验收残留计数均为 `0`，浏览器刷新确认验收数据已消失且种子拍摄日仍在。
- 本轮没有启动外部浏览器或新的持续进程。最终只读复核确认 Web `3211`（PID `27220`）与 API `3220`（PID `31600`）均为本项目 Node 进程并返回 `200`；PostgreSQL 容器 `1443598c0d70`（PID `2264705`）与 MinIO 容器 `cafa71bd324a`（PID `3303813`）均为 `healthy`，MinIO `9100/9101` 均返回 `200`。既有服务按用户要求继续运行。

## 64. 2026-08-31 P1 Agent 查询审计记录

本轮把审计查询接入 Agent 统一命令链：

- 新增只读命令 `list_audit_logs`，支持团队范围、可选项目、动作、操作者、日期和分页筛选；预览与执行均复用 `WorkspaceService.listAuditLogs()`，没有第二套审计查询或权限逻辑。
- 执行前重新检查当前账号权限；结果以 `audit_log_list` 持久化到 consumed intent，后续执行直接返回同一结果并标记 `replayed=true`。
- Web 自然语言解析支持审计动作、操作者和“最近 N 条”，Agent 面板展示命中数量及最近操作摘要。
- 迁移 `039_agent_list_audit_logs_command.sql` 将最终 action CHECK 扩展为 18 项；数据库约束 `convalidated=true`，定义精确包含全部 18 个 action。

工程与数据库验证：

- 聚焦 Agent API `47/47`、全量 API `116/116`、Web `27/27` 全部通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- 连续两次 `pnpm db:migrate` 成功，证明迁移与确定性种子可重复执行。

内置浏览器、重放与清理：

- 在审计页通过 Agent 执行“动作：agent.；操作者：繁星；最近 5 条”，返回“找到 1 条审计记录，最近操作：繁星：agent.previewed”。
- 使用当前 `buildApp + AgentCommandService + PostgresAgentCommandRepository` 对同一 consumed intent 做持久化重放，返回 HTTP `200`、`replayed=true`、`audit_log_list`；审计计数和最大 ID 重放前后均为 `2|1353`。正式实例保持 `AUTH_ALLOW_DEV_HEADER=false`，没有为验收打开身份绕过。
- 完整刷新后审计页标题和表格正常、无错误态；控制台仅有 React 开发提示与 HMR 日志，没有 error。
- 精确删除 intent `03e605df-5f7f-4357-864b-05877f72745b` 和审计 `1352/1353`；intent 与审计残留计数均为 `0`。
- 本轮没有启动外部浏览器或新的持续进程。Web `3211`（PID `27220`）返回 `200`，API `3220`（热重载后 PID `39588`）返回 `healthz=ok`；PostgreSQL（PID `2264705`）与 MinIO（PID `3303813`）容器均为 `healthy`。既有服务按用户要求继续运行。

## 65. 2026-08-31 P1 通告版本级收件与确认

本轮为每个正式通告版本补齐不可变收件记录：

- 发布事务继续使用项目成员和现有通知偏好筛选收件人，排除发布者；不从演员自由文本推断账号，也没有新增邮件、短信或 Webhook 通道。
- 迁移 `040_call_sheet_publication_recipients.sql` 保存 publication、通知和收件账号关联，以及发布瞬间的姓名、邮箱和项目角色快照；账号或通知后续删除不会破坏历史展示。
- `notifications.read_at / acknowledged_at` 继续作为已读和确认真相，历史查询只回读现有通知状态；当前收件人可通过现有通知更新接口确认，其他成员不能代替确认。
- 历史 publication 没有收件快照时返回空数组，不对旧版本伪造回填；重复发布仍由既有幂等回执直接重放，不重复生成通知或收件记录。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm test`：API `116/116`、Web `27/27` 全部通过。
- 聚焦制片与真实 PostgreSQL 测试 `35/35` 通过；覆盖发布快照、重复发布重放、偏好过滤、本人确认、确认后历史回读和非本人不能确认。
- 连续两次 `pnpm db:migrate` 成功，证明迁移与确定性种子可重复执行。

内置浏览器、回读与清理：

- 新建并发布 `P1 通告收件验收 2026-08-31`；发布记录显示 `已送达 2 · 已确认 0`，并展示宋岚、顾遥在发布时的邮箱和项目角色。
- 完整刷新后仍从 PostgreSQL 回读相同收件快照；旧的迁移版本显示 `已送达 0 · 已确认 0`，没有错误回填。发布记录弹窗位于视口中央、无横向溢出，控制台无 error 或 warning。
- 精确删除通告 `12fd34ea-3028-42cf-a10e-da07889e97a6`、publication `18343612-f88b-4a78-b420-8e05baa0d165`、2 条 recipient、2 条 notification、2 条 command receipt 和审计 `1374/1375`；六类残留计数均为 `0`。
- 内置浏览器再次刷新后验收通告已消失。本轮没有启动外部浏览器或新的持续服务，既有 Web 与 API 按用户要求继续运行。

## 66. 2026-08-31 P1 专业影视通告包

本轮将通告表从基础拍摄信息扩展为可用于现场执行的结构化通告包：

- 通告契约、PostgreSQL 行、发布快照、发布后变更快照和幂等回执统一增加部门集合、特殊器材、安全事项、交通、餐饮、关键联系人和次日预告；继续复用既有 CRUD、revision、权限、发布、通知、收件人与版本历史链路。
- 迁移 `041_call_sheet_professional_fields.sql` 为旧行提供确定性默认值，并补齐历史 publication、change snapshot 及 create/publish receipt；没有新增第二套通告状态机。
- 编辑器按“基础信息 / 制片执行”分区，结构化行可增删；展示层删除硬编码安全与交通文案，全部回读数据库数据。
- 动态行使用仅前端存在的稳定 `clientId`，输入时不会因 React key 变化丢失焦点；删除按钮列宽与共享 `size-11` 控件一致，桌面宽度不再产生 4px 伪横向滚动。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `116/116`、Web `27/27`，Next.js 16.3.2 生产构建成功。
- `pnpm --filter @shadowproducer/api test:postgres`：3 个真实 PostgreSQL 测试文件 `21/21` 通过；聚焦制片与真实 PostgreSQL测试 `35/35` 通过。
- `pnpm db:migrate` 多次重复执行成功；旧 publication `legacy-sd-05` 的七类专业字段均已补齐，内置浏览器“发布记录”可正常读取 `v1 / 历史迁移 / 已送达 0 / 已确认 0`，证明旧快照仍符合当前契约。

内置浏览器、持久化与清理：

- 创建独立通告后填写全部七类专业字段并发布 `v1`；发布历史显示 `已送达 2 · 已确认 0`，完整刷新后所有字段和发布状态仍从服务端回读，PostgreSQL publication snapshot 包含完整结构化数据。
- 精确删除通告 `24cf1d8c-d2ab-47b8-9521-6ec3166f06f8`、publication `77b1bf10-57f2-47e0-a2ef-3f47c7565dfa`、2 条 recipient、2 条 notification、2 条 receipt 和 3 条 audit；七类残留计数均为 `0`，刷新后验收记录已消失。
- 浏览器逐项检查编辑弹窗后，所有非输入元素均满足 `scrollWidth <= clientWidth`，控制台无 error 或 warning；本轮未启动外部浏览器或新的持续服务，既有 Web `3211`、API `3220`、PostgreSQL `5433` 与 MinIO `9100/9101` 继续保留。

## 67. 2026-08-31 P1 Agent 创建客户审片链接

本轮将客户审片链接接入 Agent 统一命令链：

- 新增高风险命令 `create_review_link`，预览阶段在当前团队与项目范围内选择唯一、媒体就绪的视频；确认后复用既有 `ReviewLinkService.createLink()`，没有新增分享路由、状态机或权限分支。
- 执行阶段继续由 `review.manage` 权限、文件项目归属、资产 ready 状态和对象 key 做最终校验；intent 使用 `agent-<intentId>` 作为幂等键，consumed intent 重放不创建第二条链接。
- 首版沿用现有分享弹窗的 7 天有效期和默认权限：评论、版本对比、确认成片开启，原文件下载关闭；Agent intent 不接收或保存明文密码。
- Web 支持“为审片文件‘主片 v11’生成客户审片链接”，展示完整 URL 并失效当前项目的 `review-links` 查询；迁移 `042_agent_create_review_link_command.sql` 将最终 action CHECK 扩展为 19 项。
- 审片搜索框和新链接密码框补充原生自动填充语义，避免密码管理器把账号邮箱误填到搜索条件并隐藏全部审片文件。

工程与数据库验证：

- 聚焦 Agent API `50/50`、全量 API `119/119`、Web `28/28` 全部通过；新增覆盖 high-risk 预览、未确认拒绝、确认执行、consumed 重放、无效或歧义文件和执行时权限复核。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm --filter @shadowproducer/api test:postgres` 的 3 个真实 PostgreSQL 测试文件 `21/21` 通过；数据库最终 action CHECK 已验证，精确包含 19 个 action。

内置浏览器、持久化与清理：

- 为验收创建 1 秒测试视频和精确命名的临时资产/审片版本；Agent 预览返回 high-risk 确认，确认后显示客户链接，分享列表立即出现该链接，完整刷新后仍从 PostgreSQL 回读。
- 公共链接只包含所选版本；媒体元素 `readyState=4`、时长 1 秒且无错误，预签名媒体 Range 请求返回 `206 Partial Content`、`Content-Range: bytes 0-1023/41244`。
- 精确删除 link `edabfae4-5e00-4507-af86-9e1198f9600d`、intent `4145f7c8-f8fd-4fc2-8e50-303ccaac46fd`、session `5662ce52-6f19-4f48-a65c-151cd57382bf`、临时 review file/asset、审计 `1451-1456`、MinIO 对象和本地测试视频；所有残留计数均为 `0`，审计恢复 `max=84`。
- 清理后公共链接显示“审片链接无效或已失效”，审片列表恢复 5 项、搜索值保持为空。未启动外部浏览器或新的持续服务；既有 Web `3211` 与 API `3220` 按用户要求继续运行。

## 68. 2026-08-31 P1 Agent 批准审片版本

本轮将审片批准接入 Agent 统一命令链：

- 新增高风险命令 `approve_review_file`；预览阶段只接受当前项目中唯一、可批准的视频版本，并冻结 `fileId` 与 `expectedRevision`，名称选择不使用模糊包含匹配。
- 确认后直接复用 `ProductionService.approveReviewFile()`；执行时重新检查 `review.manage`、项目归属、视频类型、状态与 revision，继续使用既有事务、幂等回执和 `review-file.approved` 审计。
- `agent-<intentId>` 作为审批幂等键，consumed intent 直接重放已保存结果；没有新增审批路由、状态机或仓储分支。
- Web 支持“批准审片文件‘冬夜咖啡_主片 v12’”，展示成功结果并刷新审片文件与作品集候选；迁移 `043_agent_approve_review_file_command.sql` 将最终 action CHECK 扩展为 20 项。
- 为 `ProductionService` 的批准与撤销方法补充准确的 `CreateResult<ReviewFile>` 返回类型，避免调用方把审片文件推断为 `unknown`。

工程与数据库验证：

- 聚焦 Agent API `52/52`、全量 API `121/121`、Web `29/29` 全部通过；新增覆盖 high-risk 预览、未确认拒绝、确认执行、consumed 重放和执行时权限复核。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Next.js 16.3.2 生产构建成功。
- `pnpm --filter @shadowproducer/api test:postgres` 的 3 个真实 PostgreSQL 测试文件 `21/21` 通过；数据库最终 action CHECK 已验证，精确包含 20 个 action。

内置浏览器、持久化与清理：

- 在审片页通过 Agent 批准 `main-v12 / 冬夜咖啡_主片 v12`；高风险确认展示唯一项目和目标，执行后列表立即显示“已通过”，完整刷新后仍从 PostgreSQL 回读 `revision 2`、批准人和批准时间。
- 精确删除 intent `94e5e4bb-66d6-4889-9be7-5a3a6423016c`、审批 receipt 和审计 `1509-1512`，并恢复 `main-v12` 为 `审阅中 / revision 1 / approved_at NULL / updated_at 2026-08-27 18:15:13.605+00`。
- intent、receipt、audit 残留均为 `0`，审计恢复 `max=84`；内置浏览器再次刷新后确认 `v12` 已恢复为“审阅中”。本轮没有启动外部浏览器或新的持续服务，既有服务继续保留。

## 69. 2026-08-31 P1 Agent 创建与更新拍摄日

本轮将拍摄日创建与更新接入 Agent 统一命令链：

- 新增写命令 `create_shooting_day` 与 `update_shooting_day`；两者直接复用既有 `ProductionService` 方法，没有新增制片服务、路由、状态机或依赖。
- 更新目标只接受拍摄日 ID 或标准化后的精确标题；预览冻结 `itemId` 与 `expectedRevision`，执行时继续由既有项目归属、权限与 revision 规则阻止越权和并发覆盖。
- 创建使用 `agent-<intentId>` 作为幂等键；consumed intent 重放沿用既有 Agent 回执，不会重复创建拍摄日。
- Web 自然语言解析支持创建拍摄日的日期、日序与标题，以及按标题更新状态；执行完成后刷新当前项目拍摄日和通告数据。
- 迁移 `044_agent_shooting_day_commands.sql` 将最终 action CHECK 扩展为 22 项；数据库约束已验证精确包含全部 22 个 action。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `126/126`、Web `31/31`，Next.js 生产构建成功。
- 聚焦 Agent API `57/57`、真实 PostgreSQL `21/21` 全部通过。
- 迁移连续执行两次成功，证明迁移和确定性种子可重复执行。

内置浏览器、持久化与清理：

- 通过 Agent 创建 `P1 Agent 验收拍摄日 20260831 / 2026-12-19 / 第 919 天`，初始状态为“草稿”，随后按精确标题更新为“已确认”。
- 完整页面重进后仍从 PostgreSQL 回读状态“已确认”和 `revision 2`，证明创建与更新均已持久化。
- 精确删除拍摄日 `2c3f1b3d-9715-4f00-a42c-3d21860b4c4d`、创建 intent `4835659d-92bc-4c0-bd38-0fdaf6b34dfd`、更新 intent `38008c74-de6a-4f6a-9063-ed9ff0f17ace`、创建回执和审计 `1615-1620`。
- 拍摄日、intent、回执和审计四类验收残留均为 `0`；审计恢复 `max(id)=84`，冬夜咖啡保留 3 个种子拍摄日，浏览器重进确认临时拍摄日已消失。
- 本轮没有启动外部浏览器或新的持续服务；既有 Web `3211`、API `3220`、PostgreSQL `5433` 与 MinIO `9100/9101` 继续保留。

## 70. 2026-08-31 P1 Agent 更新专业通告表

本轮将通告表更新接入 Agent 统一命令链：

- 新增普通写命令 `update_call_sheet`；预览按通告表 ID 或 NFKC 标准化后的精确标题唯一选择目标，并冻结 `itemId` 与 `expectedRevision`，不沿用发布命令的模糊标题匹配。
- 执行直接复用既有 `ProductionService.updateCallSheet()`，完整支持基础字段和专业制片结构字段；没有新增通告服务、路由、状态机或依赖。
- 自然语言入口覆盖标题、日期、日序、状态、集合、开拍、收工、天气、日出日落、基地、场地、医院与变更说明；复杂结构字段继续通过同一 Agent 预览契约接收结构化数据。
- 共享 `ProductionService` 拒绝空更新和只有变更说明的更新，避免直接 REST 或 Agent 调用无意义地递增 revision、写审计或生成发布后变更历史。
- Web 执行完成后同时刷新通告列表和通告历史；迁移 `045_agent_update_call_sheet_command.sql` 将最终 action CHECK 扩展为 23 项，约束已验证为 `convalidated=true` 且精确包含全部 23 项。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `130/130`、Web `33/33`，Next.js 16.3.2 生产构建成功。
- 聚焦 Agent API `61/61`、制片 API `29/29`、真实 PostgreSQL `21/21` 全部通过；覆盖唯一目标、空更新、结构化专业字段、重放、执行时权限复核和预览后 revision 冲突。
- 迁移连续执行两次成功，证明迁移和确定性种子可重复执行。

内置浏览器、持久化与清理：

- 通过 Agent 创建临时通告，再按精确标题将其更新为 `P1 Agent 通告更新已验收 20260831 0945`；更新内容包含集合 `15:45`、开拍 `17:10`、收工 `23:50`、天气“小雨”、基地“B 区”、场地“旧北站 3 号站台”、医院“市二院急诊”和状态“待确认”。
- 完整页面重进后详情仍从 PostgreSQL 回读全部字段与 `revision 2`；数据库确认没有 publication、change 或 notification，符合草稿更新语义。
- 精确删除通告 `7ae4f08f-3108-4af8-a767-a29c88513e8d`、创建 intent `ed8b27d9-b87b-4bee-a1a5-309dfa3ecd54`、更新 intent `9cfe5eb4-335c-4817-bec1-c77a46842a78`、创建回执和审计 `1672-1677`。
- 通告、intent、回执和审计四类验收残留均为 `0`；审计恢复 `max(id)=84`，冬夜咖啡保留 3 张种子通告且 Agent intent 总数为 `0`。浏览器硬刷新后临时通告已消失，3 个种子拍摄日与种子通告仍正常显示。
- 本轮没有启动外部浏览器或新的持续服务；既有 Web、API、PostgreSQL 与 MinIO 按用户要求继续运行。

## 71. 2026-08-31 P1 Agent 创建与更新脚本版本

本轮将脚本版本写入接入 Agent 统一命令链：

- 新增普通写命令 `create_script_version` 与 `update_script_version`；预览阶段解析当前或指定脚本文档，并冻结 `documentId`、当前 `versionId` 与 `expectedRevision`。
- 文档标题使用 NFKC 标准化后的精确匹配；预览会拒绝只读成员，执行阶段继续复用 `ScriptService` 的项目权限、CAS 冲突、幂等回执、不可变版本历史和审计能力。
- Agent 执行成功后发布既有 `script.version.updated` SSE 事件；Web 自然语言入口支持包含分号的多行正文，并在执行后刷新当前项目的脚本工作区。
- 迁移 `046_agent_script_version_commands.sql` 将数据库 action CHECK 扩展为 25 项；没有新增脚本服务、状态机或依赖。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `133/133`、Web `35/35`，Next.js 生产构建成功。
- 聚焦 Agent API `64/64`、真实 PostgreSQL `21/21` 全部通过；覆盖创建与 SSE、幂等重放、更新、预览后 revision 冲突和只读成员拒绝。
- 迁移连续执行两次成功；数据库约束为 `convalidated=true`，精确包含 25 个 Agent action。

内置浏览器、持久化与清理：

- 通过 Agent 从 `v7 revision 4` 创建 `v8 / P1 Agent 浏览器验收 20260831 1007`，再更新至 `revision 2`；完整硬刷新后页面仍从 PostgreSQL 回读 `v8` 和最终多行正文。
- 精确删除创建 intent `7e699aac-902d-4f0a-a78f-a72afc686ff6`、更新 intent `e4b6b8cc-6a66-4d56-9854-cf449d2cae7f`、两条脚本写回执、版本 `v8` 和审计 `1729-1734`。首次清理事务触发单当前版本唯一约束并完整回滚；调整更新顺序后，带前后置断言的事务成功提交。
- 清理后版本数恢复为 `3`、项目 Agent intent 为 `0`、审计恢复 `max(id)=84`；内置浏览器再次硬刷新后确认 `v7 revision 4`、原始正文与两条评论均已恢复。
- 本轮没有启动外部浏览器或新的持续服务；既有 Web `3211`、API `3220`、PostgreSQL `5433` 与 MinIO `9100/9101` 按用户要求继续运行。

## 72. 2026-08-31 P1 脚本拆解异步分析闭环

本轮完成脚本拆解首个真实异步分析工具 `deterministic_rules_v1`：

- 创建任务时冻结项目、脚本文档、不可变版本、`sourceRevision` 与完整正文快照；任务状态覆盖 `queued / processing / awaiting_confirmation / completed / failed / cancelled`。
- 独立 Analysis Worker 使用 PostgreSQL claim/lease、`FOR UPDATE SKIP LOCKED` 与 revision CAS；候选写入既有 `breakdown_items` 并固定为“待确认”，不会伪装成模型推理结果。
- 创建、取消和失败重试均使用幂等回执、项目权限与审计；最后一个候选处理后由数据库触发器将任务转为 `completed`。
- 制片拆解页可选择脚本文档与不可变版本，查看来源、状态、候选数、尝试次数和失败阶段，并支持取消与失败重试；活跃任务轮询后自动刷新拆解表。
- 认证 `preHandler` 已位于全部 `/v1` 路由之前，新分析路由不会绕过正式会话解析。

工程与数据库验证：

- API `134/134`、Web `35/35`、真实 PostgreSQL `25/25` 全部通过；`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过。
- `047_analysis_jobs.sql` 连续执行通过；真实 PostgreSQL 覆盖正文快照、幂等回放与键冲突、权限拒绝、claim/lease、取消、手动重试、候选去重和全部确认后完成。
- 本轮浏览器创建任务 `04f7bff0-7f39-40d7-9d21-dba7863cdf15`，从 `冬夜咖啡_拍摄稿 / v7 / revision 4` 产出 5 个候选；数据库来源精确为第 1、5、7、11 行，包含旧站台、深红色硬壳行李箱、顾遥、行李箱和积水。

内置浏览器、异常路径与清理：

- 内置浏览器观察到任务从排队/处理进入“待确认”，完整硬刷新后任务和候选仍由服务端回读；`顾遥` 候选来源列显示 `v7 · 第 7 行`，数据库摘录为原文 `顾遥`。
- 在同一浏览器调用中创建并立即取消任务 `2c3a7464-c4fb-4bc0-9b19-0408791430a6`，页面显示“已取消”；随后将该独立验收任务精确置为受控 `failed/extract` 状态，页面显示失败阶段与错误，点击重试后 Worker 再次产出 5 个候选并进入“待确认”。
- Kasidia 检查确认桌面弹窗在 `1329x912` 视口居中，背景滚动锁定，关闭后焦点返回“分析脚本”；弹窗与遮罩使用共享 Dialog 的成对进入/退出动画，界面保持零圆角、低饱和大面积背景和既有图标契约。本轮未在内置浏览器中获得可变视口能力，移动端专项视觉复核仍需后续补充。
- 精确删除 3 个 job、15 条候选、5 条 command receipt 和 8 条 audit；清理后 `analysis_jobs=0`、`breakdown_items=6`、分析关联候选/回执/审计均为 `0`、审计恢复 `max(id)=84`，浏览器重载后显示 6 个种子拆解项与“0 项分析任务”。
- 本轮新增并有意保留 Analysis Worker：根 PID `23812`、pnpm 子进程 `13468`、实际 Worker PID `20444`；既有 Web `3211`/PID `27220` 与 API `3220`/PID `13300` 健康，未启动外部浏览器。

## 73. 2026-08-31 P1 项目级脚本自动分析工作流

本轮将脚本保存与既有异步拆解 Worker 接成可配置的项目级工作流：

- 新增 `048_analysis_workflows.sql`：项目可启停“保存后自动分析”，范围固定为全部脚本文档或一个指定脚本文档，trigger 固定为 `script_version_updated`，approval 固定为 `manual_confirmation`。
- 手动分析任务明确记录 `trigger_kind=manual`；自动任务使用来源项目、文档、版本、revision、工具的 PostgreSQL 部分唯一索引防重。
- `PostgresScriptRepository.updateVersion()` 在脚本 CAS 更新、写回执与审计的同一事务中读取 workflow 并创建自动任务；重放、关闭配置、非目标文档和非脚本文档均不会创建任务。
- 配置读取允许项目只读成员，修改要求 `production.write`；首次保存使用 revision `0 -> 1`，后续更新使用 CAS、幂等回执与 `analysis.workflow.updated` 审计。
- 制片拆解现有弹窗加入零圆角 Checkbox、范围 Dropdown 与保存状态；没有新增设置页、队列、调度器、模型或前端补偿触发器。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `134/134`、Web `35/35`、真实 PostgreSQL `29/29`，Next.js 16.3.2 生产构建成功。
- 新增聚焦测试覆盖 workflow 默认值、幂等、CAS、viewer 只读/写拒绝、全部脚本、指定文档、关闭不触发、跨项目文档外键拒绝，以及脚本正文与 revision 冻结；聚焦两文件 `18/18` 通过。
- `pnpm db:migrate` 连续执行两次成功；重复迁移后仍为 `analysis_workflows=0`、`analysis_jobs=0`、`breakdown_items=6`、`v7 revision=4`。

内置浏览器、Worker 与精确清理：

- 在内置浏览器把范围设为 `冬夜咖啡_拍摄稿` 并启用；保存将 `v7 revision 4 -> 5`，同事务创建自动任务 `a35e5030-895f-4bd8-b916-6bf6944096d6`，Worker 进入 `awaiting_confirmation` 并产出 5 条“待确认”候选。
- 页面回读显示任务来源 `v7 · revision 5`、候选 `5`、尝试 `1/3`；关闭 workflow 后再次保存到 revision `6`，任务总数仍为 `1`，证明页面滚动/保存与自动触发没有前端补偿重复。
- 精确删除该 job、5 条候选、2 条 workflow 回执、2 条脚本写回执和审计 `1932-1937`，删除临时 workflow，并恢复 `v7` 正文、revision `4`、原始时间戳与审计序列验收前值 `1931`。
- 清理后数据库为 `workflow=0`、`jobs=0`、分析关联候选/回执/审计均为 `0`、种子拆解 `6`、脚本回执 `3`、审计 `max(id)=84`；浏览器硬刷新后显示原始正文、revision `4`、每类 1 条种子项与“0 项分析任务”。
- 未启动外部浏览器或新的持续服务；既有 Web `3211`/PID `27220`、API `3220`/当前运行 PID `25228`、Analysis Worker 根 `23812` / pnpm `13468` / 进程 `20444` 均健康并继续保留。

## 74. 2026-08-31 P1 Agent 发起脚本拆解分析

本轮把既有脚本拆解分析能力接入统一 Agent 命令链：

- 新增普通写命令 `create_script_breakdown_analysis`；预览阶段按脚本文档 ID、精确标题或默认文档选择来源，并冻结 `documentId`、`versionId` 与 `sourceRevision`。
- 执行阶段直接复用 `AnalysisService.createScriptBreakdown()`，继续使用既有项目写权限、脚本文档类型、版本 revision、空正文、幂等回执与审计约束；没有新增队列、Worker、状态机或模型。
- Agent 以 `agent-<intentId>` 作为分析任务幂等键；consumed intent 重放不会重复创建任务，分析候选仍固定进入“待确认”。
- Web 自然语言入口支持“分析脚本文档……生成制片拆解”和可选版本 ID；执行后刷新当前项目分析任务，并明确提示结果需人工确认。
- 迁移 `049_agent_script_analysis_command.sql` 将最终 action CHECK 扩展为 26 项；约束为 `convalidated=true`，并精确包含 `create_script_breakdown_analysis`。

工程与数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `135/135`、Web `36/36`，Next.js 16.3.2 生产构建成功。
- Agent API 聚焦测试 `1/1`、真实 PostgreSQL `29/29` 全部通过；新增覆盖冻结来源、执行、consumed 重放、只读成员拒绝和自然语言解析。
- `pnpm db:migrate` 连续执行两次成功。真实 PostgreSQL 测试期间精确暂停常驻 Analysis Worker，避免共享开发数据库中的队列领取竞争，测试后按原命令恢复。

内置浏览器、Worker 与精确清理：

- 在内置浏览器输入“分析脚本文档‘冬夜咖啡_拍摄稿’生成制片拆解；版本ID：v7”，Agent 创建 intent `68de79f6-143a-40bd-a203-c89584133cea` 和任务 `ebb18ac5-4c71-49f9-933c-24d5ab81562f`。
- Worker 将任务从 `queued` 处理到 `awaiting_confirmation`，来源精确为 `winter-coffee-shooting-script / v7 / revision 4`，产出 5 条候选；完整硬刷新后分析弹窗仍显示“待确认 / 候选 5 / 尝试 1/3”。
- 带前后断言的事务精确删除 5 条候选、1 个任务、1 条分析回执、1 个 intent 和审计 `2182-2185`，并恢复验收前审计序列值 `2181`。
- 清理后 `analysis_workflows=0`、`analysis_jobs=0`、分析 Agent intent/回执/审计均为 `0`、种子拆解 `6`、`v7 revision=4`、审计 `max(id)=84`；浏览器硬刷新后显示“0 项 / 暂无分析任务”。
- 未启动外部浏览器。Web `3211`/PID `27220` 与 API `3220`/PID `40260` 返回 `200`；Analysis Worker 根 PID `39484`、pnpm PID `28548`、实际 Worker PID `25088` 继续保留。

## 75. 2026-08-31 P1 Agent 补齐制片拆解专业关系

本轮复用既有 `ProductionService.updateBreakdown()`，把拆解项的专业关系接入统一 Agent 命令链：

- `update_breakdown` 现支持供应商、负责人、任务、团队联系人、成员共享联系人、拍摄日和通告单；预览阶段继续冻结拆解项 ID 与 revision，执行阶段沿用既有项目权限、已确认状态、跨项目关系校验和事务写入。
- Web 自然语言入口使用明确的 ID 标签，支持逗号、顿号或空格分隔；数组字段可用“清空 / 无 / 取消”清除，负责人使用相同语义解除关联。
- 契约直接复用 `UpdateBreakdownItemBodySchema` 的限制；没有新增 action、迁移、路由、服务、依赖或状态机。

工程验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `136/136`、Web `37/37`，Next.js 16.3.2 生产构建成功。
- 新增 Web 解析测试覆盖六类关系、团队/共享联系人来源和清空语义；Agent API 集成测试证明预览冻结后的关系字段完整进入执行结果，revision 从 `1 -> 2` 且只写入一次。

内置浏览器、PostgreSQL 与精确清理：

- 在内置浏览器先通过高风险确认流程将 `br6 / 人工雨与现场安全` 正式确认，再由 Agent 关联 `supplier-rain-fx`、`account-fanxing`、`task-confirm-cast`、两类联系人、`shoot-day-05` 与 `sd-05`。
- PostgreSQL 回读显示 `br6` revision 从 `1 -> 3`，六类关系均精确落库；完整刷新并重新选择“特殊执行与保障”后，页面从服务端显示远景现场特效、繁星、远景雨效/林乔、任务、第 5 拍摄日和车站夜戏通告，浏览器控制台无 error/warn。
- 精确删除两个 intent、确认回执、审计 `2182-2188` 和 `br6` 的验收关系，并恢复 `state=待确认`、`revision=1`、原始时间戳及审计序列 `2181`。
- 清理后六类关系计数均为 `0`、Agent intent 为 `0`、既有回执仍为 `3`、审计恢复 `max(id)=84 / count=46`；浏览器刷新后重新显示“未关联 / 待确认”。
- 未启动外部浏览器或新持续服务。Web `3211`/PID `27220`、API `3220`/PID `38580` 与 Analysis Worker 根 PID `39484`、pnpm PID `28548`、实际 Worker PID `25088` 均健康并继续保留。

## 76. 2026-08-31 P1 客户审片邮箱身份校验

本轮在既有免注册客户审片链接上增加两步邮箱身份校验：

- 新增 `POST /review/:token/identity-challenges` 与 `POST /review/:token/session` 两步流程；客户先提交姓名、邮箱和链接密码，再使用邮箱验证码建立既有 HttpOnly 审片 session。
- 验证码只以 HMAC 哈希持久化，10 分钟有效、60 秒内禁止重复发送、最多允许 5 次错误；验证成功后一次性消费，并在 session 记录 `verified_email` 与 `identity_verified_at`。
- 本地开发响应可返回 `developmentCode`；生产只通过 `REVIEW_IDENTITY_WEBHOOK_URL` 投递，未配置时返回 `503`，不会绕过校验。
- 沿用现有客户审片 session、评论、批准、撤销和审计链，没有创建客户账号体系或第二套授权模型；UI 调整为“身份信息 -> 验证码 -> 审片”的两步入口。

工程、PostgreSQL 与浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `137/137`、Web `37/37`，新增真实 PostgreSQL 身份事务测试通过。
- 内置浏览器完成 1 秒视频上传、审片版本与分享链接创建、错误验证码拒绝、正确验证码进入审片、掩码邮箱显示、客户评论创建以及硬刷新后的 PostgreSQL 回读；控制台无 error 或 warning。
- 精确删除验收链接、challenge、session、评论、临时 review file/asset、MinIO 对象和本地视频；清理后 `review_identity_challenges=0`、`review_sessions=0`、`team_assets=7`、`review_files=5`、`command_receipts=3`、`audit_logs=46 / max(id)=84`，审计序列恢复为 `2181`。
- 当前 Web `3211`/PID `27220`、API `3220`/PID `38352`、PostgreSQL `5433`、MinIO `9100/9101` 与既有 Analysis Worker 均健康并按用户要求继续保留；本轮没有启动额外持续进程。

## 77. 2026-08-31 P1 审片反馈智能合并

本轮把既有跨版本意见对应升级为可直接工作的合并视图：

- 继续以 `review_comment_links` 作为唯一、持久、可审计且可撤销的合并关系；没有新增表、API、状态机或依赖，原评论内容、状态和 revision 均不被合并操作改写。
- A/B 对比开启后默认进入“合并视图”；已确认关系折成一组反馈，保留 A/B 版本、作者、时间码和原文来源，未配对的 B 版意见仍作为独立反馈显示。
- 既有时间码与文本评分候选改为明确的“智能建议”并显示置信度；确认后显示“已合并”，撤销后建议立即恢复。“版本 A”分段模式仍保留原始单版本列表。
- 合并视图中的未解决/我的意见筛选按整组来源计算；同时修正 B 版意见状态更新后的 React Query 缓存归属，避免把 B 版结果写入 A 版缓存。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `137/137`、Web `37/37`，聚焦真实 PostgreSQL 跨版本关系测试 `1/1` 通过。
- 内置浏览器创建临时 v11 意见；因种子 v11 无可用媒体、时间线不可操作，验收夹具在 PostgreSQL 精确调整为 `00:07.360`，随后页面对 v12 `c1 / 00:07.120` 给出 `83%` 智能建议。
- 在浏览器确认后页面显示 `1 组已合并`，硬刷新仍从 PostgreSQL 回读；数据库确认 link 持久化、原两条评论均保持 `revision=1`。撤销后建议恢复，证明合并可逆。
- Kasidia 检查确认桌面 `1329x912` 下右侧栏稳定为 `320px`、分段控件 `319px`，页面、主区、侧栏和分段控件均无横向溢出；共享 Tabs、IconButton、语义色与成对侧栏动画保持既有契约，浏览器无 error 或 warning。
- 精确删除 comment `8fb24c4c-8464-4120-9678-98f3063101d5`、link `13924107-db6d-47fd-aa20-0297e0b09641`、对应回执与审计 `2182-2184`，并恢复审计序列 `2181`。清理后 `review_comments=4`、`review_comment_links=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`；浏览器刷新后验收文字、建议和合并关系均不存在。
- 本轮未启动外部浏览器或新的持续服务；Web `3211`/PID `27220`、API `3220`/PID `38352`、PostgreSQL、MinIO 与既有 Analysis Worker 按用户要求继续保留。

## 78. 2026-08-31 P1 Agent 创建审片版本

本轮把既有审片版本创建能力接入统一 Agent 命令链：

- 新增普通写命令 `create_review_file`，预览阶段冻结项目、视频素材 ID、名称、版本和时长，执行阶段直接复用 `ProductionService.createReviewFile()`。
- 继续沿用既有项目写权限、ready 视频素材、对象键、归档状态、单素材单审片版本、幂等回执和审计约束；没有新增服务、状态机或依赖。
- Web 自然语言入口支持素材 ID、名称、版本和时长；执行后刷新当前项目审片文件列表，并回显创建结果。
- 迁移 `051_agent_create_review_file_command.sql` 扩展 Agent action CHECK；约束保持已验证状态。

工程、API 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `139/139`、Web `39/39`，Next.js 生产构建成功。
- Agent API 测试覆盖创建、consumed intent 重放和 viewer 执行拒绝；Web 测试覆盖自然语言解析。
- 内置浏览器输入“创建审片版本；素材ID：p1-agent-review-file-20260831-a；名称：P1 Agent审片版本验收-20260831；版本：v51；时长：00:01”，列表从 5 项变为 6 项；完整硬刷新后仍从 PostgreSQL 回读 `待审阅 / v51 / 00:01`。
- 实际打开验收视频成功，浏览器回读 `readyState=4`、`duration=1`、`640x360`，并可播放至结尾；重复使用同一素材时明确返回“所选视频素材已经创建过审片版本”。

精确清理与运行态：

- 精确删除 review file `bdbad48b-8e33-44f4-ba6f-3653da3e6db9`、两个 intent、审计 `2188-2191`、素材 `p1-agent-review-file-20260831-a`、MinIO 对象 `teams/north/acceptance/p1-agent-review-file-20260831.mp4` 与本地验收视频。
- 清理后 `review_files=5`、`agent_command_intents=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`，审计序列恢复为 `2187`；浏览器硬刷新后恢复 5 项且验收条目不存在，console 无 error 或 warning。
- 一次 PowerShell replay 探测因错误 `Content-Type` 返回 `415`，随后因开发身份头未启用返回 `401`；服务日志确认不是产品反序列化故障，且两次请求均未产生数据。
- 本轮未启动外部浏览器或新的持续服务；既有 Web `3211`、API `3220`、PostgreSQL `5433`、MinIO `9100/9101` 与 Analysis Worker 继续保留。

## 79. 2026-08-31 P1 脚本实时协作可靠层

本轮在既有整篇保存与乐观锁之上补齐多实例实时可靠性：

- 复用 `audit_logs.id` 作为持久 SSE 游标，事件输出原生 `id:` 并支持 `Last-Event-ID` 断线回放；没有新增重复事件表。
- API 实例每秒从 PostgreSQL 增量读取脚本事件，使一个实例完成的写入可刷新连接到另一实例的协作者；仅当活跃协作查询产生可测压力时再升级 `LISTEN/NOTIFY`。
- presence 改为 PostgreSQL 共享状态，由服务端心跳维持并按 45 秒 TTL 过滤失效协作者；断开不会立即删除同一账号的其他标签状态。
- 保留既有整篇自动保存、revision CAS、幂等回执与三方合并；没有引入 CRDT、Redis 或新依赖。

工程与 PostgreSQL 验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `140/140`、Web `39/39`，真实 PostgreSQL `31/31`。
- 聚焦 API 测试 `69/69`、脚本 PostgreSQL 测试 `14/14` 通过；覆盖 SSE 事件 ID、游标回放、跨实例轮询和共享 presence TTL。
- `pnpm db:migrate` 连续执行两次成功；测试发现 PostgreSQL 审计 ID 实际回读为字符串后，已在实时 store 边界显式归一化为 number 并复测通过。

内置浏览器、持久化与精确清理：

- 两个内置浏览器标签同时打开 `冬夜咖啡_拍摄稿 / v7 revision 4`，均显示“实时已连接”。A 标签写入唯一验收标记并自动保存至 revision `5`，B 标签无需刷新即收到同一正文与 revision `5`。
- B 标签完整重载后仍从 PostgreSQL 回读验收正文、revision `5` 和在线状态；两个标签控制台均无 error 或 warning。
- 事务精确恢复 `v7` 原正文、revision `4`、原始更新时间与 MD5 `c0cf77f0967a5af1e0ec478da39c2567`，删除唯一脚本回执、审计 `2326` 与 presence，并恢复审计序列 `2325`。
- 清理后 `audit_logs=46 / max(id)=84`、验收回执 `0`、presence `0`；浏览器重载确认原正文完全一致、验收标记不存在、revision `4` 与实时连接正常。第二验收标签已关闭，原标签返回资源库。
- 本轮未启动外部浏览器或新的持续服务；既有 Web `3211`、API `3220`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker 与 Media Worker 继续保留。

## 80. 2026-08-31 P1 持久化分镜 Layout

本轮把分镜卡片、表格和 Layout 从组件内演示数据接入既有脚本文档协作链：

- 复用 `script_documents(type=storyboard)`、当前 `script_versions.content`、revision CAS、幂等回执、审计和 SSE；没有新增表、路由、依赖、CRDT 或第二套事件系统。
- 新增版本化分镜 JSON 契约与 5 镜头 fixture；`winter-coffee-storyboard / v4` 作为正式种子文档，三种视图共享同一份镜头、选中态、时长和备注数据。
- Inspector 支持镜头字段与时长编辑，650ms 自动保存；保存冲突会重新载入，普通失败可重试，页面覆盖加载、空态、非法内容、只读权限和新建分镜文档。
- Layout 支持播放、暂停、前后镜头、播放头和节奏时间线；修改镜头时长会立即重算后续镜头起止点和总时长。播放到总时长时固定停在最后镜头，不再回落到旧选中镜头。
- 服务端在更新或创建 storyboard 版本前使用权威 schema 校验 JSON、字段边界、未知字段和镜头 ID 唯一性；新建 storyboard 直接保存合法空结构，非法内容在写回执、审计和正文前返回 `STORYBOARD_CONTENT_INVALID`。

工程、数据库与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `140/140`、Web `42/42`、真实 PostgreSQL `32/32`，Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 连续执行两次成功；种子基线为 `v4 revision 1`、5 镜头、22 秒、正文 MD5 `fe9fced97e2b53177c43263f6a670a02`。
- 两个内置浏览器标签均显示“实时已连接”。A 标签把 `12-01` 从 `4 -> 4.5` 秒并写入唯一备注后完成一次自动保存；B 标签无需刷新即回读相同字段和总时长 `22.5` 秒，完整重载后数据仍由 PostgreSQL 回读。
- Layout 后续起点重算为 `00:04.5 / 00:09.5 / 00:12.5 / 00:16.5`；前后跳转、播放、暂停均可用，最后镜头播放结束显示 `镜内 00:06.0 / 00:06.0` 和 `00:22.5 / 00:22.5`。两个标签控制台均无 error 或 warning。

精确清理与运行态：

- 精确恢复 `v4` 原正文、revision `1`、原始更新时间和 MD5，删除唯一写回执、审计 `2432` 与 presence，并把审计序列恢复到验收前 `2325`。
- 清理后 storyboard 回执/审计/presence 均为 `0`，`audit_logs=46 / max(id)=84`；数据库与浏览器重载均确认总时长 `22` 秒、首镜 `4` 秒、原备注完全一致且验收标记不存在。第二验收标签已关闭，主标签返回资源库。
- 本轮未启动外部浏览器或新的持续服务；Web `3211`/PID `27220`、API `3220`/当前 PID `1736`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker PID `25088` 与 Media Worker PID `14476` 均健康并继续保留。

## 81. 2026-08-31 P1 资源库媒体镜头分析

本轮在既有资产、对象存储和 Media Worker 上补齐首个真实媒体分析闭环：

- 新增独立 `media_analysis_jobs` 与 `media_analysis_shots`，没有污染脚本拆解任务或基础媒体处理表；任务持久化来源资产名、SHA-256、revision、触发方式、工具、尝试次数、失败阶段和人工确认状态。
- 创建、重试和确认沿用 `team.read` / `asset.write`、幂等回执、revision CAS 与审计；Worker 领取任务后重新校验写权限，并同时核对 object key、SHA-256 与资产 revision，源快照变化时转为 `expired`。
- FFmpeg 真实执行场景检测并为每个镜头生成独立关键帧；分析失败会精确删除本次已上传的派生对象，避免自动重试遗留孤立关键帧。当前机器未接入 ASR/OCR 运行时，契约和 UI 明确显示 `unavailable`，没有生成伪结果。
- 资源库 Inspector 支持创建、轮询、失败重试、关键帧列表、点击时间码播放、刷新回读和人工确认；右键菜单提供同一分析入口。

工程、数据库与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `143/143`、Web `42/42`、真实 PostgreSQL `32/32`，聚焦媒体测试 `13/13`，Next.js 16.3.2 生产构建成功。
- `pnpm db:migrate` 第二次执行成功。内置浏览器上传 4.8 秒、640x360、25fps 的真实 H.264 样本，基础 Worker 回写元数据、缩略图和审阅代理后，“开始分析”才可用。
- 分析任务实际经历 `queued -> processing -> awaiting_confirmation -> completed`；PostgreSQL 回读 3 个镜头、3 个独立关键帧，确认后全部为 `confirmed`。点击镜头 2 以 `#t=1.700000` 打开原生视频并从该时间码播放；完整刷新后仍从 PostgreSQL/MinIO 回读候选和关键帧。
- 样本包含四段纯色，但 FFmpeg `scene` 只检测到 `1.2s`、`2.4s` 两个边界，未识别 `3.6s` 的蓝到绿变化，因此真实结果为 3 镜头。当前把它记录为 `ffmpeg_scene_v1` 的检测召回率边界，不伪造第四个切点；需要更高召回率时再引入组合检测或可调策略。

精确清理与运行态：

- 精确删除资产 `1277487d-7893-449d-905e-11dca8a68d34`、任务 `72c66d9a-8390-488e-8ea1-7123893e665b`、3 个镜头、4 条回执、审计 `2326-2331`、6 个 MinIO 对象和本地验收目录，并恢复审计序列 `2325`。
- 清理后 `team_assets=7`、`asset_media=0`、`media_processing_jobs=0`、`media_analysis_jobs=0`、`media_analysis_shots=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`，MinIO bucket 为 `0` 对象；浏览器刷新恢复“全部素材 7”，验收文件不存在。
- Web `3211`/PID `27220`、API `3220`/PID `28304`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker PID `25088` 与已加载最新代码的 Media Worker 根 PID `10776` / 实际 PID `18532` 均继续保留。Media Worker 日志位于 `output/p1-media-worker-v2.stdout.log` 与 `output/p1-media-worker-v2.stderr.log`，停止时只应终止该记录进程树。

## 82. 2026-08-31 P1 分镜画面资产关联

本轮把分镜的统一演示图替换为真实、持久的资源库画面关联：

- `StoryboardShot` 增加可选 `assetId`，继续使用 `schemaVersion: 1`；旧分镜无需迁移，解析器仍拒绝未知字段、空 ID、非字符串 ID 和超长 ID。
- 画面选择器直接复用现有 `Dialog + Command`，只列出当前项目或团队公共范围内已 ready 的图片，支持名称、项目和标签搜索、加载/失败/空态、当前选中态与解除关联。
- 卡片、表格、Inspector 和 Layout 从同一 `assetId` 解析 `TeamAsset`；无关联时显示明确空态，资源不可访问或已失效时显示不可用状态，不再暗中回退到统一咖啡图。
- 图片读取继续经过既有团队资源 API 与 Cookie 会话；分镜可写权限只控制关联修改，资源读取仍由既有 `team.read` 独立校验。没有新增表、路由、通用选择器框架、依赖或时间码字段。

工程、数据库与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `143/143`、Web `42/42`、真实 PostgreSQL `32/32`，Next.js 16.3.2 生产构建成功。
- Web 解析测试覆盖 `assetId` 往返、空字符串和非字符串拒绝；契约与 Web 类型检查同时通过。
- 内置浏览器将 ready 项目图片 `asset-station-scout-01 / 车站勘景参考 01` 关联到 `12-01`；卡片、表格、Inspector 和 Layout 均显示同一 `640x360` 画面，完整刷新后仍从 PostgreSQL 回读 `assetId`。
- 初次验收发现首张关联画面触发 Next.js LCP lazy-load 警告；仅把第一张分镜卡片设为 eager 后复测，浏览器控制台无新增 error 或 warning，其余图片继续延迟加载。

精确清理与运行态：

- 两轮验收均精确恢复 `v4` 原正文、revision `1`、原始更新时间与 MD5 `fe9fced97e2b53177c43263f6a670a02`，删除对应写回执与审计 `2326 / 2401`，并把 PostgreSQL 测试推进的审计序列恢复为 `2325`。
- 清理后 `team_assets=7`、`media_analysis_jobs=0`、`script_write_receipts=3`、`audit_logs=46 / max(id)=84`；浏览器重载显示 5 个镜头均为“未关联画面”，验收关联不存在。
- 本轮没有上传临时文件、启动外部浏览器或创建新的持续进程；Web `3211`/PID `27220`、API `3220`/PID `1728`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker PID `25088` 与 Media Worker 根 PID `10776` / 实际 PID `18532` 均健康并继续保留。

## 83. 2026-08-31 P1 按时间码持久截帧

本轮在既有媒体分析任务上补齐指定时间码截帧，没有新增第二套任务或对象存储链：

- `frame_capture / ffmpeg_frame_v1` 复用媒体分析任务、来源快照校验、权限、幂等回执、重试、审计、Worker 领取和 MinIO 派生对象；请求持久化 `requested_timecode_us`，截帧成功后直接写入一个 `confirmed` 镜头并完成任务。
- 资源库 Inspector 使用 `00:00.000` 输入，提供提交、等待/失败/完成、重试、持久历史、缩略图、点击原视频时间码播放和下载；等于或超过视频时长的请求在 UI 与服务边界拒绝。
- 首轮真实下载验收发现跨域预签名地址会让浏览器忽略 HTML `download` 并离开工作区；下载链接现追加受契约校验的 `download=1`，S3 预签名仅在该分支设置 `Content-Disposition: attachment`，预览 URL 保持内联显示。

工程、数据库与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `143/143`、Web `42/42`、真实 PostgreSQL `32/32`，Next.js 16.3.2 生产构建成功；下载附件分支另有 API 回归断言。
- `054_frame_capture.sql` 连续执行两次成功。内置浏览器上传真实 H.264 样本 `p1-frame-capture-20260831.mp4`，基础媒体回读为 `640x360 / 4 秒 / 25fps / h264`。
- 在 `00:01.250` 提交后，UI 先显示等待状态并在一次 Worker 领取后完成；任务 `f37ae212-9902-42c9-883e-5f93552070e8` 最终为 `completed / attempts 1 / revision 2`，镜头 `67c0aedb-e84c-40e7-960c-2c90fb29ec95` 为 `1250000-1250001us / confirmed`。
- PostgreSQL 回读 `requested_timecode_us=1250000`，MinIO 对象 `teams/north/derivatives/c86c28d9-1fc9-46bd-9541-0d1dd376e856/analysis/f37ae212-9902-42c9-883e-5f93552070e8/shot-0001.jpg` 为 20 KiB JPEG；浏览器实际加载为 `640x360`，点击后视频从 `1.25s` 开始播放。
- 完整刷新后仍回读 `00:01.250 / 截帧完成` 和缩略图。修复后的下载点击保持在资源库 URL 并触发附件响应；`00:04.000` 被显示为“请输入视频时长内的时间码”，未创建第二个任务。最终页面与 Inspector 横向溢出均为 0。

精确清理与运行态：

- 精确删除资产 `c86c28d9-1fc9-46bd-9541-0d1dd376e856`、任务 `f37ae212-9902-42c9-883e-5f93552070e8`、镜头 `67c0aedb-e84c-40e7-960c-2c90fb29ec95`、3 条回执、审计 `2400-2404`、4 个 MinIO 对象和本地验收目录，并把审计序列恢复为 `2325`。
- 清理后 `team_assets=7`、`asset_media=0`、`media_processing_jobs=0`、`media_analysis_jobs=0`、`media_analysis_shots=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`，MinIO bucket 为 `0` 对象；浏览器刷新显示“全部素材 7”，验收文件不存在，最终控制台无 error 或 warning。
- 本轮未启动外部浏览器或新的持续服务；Web `3211`/PID `27220`、API `3220`/PID `38672`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker 根 PID `30440` / 实际 PID `25088` 与 Media Worker 根 PID `34604` / 实际 PID `12312` 均健康并继续保留。

## 84. 2026-08-31 P1 自动分析审批规则

本轮为项目级脚本自动分析工作流补齐可配置审批规则：

- `manual_confirmation` 保持全部候选人工确认；`confidence_threshold` 按 `0-100` 阈值自动确认高置信度候选，低于阈值的候选继续等待人工处理。
- 自动任务创建时冻结审批策略和阈值，后续修改工作流不会改变已有任务；手动分析始终保持人工确认。
- 复用既有 workflow、analysis job、拆解候选、revision CAS、权限、幂等回执和审计，没有新增规则引擎或依赖。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过；API `143/143`、Web `42/42`、真实 PostgreSQL `33/33`；迁移 `055_analysis_workflow_approvals.sql` 连续执行两次成功。
- 首次真实验收发现常驻 Analysis Worker 仍加载旧代码：任务虽冻结 `confidence_threshold / 96`，6 条候选仍全部待确认。精确停止旧进程树 `30440 -> 25088` 后，以当前源码重启根 PID `20392` / 实际 PID `35076`，没有触碰 Web、API 或 Media Worker。
- 新任务 `516c4650-8609-46f1-8f73-46bd87155b87` 回读 `awaiting_confirmation / revision 3 / candidate_count 6`；置信度 `98/98/96/96` 的 4 条候选自动确认为已确认，`95/94` 的 2 条候选保持待确认。
- 浏览器硬刷新后仍回读“保存后自动分析 / 全部脚本文档 / 高置信度自动确认 / 96”，任务显示 `v7 revision 6 / 候选 6 / 自动确认 >= 96%`；弹窗无横向溢出，关闭后焦点恢复到“分析脚本”，刷新后的控制台无新增 error 或 warning。

精确清理与运行态：

- 精确删除两轮验收任务、12 条候选、workflow、2 条脚本写回执、1 条 workflow 回执和审计 `2555-2561`，恢复 `v7` 原正文、revision `4`、原始更新时间，并把审计序列恢复为 `2554`。
- 清理后 `analysis_workflows=0`、`analysis_jobs=0`、`breakdown_items=6`、`command_receipts=3`、`audit_logs=46 / max(id)=84`、`script_write_receipts(v7)=3`；浏览器刷新显示自动分析关闭且任务为 `0`。
- 临时视觉截图已删除。Web `3211`/PID `27220`、API `3220`/PID `40828`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker 根 PID `20392` / 实际 PID `35076` 与 Media Worker 根 PID `34604` / 实际 PID `12312` 按用户要求继续保留。

## 85. 2026-08-31 P1 持久化审片文件夹

本轮把审片资源管理器中的演示文件夹替换为项目级持久化文件夹：

- 新增 `review_folders` 与 `review_files.folder_id`；审片文件夹按项目隔离，不复用团队资源库的素材文件夹，避免混淆作用域、权限和业务语义。
- 文件夹创建和文件移动统一使用 `review.manage`，支持移动到指定文件夹或根目录，并沿用 revision CAS、幂等回执和审计。
- 文件夹保持一级结构；列表和网格视图共享同一份服务端数据，支持文件夹计数、新建、空态和移动菜单，没有增加文件系统抽象或第二套资源模型。
- 迁移 `056_review_folders.sql` 将旧 `review_files.type='folder'` 种子安全转换为真实文件夹，并把审片文件类型约束收紧为视频；迁移可重复执行。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过；API `144/144`、Web `42/42`、真实 PostgreSQL `34/34`，聚焦 production API `30/30`、workspace PostgreSQL `7/7`。
- `056_review_folders.sql` 连续执行两次成功。API 与 PostgreSQL 测试覆盖创建、同名冲突、幂等重放、跨项目拒绝、revision 冲突、移动到文件夹及根目录、权限和审计。
- 内置浏览器创建临时文件夹，将 `社交媒体竖版` 移入后完整硬刷新，仍从 PostgreSQL 回读；随后移回根目录，列表与网格视图均正确，横向溢出为 `0`，页面无运行时错误文本。
- 浏览器验收发现移动完成后按钮仍保持禁用；修正 mutation 完成态后重新验收通过。

精确清理与运行态：

- 精确删除临时文件夹、3 条命令回执与审计 `2603-2605`，并恢复 `social.folder_id=NULL`、revision `1`、原始更新时间和审计序列 `2554`。
- 清理后 `review_folders=2`、`review_files=3`、`command_receipts=3`、`audit_logs=46 / max(id)=84`；既有两个项目文件夹和三个审片视频均保留。
- 本轮没有启动新的持续进程；既有 Web、API、PostgreSQL、MinIO、Analysis Worker 与 Media Worker 按用户要求继续保留。

## 86. 2026-08-31 P1 团队素材回收站

本轮复用素材既有 `archived_at` 与 revision 乐观锁，补齐团队素材归档后的查看与恢复闭环：

- 素材列表支持活动/已归档范围；资源库新增“回收站”入口、加载/错误/空态以及右键菜单和 Inspector 恢复操作，没有新增删除框架、迁移或依赖。
- 恢复只更新仍处于归档状态且 revision 匹配的素材；过期 revision 返回冲突，重复恢复返回 `ASSET_NOT_ARCHIVED`，不会覆盖并发修改。
- 恢复继续使用既有 `asset.write` 权限；无写权限时前端不显示恢复控件，后端仍独立拒绝越权请求。
- `asset.archived` 与 `asset.restored` 审计记录保留素材的 `projectId`，可按项目范围查询。
- 桌面侧栏与移动端均可进入回收站；导航目标至少 44px，并提供可见的 2px 内嵌克莱因蓝键盘焦点环。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过；API `145/145`、Web `42/42`，聚焦素材 API `10/10`、真实 PostgreSQL `8/8`，Biome 检查通过。
- API/PostgreSQL 测试覆盖活动与归档列表、恢复成功、revision 冲突、重复恢复、权限以及审计项目范围。
- 内置浏览器插入唯一素材 `e2e-recycle-bin-fixture-20260831-1948`，从活动资源库归档后消失；硬刷新后仍保持归档，在回收站回读并恢复后回收站显示空态。
- 新标签页重新选择团队后，恢复素材仍从 PostgreSQL 回读到活动资源库；最终硬刷新显示“全部素材 7 项”，验收素材为 `0`，根页面与 `body` 横向溢出均为 `0`。
- 回收站按钮保持键盘焦点并显示 2px 内嵌焦点环。移动端入口完成静态契约核验；内置浏览器不提供 viewport resize，本轮不虚报 375px 渲染验收。

精确清理与运行态：

- 素材 revision 在验收中按 `1 -> 2 -> 3` 变化，归档/恢复审计为 `2607/2608`，两条均带 `project_id=winter-coffee`。
- 精确删除临时素材及其审计；清理后 `team_assets=7`、`command_receipts=3`、`audit_logs=46 / max(id)=84`、`audit_logs_id_seq.last_value=2606`，验收素材与审计均为 `0`。
- 本轮没有启动新的持续进程；最终只读健康检查确认 Web `3211`/PID `27220` 返回 `200`、API `3220`/PID `29156` 返回 `{"status":"ok"}`，PostgreSQL `5433` 与 MinIO `9100/9101` 均为 `healthy`，Analysis Worker 根 PID `20392` / 实际 PID `35076`、Media Worker 根 PID `34604` / 实际 PID `12312` 按用户要求继续保留。

## 87. 2026-08-31 P1 Agent 发起视频镜头分析

本轮把既有资源库媒体分析接入统一 Agent 命令链：

- 新增普通写命令 `create_media_analysis`；预览按当前团队和可选项目范围解析唯一、活动、ready 视频素材，并冻结素材 ID、名称、revision、object key 与 SHA-256。
- 执行阶段重新校验 `asset.write`、ready/归档状态和 revision，使用 `agent-${intent.id}` 作为既有 `AssetService.createMediaAnalysis()` 的幂等键；没有新增任务表、Worker、权限模型、依赖或第二套媒体分析实现。
- Web 自然语言入口支持“分析素材“文件名”的镜头”和“对“文件名”做镜头分析”；成功后精确失效目标素材的 `media-analysis` 查询并回显已提交状态。
- 迁移 `057_agent_media_analysis_command.sql` 只扩展 Agent action CHECK，连续执行两次成功。

工程、API 与 Web 验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm build` 全部通过；API `147/147`、Web `43/43`。
- Agent 测试覆盖唯一名称选择、项目范围过滤、歧义拒绝、revision 冲突、执行时权限重检和 consumed intent 幂等重放；真实 `AssetService` API 测试确认过期 revision 返回 `RESOURCE_CONFLICT`。
- 主要改动位于 `packages/contracts/src/agent.ts`、`packages/contracts/src/assets.ts`、`packages/application/src/agent-command-service.ts`、`packages/application/src/asset-service.ts`、`apps/api/src/database.ts`、`apps/api/src/server.ts`、`apps/web/src/lib/agent-request.ts` 与 `apps/web/src/components/workspace/workspace-shell.tsx`。

内置浏览器、真实 Worker 与持久化验收：

- 内置浏览器在资源库输入“分析素材“P1 Agent 镜头分析 20260831.mp4”的镜头”；Agent intent `161e8a4e-0f85-4191-b9ec-d329eae72a21` 成功消费，任务 `26437e7a-cdeb-4da6-b552-6b57d9664cc0` 先显示 `等待分析`。
- 既有 Media Worker 从 MinIO 下载真实 2 秒 H.264/AAC 样本，任务经历 `queued -> awaiting_confirmation -> completed`，`attempts=1 / revision=3 / shot_count=1`；镜头 `28d0d2df-093c-4a6c-9b20-6de74d6e773f` 为 `0-2s`，关键帧时间 `0.5s`，确认后状态为 `confirmed`。
- 资源库先显示“待确认 / 镜头 1 / 源版本 r1”，点击“确认结果”后显示“镜头分析结果已确认 / 已确认”；完整刷新和第二个内置浏览器标签均从 PostgreSQL/MinIO 回读同一素材、状态和镜头。
- 浏览器无 console error；开发模式仍报告既有 `/assets/winter-coffee.png` LCP eager warning，本切片没有扩展范围修改无关种子图片加载策略。
- 首次占位对象验收被常驻 Media Worker 真实领取并在下载阶段失败，证明队列未伪成功；已精确删除该失败任务、intent、回执与审计后，改用真实 MinIO 视频重新完成上述链路。

精确清理与运行态：

- 精确删除验收资产 `p1-agent-media-e2e-20260831`、任务、镜头、intent、两条命令回执、审计 `2611-2615`、原视频对象、关键帧对象和本地 MP4；首次占位尝试的任务 `aaec131f-a16e-4a17-847c-ebf4a87616fd`、intent `97bb6751-d5de-4e04-b588-b6570ad3f5be`、回执与审计 `2607-2610` 亦已删除。
- 清理后 `team_assets=7`、`asset_media=0`、`media_analysis_jobs=0`、`media_analysis_shots=0`、`agent_command_intents=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`；MinIO 验收前缀与本地验收文件均为空。
- 本轮没有启动外部浏览器或新的持续服务；既有 Web、API、PostgreSQL、MinIO、Analysis Worker 与 Media Worker 按用户要求继续保留。

## 88. 2026-08-31 P1 拍摄日生成专业通告草稿

本轮让既有通告表创建链直接复用已确认的制片拆解事实：

- 只读取关联当前拍摄日且状态为“已确认”或“已完成”的拆解项；待确认项不会进入通告。
- 场地、场次、演员、部门准备、器材、安全事项和真实负责人写入既有通告字段；未知集合、妆发、页数、风险等级和电话保持“待定”或空值，不推测供应商与联系方式。
- 创建事务同时写入 `breakdown_item_call_sheets` 来源关联；幂等哈希仍只基于用户命令，同一幂等键不会因后续拆解变化而生成不同通告。
- 复用既有通告表模型、路由、权限、回执、审计和 UI 创建入口，没有新增表、迁移、端点、组件或依赖。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过；API `148/148`、Web `43/43`，Next.js 16.3.2 生产构建成功。
- API 回归覆盖已确认/已完成筛选、待确认排除、场次与演员生成、部门/器材/安全/联系人映射及拆解来源关联。
- 内置浏览器在既有 `shoot-day-06` 创建“P1 自动制片草稿验收 0831”；页面显示成功反馈，并回读场次 `1`、演员 `1`、部门 `2`、器材 `1`、安全事项 `1`、关键联系人 `3`。
- PostgreSQL 回读 `location=旧站台`、上述 JSON 数量、6 条 `breakdown_item_call_sheets`、1 条命令回执和 1 条创建审计；硬刷新与第二个内置浏览器标签均回读同一通告内容。

精确清理与运行态：

- 验收使用 6 条独立前缀临时拆解，不修改 `br1-br6` 或 `sd-05/sd-06/sd-07`；验收后精确删除通告、6 条来源关联、回执、审计、拍摄日关联和临时拆解，第二标签页已关闭。
- 清理后 `breakdown_items=6`、`shooting_days=3`、`call_sheets=3`、`breakdown_item_shooting_days=0`、`breakdown_item_call_sheets=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`，全部验收标记为 `0`；主标签硬刷新不再显示临时通告。
- 本轮没有启动新的持续服务；Web `3211`/PID `27220`、API `3220`/PID `40288`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker 根 PID `20392` / 实际 PID `35076` 与 Media Worker 根 PID `34604` / 实际 PID `12312` 均健康并按用户要求继续保留。

## 89. 2026-08-31 P1 Agent 查询团队资源

本轮让全局 Agent 通过正式业务服务发现可关联的团队资源：

- 新增只读命令 `list_team_resources`，按当前团队、可选项目和关键词返回联系人、供应商与活动素材；项目范围会重新校验当前账号的项目访问权限。
- 联系人沿用既有共享字段裁剪，未共享电话或邮箱保持 `null`；供应商中的联系人引用只保留当前账号可见的联系人，避免通过关联关系泄露已撤销共享。
- 预览与执行阶段都重新读取 `ContactService` 和 `AssetService`，执行结果持久化到既有 Agent intent；重复执行返回同一结果。
- 复用既有 Agent API、intent、权限、联系人、供应商、素材和 Web 面板，没有新增表、端点、搜索框架或依赖；迁移 `058_agent_list_team_resources_command.sql` 只扩展 action 约束。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过；API `150/150`、Web `44/44`，Next.js 16.3.2 生产构建成功。
- API 回归覆盖项目和关键词过滤、共享字段裁剪、隐藏联系人引用剔除、空结果、团队越权拒绝及 consumed intent 重放；迁移连续执行两次成功。
- 内置浏览器在资源库输入“查看团队资源”，真实回读 `4 位联系人、2 家供应商、7 项素材`；完整刷新后输入“查询团队资源；关键词：雨效”，回读 `1 位联系人、0 家供应商、1 项素材`，证明结果来自服务端而非前端缓存。
- PostgreSQL 确认两次命令均为 `consumed`，结果 JSON 保存真实联系人、供应商和素材；Agent 面板显示来源名称和分类计数。

精确清理与运行态：

- 精确删除验收 intent `df05fd4a-0d5f-47d4-8ba0-c4cb41114d4e`、`2c383f5a-3241-489f-9dfb-42030f8a0c60` 与审计 `2617-2620`，审计序列恢复为 `2616`。
- 清理后 `agent_command_intents=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`；资源库刷新仍显示 7 项素材，验收对话不存在。
- 本轮没有启动新的持续服务；既有 Web、API、PostgreSQL、MinIO、Analysis Worker 与 Media Worker 继续保留。

## 90. 2026-08-31 P1 多人脚本在线光标与即时离线

本轮补齐既有 SSE 脚本协作链中缺失的编辑区光标反馈和连接清理：

- 编辑器在原生 `textarea` 上增加只读镜像层，只绘制当前版本中其他正在编辑的协作者；自己的光标、其他版本、非编辑状态和未知协作者均不会进入镜像。
- 远端选区使用低饱和透明覆盖，光标与姓名标签只使用既有主色/辅助色；镜像层 `pointer-events: none`、`aria-hidden`，不抢焦点、不替换原生文本输入，并同步 textarea 横纵滚动。
- PostgreSQL realtime store 新增精确 presence 删除；同一账号在当前进程的最后一个 SSE 连接关闭后立即离线，仍有同账号连接时不删除。现有 45 秒 TTL 继续作为异常断线兜底。
- 复用现有 `script_presence`、SSE、权限、版本和协作者契约，没有新增迁移、WebSocket、CRDT、依赖或第二套协作状态。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部通过；API `150/150`、Web `45/45`、真实 PostgreSQL `35/35`，Next.js 16.3.2 最终生产构建成功。
- Web 单测覆盖当前账号/版本过滤和过期光标偏移裁剪；API SSE 测试确认真实断开会调用持久化清理；PostgreSQL 测试确认删除后 presence 列表为空。
- 内置浏览器打开 `冬夜咖啡_拍摄稿 / v7`，繁星与顾遥两条真实登录 SSE 同时在线；繁星端显示“2 人在线”和“顾遥 正在编辑 v7 · 第 5 行 · 2 列”。
- 顾遥的 `25-41` 选区在正文内显示淡色覆盖、32px 单行光标与姓名标签；标记完全位于 `450x560` 编辑器内，镜像层 `pointer-events=none`。
- 关闭顾遥 SSE 后，繁星端在 1 秒内回到“1 人在线”，顾遥编辑状态和远端光标层同时消失，繁星的“实时已连接”保持不变。

精确清理与运行态：

- PostgreSQL 回读顾遥 presence 为 `0`；本轮临时登录创建的唯一 `auth_session` 已按 ID `w2LfDZECydc5fVZL71yFN6AixPfOqq0t` 删除，既有顾遥会话保持不变。
- 临时 Node SSE 会话已终止；没有修改脚本正文、revision、回执、审计或业务表，也没有启动外部浏览器。
- Web `3211`/PID `27220`、API `3220`/当前 PID `7520`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `34604 / 12312` 按用户要求继续保留。

## 91. 2026-08-31 P1 Agent 创建团队联系人与供应商

本轮把既有团队资源创建能力接入统一 Agent 命令链：

- 新增普通写命令 `create_team_contact` 与 `create_team_supplier`；联系人支持姓名、职位、公司、电话、邮箱和项目关联，供应商支持名称、类别、服务、联系方式、地址、项目与联系人关联。
- 预览阶段通过既有 `ContactService` 校验团队写权限、项目访问权和联系人可见性；执行阶段继续调用同一服务，并使用 `agent-${intent.id}` 作为既有创建命令的幂等键。
- Web 自然语言入口解析结构化字段，执行成功后失效联系人和供应商查询；没有新增权限模型、资源表、端点、依赖或第二套创建实现。
- 迁移 `059_agent_create_team_resources_commands.sql` 仅扩展 Agent action 约束，已成功应用。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `151/151`、Web `46/46`、真实 PostgreSQL `35/35`，聚焦 Agent API `74/74`。
- API 回归覆盖创建字段、项目与联系人关联、团队权限、无效项目/联系人、consumed intent 重放、回执与审计；Web 单测覆盖两类自然语言命令解析。
- 内置浏览器通过 Agent 创建联系人 `P1 Agent 验收联系人 20260831-2305`，随后创建供应商 `P1 Agent 验收供应商 20260831-2305` 并关联该联系人；Agent 分别回显创建成功。
- 联系人页显示职位“制片协调”，供应商页显示类别“灯光器材”、服务“现场灯光”和“关联 1 位联系人”；供应商详情展开后明确显示该联系人已选中。强制整页重载后，两项资源仍从 PostgreSQL 回读。
- PostgreSQL 确认两个 intent 均为 `consumed`，两份命令回执、六条 Agent/业务审计完整，`supplier_contacts` 精确关联供应商与联系人。

精确清理与运行态：

- 精确删除联系人 `e85f42d9-44d2-46e3-b0a3-4e88b1239baf`、供应商 `6b83444d-ed8b-418d-9915-33f05151ec5c`、两个 intent、两份回执与审计 `2784-2789`；供应商联系人关联随目标供应商删除。
- 清理后北岸影像活动 `team_contacts=2`、`suppliers=2`；验收联系人、供应商、关系、intent、receipt、audit 均为 `0`。
- 本轮没有启动新的持续进程；既有 Web `3211` 返回 `200`、API `3220` 返回 `{"status":"ok"}`，PostgreSQL、MinIO、Analysis Worker 与 Media Worker 按用户要求继续保留。

## 92. 2026-09-01 P1 跨时区执行排期与完整资源冲突

本轮在既有执行阶段模型上补齐跨时区排程与专业资源冲突反馈：

- 本地日期时间按原始 IANA 时区转换为 UTC；夏令时跳过的不存在时刻会被拒绝，秋季重复时刻必须明确选择第一次或第二次，不再依赖运行机器的本地时区。
- 时区控件复用可搜索 Popover，覆盖运行时 `Intl.supportedValuesOf("timeZone")` 提供的完整时区，并显示 UTC offset、跨日和显示时区换算信息。
- 冲突继续以稳定的资源类型和资源 ID 为身份，覆盖演员、工作人员、场地和设备；PostgreSQL 使用 `GREATEST/LEAST` 返回准确重叠区间，并保留无权访问跨项目排期的脱敏边界。
- Web 按资源类型分组冲突并提供定位编辑；编辑时保留既有资源 ID，顿号分隔的多资源不会再被合并为单一资源。
- 复用既有 `timestamptz`、`original_timezone`、执行阶段、资源、权限、幂等和审计契约，没有新增排程模型、迁移或依赖。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Web `50/50`、API `152/152`，聚焦排期 API 与真实 PostgreSQL `41/41`，Next.js 16.3.2 生产构建成功。
- 单测覆盖普通时区、DST 不存在/重复时刻、稳定资源 ID、多资源解析和四类资源冲突；PostgreSQL 覆盖准确交集、相邻区间不冲突、跨项目可见/脱敏及权限边界。
- 内置浏览器选择 `America/New_York`，重复时刻分别显示第一次 `UTC-04:00`、第二次 `UTC-05:00`；编辑保存、完整刷新和 PostgreSQL 回读保持同一 UTC 瞬时值及资源 ID，冲突准确显示 `11/1 01:30 - 11/1 02:15`。
- `375x812` 验收确认时区弹层不越界、DST 二选一可操作、编辑器关闭后焦点返回触发按钮；修正窄屏工具栏裁剪后，执行计划与通告表全部操作均完整可见，页面无横向溢出。

精确清理与运行态：

- 精确删除验收阶段 `p1-schedule-dst-a-20260831`、`p1-schedule-dst-b-20260831`、2 条关联资源与 2 条 `execution-stage.updated` 审计；所有 `pg-workspace-%` 测试命名空间残留为 `0`。
- 清理后 `winter-coffee` 为 4 个执行阶段、0 项资源冲突；验收阶段、资源和审计均为 `0`，内置浏览器重新进入团队并刷新后回读同一基线。
- 本轮没有启动新的持续进程；既有 Web、API、PostgreSQL、MinIO、Analysis Worker 与 Media Worker 继续保留。

## 93. 2026-09-01 P1 团队可见业务数据导出

本轮在团队审计页补齐可携带、可核验的业务数据 JSON 导出：

- 导出覆盖当前团队及当前账号可见项目的 46 个业务分区；不受审计页筛选条件影响，也不导出媒体二进制、对象存储键、认证/会话、外部审片令牌、命令回执、presence、任务锁或未共享的个人联系人字段。
- 共享联系人仅保留明确共享字段；嵌套 `password`、`token`、`secret`、`requestHash`、`idempotencyKey`、`objectKey` 等敏感键会递归裁剪。
- 权限复核、`REPEATABLE READ` 快照和 `team-data.exported` 成功审计在同一 PostgreSQL 事务中完成；无 `team.read` 时不生成快照或成功审计，审计写入失败也不会留下半完成事务。
- Web 使用现有审计页按钮触发原生 JSON 附件下载，并提供禁用、加载、成功与错误反馈；没有新增 ZIP、后台导出任务、存储对象或依赖。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Web `50/50`、API `155/155`、真实 PostgreSQL `38/38`，聚焦导出 API `3/3`、聚焦导出 PostgreSQL `2/2`。
- API 测试覆盖 `200` 附件、`no-store`、无权限 `403` 和普通 JSON `500`；PostgreSQL 测试覆盖可见项目、隐藏项目排除、共享联系人裁剪、敏感字段递归过滤、成功审计与越权不审计。
- 内置浏览器在 `#/team/north/audit` 点击“导出可见数据”，页面显示 `已下载 北岸影像-2026-08-31-业务数据.json`；真实文件为 `68,086` 字节，解析为 `shadowproducer-business-data / v1 / north`。
- 下载内容包含 9 个当前账号可见项目、46 个分区和 189 条记录；项目 ID 与 PostgreSQL membership 查询逐项一致，manifest 每个 count 均与实际数组长度一致，递归敏感键扫描为 `0`。
- 审计 `2937` 的 actor 为 `account-fanxing`，`projectCount=9`、`sectionCounts=46`，与同一次下载内容一致。

精确清理与运行态：

- 精确删除浏览器下载文件及本次成功审计 `2937`；清理后下载目录匹配文件为 `0`、北岸影像 `team-data.exported` 审计为 `0`。
- PostgreSQL 与 MinIO 容器均为 `healthy`；Web `3211`/PID `27220` 返回真实 HTML `200`，API `3220`/PID `41936` 的 `/healthz` 返回 `{"status":"ok"}`。
- 本轮没有启动新的持续进程；既有 Web、API、PostgreSQL、MinIO、Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `34604 / 12312` 按用户要求继续保留。

## 94. 2026-09-01 P1 权限变更通知偏好

本轮把团队与项目权限变更纳入既有站内通知偏好：

- `notification_preferences` 新增 `permission_assignments`，默认开启；旧账号无需迁移数据即可维持原有通知行为。
- 团队与项目权限分配通知在写入前读取接收人的团队偏好；明确关闭后不创建对应通知，未配置时保持开启。
- 通知设置菜单新增“团队与项目权限变更”，沿用既有查询、更新、加载与错误反馈；业务数据导出同步包含该字段。
- 复用现有站内通知渠道、权限服务、API 路由与偏好 upsert，没有新增通知渠道、设置页或依赖。

工程、PostgreSQL 与内置浏览器验证：

- 迁移 `060_permission_notification_preferences.sql` 连续执行两次成功；`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `50/50`、API `155/155`、真实 PostgreSQL `38/38`。
- API 回归覆盖第三个偏好的读写契约；PostgreSQL 回归覆盖默认开启、关闭后抑制团队/项目权限通知、重新开启后恢复。
- 内置浏览器关闭“团队与项目权限变更”后，PostgreSQL 回读 `account-fanxing / north / true / true / false`；完整刷新并重新选择团队后仍显示未选中。
- 精确删除该偏好行后再次刷新，默认值重新显示为选中，证明默认状态来自服务端契约而非前端残留。

精确清理与运行态：

- 清理后 `account-fanxing / north` 偏好行为 `0`，`pg-workspace-%` 测试账号与团队均为 `0`；本轮没有新增业务记录、下载文件或对象存储制品。
- Web `3211`/PID `27220` 返回 `200`，API `3220`/PID `26832` 的 `/healthz` 返回 `{"status":"ok"}`；PostgreSQL 与 MinIO 容器均为 `healthy`。
- 本轮没有启动新的持续进程；既有 Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `34604 / 12312` 按用户要求继续保留。

## 95. 历史验证：已废弃的本地 whisper.cpp / Tesseract 方案（迁移前）

> 历史记录：本节验证的是已废弃的本地 whisper.cpp / Tesseract 方案，仅用于保留迁移前证据，不代表当前架构、部署前提或回退路径。现行边界以第 109、110 节为准：识别与 embedding 依赖第三方 API，服务器不运行或回退到本地模型。

以下内容全部属于迁移前历史证据，禁止作为当前部署、实现或回退依据。当时的素材文本分析接入既有媒体任务、权限和资源库搜索链：

- 当时音频/视频可创建 `transcription / whisper_cpp_v1` 任务，图片可创建 `ocr / tesseract_v1` 任务；Worker 持久化来源 revision、checksum、派生输入、运行时、模型摘要、语言、分段文本和失败阶段。
- 分析结果先进入 `awaiting_confirmation`，仅确认后的 `completed` 文本且 `source_revision` 仍等于素材当前 revision 时可参与团队范围搜索；源素材变化会使旧文本失效。
- 资源库显示候选文本、时间码或序号、运行时与来源版本，并提供创建、失败重试和确认入口；搜索框沿用既有素材查询，没有新增搜索服务或索引系统。
- 迁移 `061_media_text_analysis.sql` 扩展任务 kind/tool、文本结果与 trigram 索引，连续执行两次成功。
- 当时的真实 OCR 首轮暴露了可复现缺陷：固定 tessdata 目录不含 `configs/tsv`，旧命令静默输出纯文本而解析器得到空结果。Worker 改用 Tesseract 原生 `-c tessedit_create_tsv=1`，不再依赖外部配置文件。

工程、真实运行时与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API `158/158`、Web `50/50`、真实 PostgreSQL `39/39`，Next.js 16.3.2 生产构建成功。
- whisper.cpp `1.9.3` 使用 `ggml-small.bin`，模型 SHA256 为 `1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b`；Tesseract `5.4.0.20240606` 使用固定 `chi_sim+eng` 模型集合，摘要为 `96e34724d06d5760a694bc14aa1b7a3c82da725a0557548857c36bf9ee9c3d95`。
- 内置浏览器一次导入真实 WAV 与 PNG；音频任务 `9d0b8eff-065b-4c60-8799-aa72c04a8aff` 产出“车展夜戏今晚开始拍摄”，OCR 任务 `06fefc2a-addd-4a3e-a39c-7de339a94a10` 修复后产出“冬 夜 咖啡 车 站 夜 戏 通告 确认”。
- 两类候选在确认前关键词搜索均为 `0`；确认后分别精确命中 `1`，完整刷新后再次分别命中 `1`，证明搜索来自 PostgreSQL 已确认结果而非前端状态。

精确清理与运行态：

- 精确删除两份验收素材、两项分析任务、9 条命令回执、审计 `3141-3154`、5 个 MinIO 原始/派生对象和本地验收目录；清理后北岸影像活动素材恢复为 `7`，验收 asset/job/receipt/audit/object/file 均为 `0`。
- 当前基线为 `media_analysis_jobs=0`、`command_receipts=3`、`audit_logs=46 / max(id)=84`；Web `3211`/PID `27220` 返回真实 HTML `200`，API `3220`/PID `42800` 返回 `{"status":"ok"}`，PostgreSQL 与 MinIO 均为 `healthy`。
- Web、API、PostgreSQL、MinIO 与 Analysis Worker 均未重启；Media Worker 为应用修复精确重启，当前根/实际 PID `37940 / 42640`，无监听端口，按用户要求继续保留。

## 96. 2026-09-01 P1 项目中心真实进度与动态

本轮移除项目中心的静态项目画像，复用现有真实业务接口形成只读决策视图：

- 项目身份和状态来自 `workspace-context`；执行阶段、拍摄日、通告与项目审计并发读取，继续沿用既有项目权限边界。
- 整体完成度按真实执行阶段平均进度计算；没有执行计划时显示 `--`，不伪造百分比。当前阶段、阶段链和摘要均由持久化数据推导。
- 关键节点优先由拍摄日与关联通告生成，没有拍摄日时才退回执行阶段；节点可直接进入“拍摄与通告”。
- 动态按项目范围审计倒序展示来源模块、操作者与时间；执行阶段、拍摄日和通告在审计未附标题时复用已读取实体名称，不暴露内部 ID。
- 页面覆盖加载、失败重试、无执行计划、无节点和无动态状态；没有新增表、迁移、端点、依赖或第二套项目概览 API。

工程与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`、`git diff --check` 全部通过；API `158/158`、Web `52/52`，Next.js 16.3.2 生产构建成功。
- Web 单测覆盖真实阶段排序、平均进度、当前阶段、拍摄日/通告节点、审计来源与实体名称回退，以及无计划时不虚构进度或节点。
- 内置浏览器初次回读真实基线：4 个执行阶段、整体 `46%`、当前阶段“拍摄执行”、3 个拍摄节点和项目审计动态。
- 经“拍摄与通告”UI 将“拍摄执行”临时写为 `72%` 后，项目中心回读整体 `47%`、阶段 `72%` 和 `execution-stage.updated` 动态；完整页面刷新后仍保持一致。
- 拍摄节点和最新执行动态均可进入 `#/project/north/winter-coffee/schedule`，证明导航不是静态装饰。

精确清理与运行态：

- 经 UI 恢复“拍摄执行”为 `68%`，随后精确恢复 `revision=1` 与原 `updated_at=2026-08-30 09:31:07.526334+00`，删除本轮审计 `3242-3244`。
- 清理后完整刷新回读整体 `46%`、阶段 `68%`，临时 `exec-shoot` 动态不存在；没有新增命令回执、业务实体、对象存储制品或下载文件。
- 本轮没有启动新的持续进程；既有 Web `3211`、API `3220`、PostgreSQL `5433`、MinIO `9100/9101`、Analysis Worker 与 Media Worker 按用户要求继续保留。

## 97. 2026-09-01 P1 跨 API 实例脚本在线租约

本轮修复同一账号跨多个 API 实例协作时的错误离线：

- 新增 `script_presence_connections`，每条 SSE 连接使用独立 `connection_id`、`instance_id` 和 45 秒 PostgreSQL 租约；聚合 `script_presence` 继续作为账号级光标状态。
- 心跳只延长当前连接租约，断开只删除当前连接；仅当同账号、同文档已无有效租约时才删除聚合 presence。
- 连接与断开事务都锁定同一聚合 presence 行，避免一侧检查完“无其他连接”后，另一实例刚建立连接却仍被删除的竞态。
- SSE 清理监听改为实际输出的 `reply.raw.close`，浏览器刷新或关闭 EventSource 后不再等待 TTL 才离线。
- 继续复用既有 SSE、`audit_logs` 事件事实源、1 秒 PostgreSQL 轮询、`Last-Event-ID` 回放和 `ScriptService.getWorkspace` 权限校验；没有引入 Redis、WebSocket、CRDT、新依赖或第二套实时系统。

工程、PostgreSQL、双实例与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`、`git diff --check` 全部通过；API `158/158`、Web `52/52`、真实 PostgreSQL `39/39`，Next.js 16.3.2 生产构建成功。
- PostgreSQL 回归使用两个 `PostgresScriptRealtimeStore` 和同一账号的两条连接，确认断开 A 后 B 仍在线、断开 B 后离线、过期租约不出现在列表且不能更新 presence。
- 临时 API `3221/3222` 共享同一 PostgreSQL：断开第一实例后另一实例更新返回 `200`；实例 A 创建评论后实例 B 收到事件 `3279`，以 `Last-Event-ID=3278` 重连后再次回放同一事件；全部连接断开后更新返回 `409 SCRIPT_PRESENCE_CONNECTION_REQUIRED`。
- 内置浏览器打开 `冬夜咖啡_拍摄稿 / v7`，显示“实时已连接 / 1 人在线”；聚焦正文后状态变为“正在编辑”，完整刷新后自动恢复“实时已连接 / 1 人在线”，控制台无 warning 或 error。

精确清理与运行态：

- 精确删除验收评论 `673fe1ab-3762-40eb-8de8-f6e70edb21ed`、审计 `3279` 与三条临时连接租约；回读 comment、audit、临时 connection 和临时聚合 presence 均为 `0`。
- 本轮两次临时 API 进程树及 `3221/3222` 均已终止，8 个临时日志已删除；当前仅保留内置浏览器脚本页对应的一条 `account-fanxing` 有效租约。
- 既有 Web `3211`/PID `27220` 与 API `3220`/当前 PID `36508` 继续运行，`/healthz` 返回 `{"status":"ok"}`；PostgreSQL、MinIO、Analysis Worker 与 Media Worker 未重启。

## 98. 2026-09-01 P1 工作区回收站纵切

本轮把当前账号在当前团队中的任务、日历事件和笔记 / 便签接入统一回收站：

- 删除继续复用三类实体既有 `deleted_at` 墓碑；回收站查询按 actor 与 team 隔离，不引入第二套删除状态。
- 恢复要求 `expectedRevision`，成功后 revision 加一，并写入 `task.restored`、`calendar-event.restored` 或 `note.restored` 审计。
- 新增团队侧栏“回收站”及加载、错误、空态、恢复中和结果反馈；恢复后同步失效对应活动列表查询。
- 修正日历恢复使用了不存在的 `workspace-calendar` 查询键，现与日历页面实际的 `calendar-events` 键一致。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦 Workspace API `10/10`、真实 PostgreSQL `10/10`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 已通过，Web `52/52`、API `159/159`。
- 缓存键修正后再次执行 Web `lint` 与 `typecheck`，均通过。
- 内置浏览器创建唯一任务并从看板删除；回收站立即显示该任务，恢复后看板无需刷新即回显，完整页面刷新后仍从 PostgreSQL 读回。
- 内置浏览器创建唯一日历事件并从日期列表删除；回收站立即显示该事件，恢复后切回日历无需刷新即回显，完整页面刷新后仍从 PostgreSQL 读回，直接覆盖修正后的缓存失效路径。
- PostgreSQL 回读两项恢复记录均为活动态、revision `1 -> 2`；每项 created / deleted / restored 三条审计完整。

精确清理与运行态：

- 精确删除任务 `3d0a4578-f074-4121-aa7e-e8dab766de9a`、日历事件 `dfdf7723-00bd-4303-89df-be556e9539d9`、两份命令回执与审计 `3438-3443`；业务记录、回执和审计残留均为 `0`。
- 本轮临时 API `3221` 已终止且端口释放；既有 Web `3211`/PID `19516` 与 API `3220`/PID `39596` 继续运行并分别返回 HTML `200` 与 `{"status":"ok"}`。

## 99. 2026-09-01 P1 团队联系人与供应商回收站

本轮将团队联系人和供应商并入现有团队回收站：

- 回收站复用 `team_contacts.deleted_at` 与 `suppliers.deleted_at`，按 team 隔离团队资源；个人联系人仍保持 owner 语义，不塞入团队范围端点。
- 恢复要求 `expectedRevision`，成功后 revision 加一，并写入 `team-contact.restored` 或 `team-supplier.restored` 审计。
- 供应商的项目、联系人关系和团队联系人的项目关系在软删除与恢复期间保持不变。
- 资源库为团队联系人补齐与供应商一致的删除确认入口；恢复后失效联系人、供应商和回收站查询，页面即时回显。
- 未实现永久删除、自动清理或默认保留期限；这些策略会影响审计、通告、作品集和历史引用，继续等待产品规则冻结。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦 Workspace API `11/11`、聚焦 Workspace PostgreSQL `11/11`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 通过，Web `52/52`、API `160/160`、真实 PostgreSQL 串行 `41/41`。
- PostgreSQL 回归覆盖两类资源的创建、关系持久化、软删除、回收站列表、过期 revision 冲突、恢复、活动列表回显、revision `1 -> 2` 和完整审计序列。
- 默认并行 PostgreSQL 套件曾被既有 Analysis Worker 抢占一项队列任务；对应文件单独复跑 `6/6`、7 个 PostgreSQL 文件串行复跑 `41/41`，未停止用户要求保留的 worker。
- 内置浏览器创建唯一团队联系人和关联供应商，依次从资源库删除；回收站同时显示“团队联系人 / 供应商”和团队范围。
- 两项恢复后资源库无需刷新即回显；完整页面刷新后仍从 PostgreSQL 回读，供应商继续显示 1 位关联联系人和“冬夜咖啡”项目。
- 联系人 Dialog 在 `1280 x 720` 视口测得 `left=448 / right=832 / width=384`，保持居中且未越界。

精确清理与运行态：

- 精确删除联系人 `bae2c577-5a02-4913-9ebb-301fcfc51892`、供应商 `a7662107-f033-4997-abf7-6fd56f6fa787`、关联关系、两份命令回执与审计 `3482-3487`；实体、关系、回执和审计残留均为 `0`。
- 构建后 Web `3211`/PID `19516` 返回 HTML `200`，API `3220`/PID `42040` 的 `/healthz` 返回 `{"status":"ok"}`；内置浏览器重新载入资源库成功，验收数据已消失。
- 本轮没有启动新的持续进程；既有 Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `37940 / 42640` 按用户要求继续保留。

## 100. 2026-09-01 P1 素材文件夹回收站

本轮补齐团队素材文件夹的归档与恢复闭环：

- 复用 `asset_folders.archived_at`，素材列表按当前活动/回收站范围同时返回对应文件夹；没有新增表、迁移或依赖。
- 文件夹归档与恢复要求 `expectedRevision`，成功后 revision 加一，并写入 `asset.folder.archived` 或 `asset.folder.restored` 审计。
- 非空文件夹禁止归档；检查覆盖活动及已归档素材、活动及已归档子文件夹，不级联、不搬移素材，也不破坏原层级引用。
- 恢复回到原父级；父级不可用或原位置已有同名活动文件夹时明确返回冲突，不静默改到根目录。
- 资源库侧栏和主区域使用同一文件夹数据与操作语义，提供归档确认、恢复、加载、权限、冲突和状态反馈；回收站只有文件夹时不再误报为空。
- 永久删除、自动清理和默认保留期限仍未实现，等待与素材、审片、作品集及审计引用一起冻结产品规则。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦素材 API 与 PostgreSQL `14/14`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `52/52`、API `161/161`、真实 PostgreSQL 串行 `42/42`，Next.js 16.3.2 生产构建成功。
- API 回归覆盖 `asset.write` 权限、非空拒绝、活动/回收站列表、过期 revision 冲突、恢复及重复恢复；PostgreSQL 回归另外覆盖子文件夹阻止与完整审计序列。
- 内置浏览器创建唯一空文件夹，归档后资源库回收站同时在侧栏和主区域显示；恢复后完整刷新仍从 PostgreSQL 回读为活动文件夹，revision 为 `1 -> 2 -> 3`。
- 对既有非空文件夹“成片”执行归档，Dialog 保持打开并显示“仅空文件夹可以移入回收站”，数据库未发生变更；`1280 x 720` 下确认 Dialog 为 `384 x 189`、水平居中且页面无横向溢出，控制台无 error。

精确清理与运行态：

- 精确删除验收文件夹 `a6efcd19-1146-4a91-b8aa-bdb873998b44`、创建回执 `a4ef7368-a2c6-4366-879a-18755c37b40d` 与审计 `3703-3705`；文件夹、回执和审计残留均为 `0`，刷新后页面不再显示验收名称。
- Web `3211`/PID `19516` 返回 HTML `200`，API `3220`/当前 PID `7568` 的 `/healthz` 返回 `{"status":"ok"}`；PostgreSQL 与 MinIO 均为 `healthy`。
- 本轮没有启动新的持续进程；既有 Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `37940 / 42640` 按用户要求继续保留。

## 101. 2026-09-01 P1 审片文件夹回收站

本轮补齐项目审片文件夹的归档与恢复闭环：

- 复用 `review_folders.archived_at`，活动列表与回收站列表共享同一实体和 revision；没有新增表、迁移或依赖。
- 文件夹归档与恢复要求 `review.manage`、项目写权限和 `expectedRevision`，成功后 revision 加一，并写入 `review-folder.archived` 或 `review-folder.restored` 审计。
- 非空审片文件夹禁止归档，不级联归档或搬移审片版本；评论、标注、审批、外部审片链接和作品集引用均不受文件夹操作影响。
- 恢复回原项目；同项目已有同名活动文件夹时明确返回冲突，不覆盖、不自动改名。
- 审片资源管理器侧栏新增回收站入口、空文件夹归档与恢复操作，并覆盖加载、空态、错误和进行中状态。
- 永久删除、自动清理、默认保留期限及审片版本自身的回收站仍未实现，等待跨审片链接、作品集与审计引用的产品规则冻结。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦内存 API 与 PostgreSQL 各 `1/1`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `52/52`、API `161/161`、真实 PostgreSQL 串行 `42/42`，Next.js 16.3.2 生产构建成功。
- 回归覆盖 `review.manage` 权限、非空拒绝、活动/回收站列表、过期 revision 冲突、恢复、同名恢复冲突与完整审计序列。
- 内置浏览器创建唯一空审片文件夹，归档后审片回收站显示，恢复后完整刷新仍从 PostgreSQL 回读为活动文件夹，revision 为 `1 -> 2 -> 3`。
- PostgreSQL 精确回读验收文件夹 `a807d4b1-a589-465b-8fc7-20631dbb37a6` 的 revision 为 `3`；非空拒绝由内存 API 与真实 PostgreSQL 测试共同覆盖。

精确清理与运行态：

- 精确删除验收文件夹 `a807d4b1-a589-465b-8fc7-20631dbb37a6`、一份创建回执与三条审计；文件夹、回执和审计残留均为 `0`。
- Web `3211`/PID `19516` 返回 HTML，API `3220`/当前 PID `19028` 的 `/healthz` 返回 `{"status":"ok"}`；既有 PostgreSQL、MinIO、Analysis Worker 与 Media Worker 继续运行。
- 本轮没有启动新的持续进程，用户要求保留的现有服务均未停止。

## 102. 2026-09-01 P1 团队作品集回收站

本轮补齐团队作品集的归档与恢复闭环：

- 复用 `team_portfolios.archived_at`、现有 revision、命令回执和审计基础设施；没有新增迁移或依赖。
- 活动列表与回收站按团队隔离；归档、恢复均要求 `portfolio.write`、`expectedRevision` 和幂等键。
- 归档会立即撤回公开状态，但保留内容条目、原 slug、域名配置、统计和历史引用；slug、媒体、访问统计与已验证自定义域名入口均停止公开访问。
- 恢复后固定回到“团队可见”，不自动重新发布；同名活动作品集占用原名称时返回冲突，不覆盖、不自动重命名。
- 作品集页面新增回收站、归档确认和恢复操作，并覆盖加载、空态、错误和进行中状态；永久删除和自动清理仍未提供。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦作品集内存 API `8/8`、PostgreSQL `2/2`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `52/52`、API `163/163`、真实 PostgreSQL `44/44`，Next.js 16.3.2 生产构建成功。
- API 回归覆盖权限、revision 冲突、幂等重放、公开 slug/媒体/view 立即 `404`、恢复后仍保持私有、同名恢复冲突和内容引用保留；PostgreSQL 回归确认 `portfolio.archived/restored` 审计。
- 内置浏览器创建唯一作品集并添加已通过成片，归档后回收站显示 1 项内容；恢复后完整刷新仍从 PostgreSQL 回读为“团队可见”，revision 为 `1 -> 2 -> 3 -> 4`。
- 浏览器验收所选成片不满足当前发布按钮的源文件可用条件，因此没有绕过 UI 强行发布；公开入口失效由同一服务与仓储的 API 回归覆盖。

精确清理与运行态：

- 精确删除验收作品集 `5a025fd2-b8b6-4cb3-b54f-e6303f77f85b`、内容条目 `6e21e3b4-d5fb-47b4-909e-b3faa561dc97`、4 份命令回执与审计 `4033-4036`；作品集、条目、回执和审计残留均为 `0`，完整刷新后验收名称不存在。
- Web `3211`/PID `19516` 返回真实页面，API `3220`/当前 PID `23884` 的 `/healthz` 返回 `{"status":"ok"}`；既有 PostgreSQL、MinIO、Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `37940 / 42640` 继续运行。
- 本轮没有启动新的持续进程，用户要求保留的现有服务均未停止。

## 103. 2026-09-01 P1 个人联系人恢复

本轮补齐个人联系人 owner 范围内的软删除恢复闭环：

- 已删除列表与恢复端点只按 `owner_account_id` 返回和修改数据，不进入团队回收站；团队成员或管理员不能代替所有者恢复个人联系人。
- 恢复要求 `expectedRevision`，只清除 `deleted_at`，成功后 revision 加一并写入 `contact.restored` 审计；原 ID、字段和团队共享关系保持不变。
- 个人联系人页新增“已删除”入口、加载、错误、空态和恢复中状态；删除或恢复后同时失效个人列表、已删除列表及全部团队联系人投影缓存。
- 同名联系人不阻止恢复，不自动合并或覆盖；永久删除、自动清理和保留期限仍未提供。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦联系人 API `9/9`、聚焦 PostgreSQL `6/6`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `52/52`、API `164/164`、真实 PostgreSQL `45/45`，Next.js 16.3.2 生产构建成功。
- 回归覆盖 owner 隔离、删除后个人列表和团队投影隐藏、过期 revision 冲突、恢复后 revision 加一、共享字段与 `allowProjectLink` 保留，以及 `contact.restored` 审计。
- 内置浏览器创建唯一个人联系人，共享姓名与电话且禁止新项目关联；资源库显示字段投影，删除后投影消失，个人“已删除”列表可恢复。
- 恢复后资源库投影重新出现；完整页面刷新仍从 PostgreSQL 读回原字段、2 个共享字段及“不允许建立新项目关联”，revision 为 `1 -> 2 -> 3`。

精确清理与运行态：

- 精确删除验收联系人 `8a3fa0af-a149-45de-b540-f555a21dad7b`、north 共享关系、创建回执 `0a183603-4e52-442b-9653-0f17d8f458e9` 与审计 `4160-4163`；联系人、共享、回执和审计残留均为 `0`，完整刷新后验收名称不存在。
- Web `3211`/PID `19516` 返回 HTML `200`，API `3220`/PID `33080` 的 `/healthz` 返回 `{"status":"ok"}`；既有 Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `37940 / 42640` 继续运行。
- 本轮没有启动新的持续进程，用户要求保留的现有服务均未停止。

## 104. 2026-09-01 P1 审片版本回收站

本轮补齐项目审片版本的软归档与恢复闭环：

- `review_files` 新增 `archived_at`，活动与归档查询继续复用同一版本实体、revision、权限、幂等回执和审计基础设施。
- 归档与恢复要求项目写权限、`review.manage`、`expectedRevision` 和幂等键；成功后 revision 加一，并写入 `review-file.archived` 或 `review-file.restored` 审计。
- 归档版本从项目活动审片、客户审片和新作品集候选中隐藏；成员评论、审批和移动操作对归档版本关闭。
- 评论、审批记录、客户链接关系、素材与作品集引用全部保留；恢复后原客户链接重新可见，已发布作品集继续播放归档前已引用的内容。
- 审片资源管理器的文件操作新增“移入回收站”，既有回收站统一展示归档文件夹和归档版本，并提供恢复、加载、错误、空态和进行中状态。
- 永久删除、自动清理和默认保留期限仍未实现，避免破坏审片、作品集和审计历史。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦 Production API `33/33`、Workspace PostgreSQL `12/12`、客户审片 PostgreSQL `2/2`、作品集 PostgreSQL `3/3`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `52/52`、API `165/165`，Next.js 16.3.2 生产构建成功。
- 客户审片回归确认归档后 workspace 隐藏版本，评论、批准和媒体访问被阻断，`review_link_files` 关系不删除；恢复后同一链接重新显示原版本。
- 作品集回归确认归档版本不再进入新候选和新增内容，同时已发布作品集的既有内容与媒体来源继续可读。
- 内置浏览器创建唯一 `v13` 审片版本，归档后活动列表从 4 项变为 3 项、回收站从 0 变为 1；完整刷新后状态保持。恢复后回收站归零，完整刷新后版本回到原根目录。
- PostgreSQL 回读同一版本 revision 为 `1 -> 2 -> 3`，归档时间在恢复后清空，created / archived / restored 三条审计与三份命令回执完整。

精确清理与运行态：

- 精确删除验收审片版本 `32bcfded-b9b2-489a-829a-39e9e4a3dc6a`、前置素材 `asset-p1-review-archive-20260901-0827`、3 份命令回执与审计 `4214-4216`；版本、素材、回执和审计残留均为 `0`，页面刷新后验收名称不存在。
- 前置素材只用于提供可选择的 ready 视频记录，没有创建对象存储制品、媒体任务或衍生文件；关联评论、客户链接和作品集条目均为 `0`。
- 本轮没有启动新的持续进程；既有 Web `3211`/PID `19516` 与 API `3220`/当前 PID `35960` 继续运行，PostgreSQL、MinIO、Analysis Worker 与 Media Worker 均未停止。

## 106. 2026-09-01 P1 Agent 并发执行幂等

本轮修复同一 Agent intent 被并发执行时可能重复产生业务副作用的竞态：

- `AgentCommandService.execute()` 现在在 repository 执行锁内重新读取 intent；首个请求执行并保存结果后，后续并发请求直接回放同一结果。
- PostgreSQL repository 使用 `pg_advisory_xact_lock(hashtextextended(...))`，锁键绑定 actor 与 command，可跨 API 实例生效；没有新增队列、状态表、分布式锁服务或依赖。
- intent 在开始执行前仍必须未过期；执行已经开始后不再因跨过过期边界而拒绝保存结果，避免“业务已成功但 intent 未消费”。
- 内存 repository 使用同键 Promise 串行，仅用于 API 回归；真实 PostgreSQL 测试由两个独立 repository 实例争抢同一 advisory lock。

工程、PostgreSQL 与内置浏览器验证：

- 并发 API 回归使用两个同时到达的 execute 请求，断言两次均为 `200`、结果相同、仅一次业务写入，后续请求 `replayed=true`。
- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Web `54/54`、API `165/165`，Next.js 16.3.2 生产构建成功。
- 真实 PostgreSQL 套件 `9` 个文件、`49/49` 通过；新增锁测试没有写入业务表或产生验收数据。
- 内置浏览器通过正式登录会话在 Agent 面板创建唯一任务，Agent 回显成功；完整刷新看板后仍从 PostgreSQL 回读该任务。

精确清理、运行态与 P1 剩余边界：

- 精确删除浏览器验收 task `eb6f5bd9-bd5b-456c-a035-a1cf8d493d5f`、intent `a7790262-3686-4782-b4cb-fd0721223be7`、一份命令回执与审计 `4261-4263`；四类残留均为 `0`，north 任务恢复为 `4`，浏览器刷新后验收名称不存在。
- 迁移前状态：当时 PostgreSQL 尚未提供 `vector` 扩展，本机也没有 Ollama、embedding/vision 端点或 CLIP/ONNX 运行时；该状态已被第 109 节的 pgvector 迁移取代。服务器仍不部署本地模型。
- Web `3211`/PID `19516` 返回 HTML `200`，API `3220`/当前 PID `29356` 的 `/healthz` 返回 `ok`；本轮没有启动新的持续进程，既有服务继续运行。

## 105. 2026-09-01 P1 工作区授权上下文统一

本轮将工作区入口、路由、顶部切换与团队概览统一到既有服务端授权上下文：

- `AuthenticatedWorkspace` 只查询一次 `GET /v1/workspace-context`；团队选择、当前团队、当前项目、顶部项目选择器和全局搜索共享同一份 React Query 数据，不再分别读取编译期团队与项目夹具。
- hash 路由按服务端返回的团队和项目集合校验；无权团队、无权项目及跨团队项目组合不能进入工作区。没有项目的团队仍可进入团队与个人页面，项目页不会伪造默认项目。
- 团队概览项目卡片使用当前团队真实项目；成员来自权限工作区，动态来自审计记录，并明确显示项目或团队来源、动作、操作者和时间。
- 后端没有成员在线状态字段，因此删除了伪造的“在线 / 几分钟前”信息；成员列表只显示真实角色和权限模板或项目数量。
- 继续复用现有 `workspaceApi.getContext/getPermissions/listAuditLogs`、Command 项目选择器、React Query 与既有零圆角组件；没有新增 API、状态库、依赖或全局 Provider。

工程、数据库与运行态验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Web `54/54`、API `165/165`，Next.js 16.3.2 生产构建成功。
- 新增纯路由回归覆盖授权团队、授权项目、无权团队、跨团队项目、空项目团队及 canonical hash；真实 PostgreSQL Workspace 套件 `12/12` 通过。
- 匿名实时 API 请求返回 `AUTH_REQUIRED`，未读取或复用浏览器 Cookie；Web `3211` 返回 HTML `200`，API `3220` `/healthz` 返回 `ok`。
- 内置浏览器现有标签页显示“无法访问此站点”，且浏览器安全策略拒绝重新载入或读取该本地 URL；本轮没有绕过策略、没有切换到外部 Chrome，因此可见页面交互验收仍待内置浏览器恢复后补做，不能以 HTTP 健康检查代替。

清理与保留项：

- 本轮没有创建业务验收数据、数据库迁移、对象存储制品或持续进程，无需数据清理。
- 构建、测试和类型检查进程均已退出；既有 Web `3211`/PID `19516`、API `3220`/PID `35960`、PostgreSQL、MinIO、Analysis Worker 根/实际 PID `20392 / 35076` 与 Media Worker 根/实际 PID `37940 / 42640` 按用户要求继续运行。

## 107. 2026-09-01 P1 多模态与向量架构纠偏

本轮修正 P1 多模态能力的部署前提：

- 多模态识别、OCR、语音转写和 embedding 生成均依赖可配置的第三方 API；服务器通常不具备有效模型运行条件，不再把 Ollama、CLIP、ONNX、本地视觉模型或本地 embedding 模型作为目标架构、部署前提或回退路径。
- 服务端负责权限与模型数据策略、媒体派生输入、后台任务、结果校验、审计和重试；PostgreSQL `pgvector` 负责持久化第三方 API 返回的向量及团队/项目 Actor Scope 下的相似度检索。
- P1 向量记录必须绑定供应商、模型、维度、来源对象和 `source_revision`；来源变化或模型变更后，旧向量不再进入活动检索结果。
- 第 95 节记录的本地 whisper.cpp 与 Tesseract 是已验证过的历史实现，不再代表目标架构；迁移完成前现有关键词搜索仍只覆盖素材元数据和已确认文本结果，不能声称已实现多模态语义检索。
- 第 106 节记录的“本机没有模型运行时”不是阻塞条件。本节记录的是迁移前基线；其中 API 契约、`pgvector`、任务执行、检索链路和失败态已由第 109 节及后续验证补齐，当前仍缺真实供应商凭据下的模型质量、限流、费用与生产兼容性验收。

本轮只修正需求与架构基线，没有接入未指定的供应商、添加占位适配器或把环境变量当成功能完成；未启动持续进程，也未产生业务数据、数据库迁移或对象存储制品。

## 108. 2026-09-01 P1 Agent 执行计划查询与笔记更新

本轮补齐两个直接复用现有领域服务的 Agent 命令：

- `list_execution_schedule` 以只读风险查询当前项目的执行阶段、资源冲突和拍摄日；preview 与 execute 均重新校验项目授权，不要求确认。
- `update_note` 按显式 ID 或 NFKC 规范化后的唯一标题选择个人拥有的笔记，并在 preview 时固化 `itemId` 与 `expectedRevision`；项目作用域只匹配当前项目笔记。
- 笔记更新至少需要标题、正文或置顶状态之一；execute 复用 `WorkspaceService.updateNote` 的团队写权限、owner 隔离、revision CAS 与 `note.updated` 审计。
- 中文请求解析支持显式字段以及“置顶笔记”“取消置顶便签”；更新后统一失效 notes 查询，Agent 回显 `note_updated` 结果。
- 迁移 `065_agent_list_execution_schedule_command.sql` 与 `066_agent_update_note_command.sql` 只扩展既有 Agent action CHECK，没有新增命令框架或依赖。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦解析器 `33/33`，聚焦笔记 Agent API `4/4`；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过，Web `56/56`、API `169/169`，Next.js 16.3.2 生产构建成功。
- 真实 PostgreSQL 套件 `9` 个文件、`49/49` 通过；`pnpm db:migrate` 连续执行两次成功，证明 `065/066` 可重复应用。
- 内置浏览器创建唯一项目笔记，Agent 将标题、正文和置顶状态一次更新；完整刷新后从 PostgreSQL 回读新标题与正文，“只看置顶”过滤仍显示该记录。
- 内置浏览器在项目拍摄与通告页查询执行计划，Agent 返回 `4` 个执行阶段、`3` 个拍摄日和 `0` 项资源冲突。

精确清理与运行态：

- 精确删除验收笔记 `8d22c86f-1372-4523-902e-1abcdb877b9c`、Agent intent `457228e7-63b4-44c1-8760-6a313614161c` 与 `f662c21f-478b-4404-96c4-88cec8513517`、创建回执及审计 `4492-4497`；笔记、intent、回执和审计残留均为 `0`，完整刷新后验收标题不存在。
- 一次含 `undefined` 的错误自然语言请求在解析/选择阶段失败，没有写入 intent、回执或业务记录。
- 本轮没有启动新的持续进程，用户要求保留的现有 Web、API、PostgreSQL、MinIO 与 Worker 均未停止。

## 109. 2026-09-01 P1 第三方多模态 API 与 pgvector 链路

本轮将多模态识别与向量能力落实为第三方模型 API + 自有 PostgreSQL `pgvector`，不提供服务器本地模型回退：

- 转写使用 OpenAI-compatible `/audio/transcriptions`，OCR / 视觉识别使用 `/chat/completions`，embedding 使用 `/embeddings`；三项能力分别由 `TRANSCRIPTION_API_*`、`MULTIMODAL_API_*`、`EMBEDDING_API_*` 配置供应商、模型、基址、密钥和超时。
- FFmpeg / FFprobe 只负责探测与生成 WAV、PNG 派生输入；已删除 Worker 中 whisper.cpp / Tesseract 的本地执行解析代码和环境变量。历史已完成记录仍可读取旧工具标记，迁移会将未完成旧任务切换到第三方协议工具标记。
- Provider 使用原生 `fetch`，不新增模型 SDK；拒绝含 userinfo、query 或 fragment 的 API 基址，禁止自动重定向，非 2xx、无效 JSON、无效响应和网络/超时错误均转换为不含 API Key 或供应商正文的稳定错误。
- PostgreSQL 使用 pgvector `0.8.6` 保存第三方 embedding；向量记录绑定 asset、来源 revision、来源类型、内容 SHA-256、供应商、模型和维度，语义检索继续强制团队、项目、当前素材 revision、模型和维度隔离。
- 确认 OCR / 转写结果时，在同一事务内创建 `asset_embedding_jobs`；Worker 使用 `FOR UPDATE SKIP LOCKED`、租约、最大尝试次数和退避消费任务。素材或分析来源变化时任务过期，达到最大尝试次数的过期租约转为失败，不再被无限认领。

迁移、工程与真实 PostgreSQL 验证：

- 迁移 `067_asset_embeddings.sql` 已启用 pgvector；`068_third_party_model_jobs.sql` 连续执行两次成功，第二次只报告既有列、表和索引，不产生重复对象。
- Provider 与 Worker 聚焦测试 `9/9`、素材 API 聚焦回归合计 `26/26`；完整 `pnpm lint`、`pnpm typecheck` 通过，API build 的 `tsc --noEmit` 通过。
- 完整普通测试通过：Web `56/56`、API `177/177`；真实 PostgreSQL 套件 `9` 个文件、`50/50` 通过。
- PostgreSQL 验证覆盖向量排序与 Actor Scope、确认分析后原子入队、成功写入 pgvector、瞬时失败重试、最终失败、最大尝试次数停止认领和素材 revision 变化后过期。
- 完整 Web 生产构建本轮未执行，因为现有 `next dev` 正使用同一 `.next` 目录且用户要求保持服务运行；Web 未改动，并以既有服务 HTTP `200`、内置浏览器可见页面与零控制台错误替代本轮在线回归。

数据、备份、运行态与验收边界：

- 迁移前备份位于 `output/backups/pgvector-migration-20260901-103643/shadowproducer-pre-pgvector.dump`，大小 `234045` 字节，SHA-256 `9A8CD391DACE2736123BBED7BDB25B1405E1CB8E4F074EB15178221B208EDFE8`，`pg_restore --list` 已验证包含 `440` 个 TOC 条目。
- 测试结束后 `asset_embeddings=0`、`asset_embedding_jobs=0`，无媒体分析或测试审计残留；没有创建对象存储制品。
- Web `3211` / PID `19516` 返回 HTML `200`，API `3220` / PID `43848` 的 `/healthz` 返回 `ok`，PostgreSQL `shadowproducer-postgres` 使用 `pgvector/pgvector:pg17` 且健康。本轮没有启动新的持续进程，现有服务未停止。
- 当前没有真实第三方供应商凭据，因此已证明配置隔离、HTTP 协议、响应校验、密钥不泄漏、任务状态机与 pgvector 持久化，但不能声称真实供应商模型质量、限流策略、费用或生产 API 端到端已经验收。

## 110. 2026-09-01 P1 资源库语义检索入口

本轮补齐了用户可用的查询链路，并继续遵守第三方模型 API + 自有 PostgreSQL `pgvector` 边界：

- `POST /v1/teams/:teamId/assets/semantic-search` 接收查询文本；服务先校验团队与可选项目权限，再调用配置的第三方 embedding API，避免越权请求消耗供应商额度。
- 查询向量只在 PostgreSQL pgvector 中执行相似度检索，并按团队、项目、provider、model、维度、素材当前 revision 隔离；应用服务器不加载或运行 embedding 模型。
- 资源库提供可逆的“关键词 / 语义”模式。语义查询包含 500ms 延迟触发、加载、错误、空态、匹配来源与相似度；第三方 API 未配置或不可用时明确返回 `503`，不静默降级为关键词或本地模型。

工程与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、API build 通过；Web `56/56`、API `179/179`、素材聚焦 `19/19`、PostgreSQL `50/50` 通过。
- 注入测试 Provider 后，查询规范化、项目权限、embedding 调用和 repository 隔离参数均通过；未配置真实 embedding 凭据时，浏览器显示明确的 `503` 错误态，切回关键词模式后恢复正常素材列表。
- 375x812 视口无页面横向溢出，模式控件与输入框均在视口内，控制台无 warning/error。
- 当前仍没有真实第三方供应商凭据，因此真实模型质量、供应商限流、费用与生产端到端兼容性仍待凭据就绪后验收。

## 111. 2026-09-01 P1 镜头视觉识别与时间码语义检索

本轮把视频镜头纳入既有第三方模型 API + 自有 PostgreSQL `pgvector` 链路，没有引入服务器本地模型：

- FFmpeg 继续只负责镜头边界检测和关键帧派生；每个镜头关键帧由 OpenAI-compatible `/chat/completions` 视觉 API 生成中文描述，描述随 `sequence/startUs/endUs` 保存到分析任务结果。
- 确认镜头分析后，按 `analysis_job_id + sequence` 原子创建多个 `vision` embedding 任务；Worker 继续调用第三方 `/embeddings`，并校验分析类型、镜头序号、描述内容、素材 checksum 与 revision。
- PostgreSQL pgvector 保存镜头描述的第三方向量；检索结果返回命中镜头的 `sequence` 与 `timecodeUs`，资源库可显示视觉模型、镜头描述和命中时间码，双击结果从对应镜头播放。
- 关键词检索也会匹配已确认的镜头描述。历史 `whisper_cpp_v1` / `tesseract_v1` 工具值只用于读取迁移前已完成记录，不是新任务路径、部署前提或本地回退。

迁移、工程、数据库与内置浏览器验证：

- `069_multimodal_shot_embeddings.sql` 扩展 `vision` 失败阶段和 embedding 来源，并把分析任务唯一键调整为 `(analysis_job_id, sequence)`；在已应用状态再次执行完整 `pnpm db:migrate` 成功。
- `pnpm lint`、`pnpm typecheck`、API build 全部通过；普通测试 Web `56/56`、API `179/179`，真实 PostgreSQL 套件 `9` 个文件、`51/51` 通过。
- PostgreSQL 回归确认两个镜头生成两个独立任务、第三方 embedding 响应写入 pgvector，并返回命中镜头 `sequence=1`、`timecodeUs=500000`；素材 revision 变化时旧向量任务仍按既有规则过期。
- 内置浏览器资源库可切换到语义模式；未配置真实第三方凭据时显示明确服务不可用状态，不回退到关键词或本地模型。`1329x912` 视口无页面溢出，控制台无 warning/error。

验收边界：

- 当前向量来源是第三方视觉 API 生成的镜头描述文本，不宣称支持供应商原生图像 embedding；这已覆盖按画面语义检索和时间码定位的 P1 需求。
- 当前仍没有真实第三方供应商凭据，因此不能声称真实模型描述质量、向量召回质量、供应商限流、费用或生产 API 端到端已经验收。
- 测试后 `asset_embedding_jobs=0`、`asset_embeddings=0`，活跃 embedding 与媒体分析队列均为 `0`；精确清理一组早先中断的作品集 PostgreSQL 测试夹具后，`pg-*` 团队、账号、素材、分析任务和审计残留均为 `0`。该夹具只直接写入数据库，没有创建对象存储制品。
- 本轮没有启动新的持续进程；既有 Web `3211` / PID `19516` 返回 HTML `200`，API `3220` / PID `10328` 的 `/healthz` 返回 `ok`，按用户要求继续保留运行。

## 112. 2026-09-01 P1 多模态架构消歧与审片版本事实源

本轮再次固化多模态边界，并修正审片跨版本语义：

- 多模态识别、OCR、转写和 embedding 继续统一依赖第三方 API；服务器只做媒体预处理、任务编排、结果校验和持久化，PostgreSQL `pgvector` 只保存和检索第三方 API 返回的向量，不部署或回退到本地模型。
- 第 95 节已明确改名为迁移前历史验证；代码中的 `whisper_cpp_v1` / `tesseract_v1` 只为已完成历史记录的审计与读取兼容保留，不是可执行工具或部署能力。
- 团队审片评论的 `version` 改由 `review_files.version` 决定，客户端提交的版本字符串不再能制造评论与文件版本不一致。
- 评论对应关系改为比较两个评论所属审片文件的真实版本；不同文件 ID 但相同版本会被拒绝，建议接口也不再为同版本文件生成跨版本建议。
- 审片 UI 的 B 菜单只显示与 A 真实不同的版本；切换 A 后若当前 B 变成同版本，会自动退出对比。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；Web `56/56`、API `179/179`，Next.js 16.3.2 生产构建成功。
- PostgreSQL 套件 `9` 个文件、`52/52` 通过；回归覆盖伪造评论版本被数据库文件版本覆盖、同版本建议被拒绝、同版本评论链接被拒绝，以及真实不同版本的创建、幂等回放和解除。
- 内置浏览器打开 v12 审片后，B 菜单只出现 v11；选择后双版本合并视图正常加载。`1329x912` 下页面尺寸等于视口，无溢出或可见错误。
- 测试结束后 `pg-*` 账号、团队、项目、审片文件、评论、评论链接、媒体分析任务和 embedding 任务残留均为 `0`。
- 本轮没有启动新的持续进程；既有 Web `3211` / PID `19516` 返回 `200`，API `3220` / PID `26500` 的 `/healthz` 返回 `ok`，PostgreSQL 与 MinIO 均为 `healthy`，按用户要求继续保留运行。

## 113. 2026-09-01 P1 作品集发布通知与可发布候选一致性

本轮补齐作品集发布/下架通知闭环，并修正候选列表与发布校验不一致：

- 新增 `portfolio_published` 与 `portfolio_unpublished` 团队级通知；发布者之外的团队成员会收到通知，`project_id` 保持 `NULL`，去重键绑定作品集、动作和 revision。
- 通知遵守“作品集发布与下架”偏好；未配置偏好时默认启用，关闭后不会生成后续作品集通知。
- 通知面板展示发布者与时间，不再为团队级通知渲染空项目；点击后先标记已读，再进入团队作品集页。
- 作品集候选查询现在与发布校验一致，要求源素材 `object_key` 非空；缺少可用源文件的已通过审片版本不再先出现在候选中、到发布时才失败。
- `070_portfolio_notifications.sql` 已在应用状态下再次执行完整 `pnpm db:migrate`，迁移器正确跳过已应用版本并完成确定性 seed。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `56/56`、API `179/179`、PostgreSQL `53/53`，Next.js 16.3.2 生产构建成功。
- PostgreSQL 回归覆盖发布、幂等回放、下架、团队级通知、偏好抑制，以及无 `object_key` 素材不进入候选。
- 内置浏览器以顾遥发布和下架临时作品集；繁星收到两类通知，发布通知点击后进入作品集页，下架后完整刷新显示“团队可见”，两条通知均可标记已读。
- 通知设置中“作品集发布与下架”偏好可见且默认选中；通知正文显示顾遥与时间，没有空项目占位。

精确清理与运行态：

- 精确删除临时作品集 `714eb408-279d-4b4d-9487-f3c848d84817`、4 条通知、4 条审计、4 份命令回执和作品集内容；相关残留均为 `0`。
- 种子素材 `asset-winter-main-v11` 仅在验收期间临时获得对象键，清理后已恢复为 `NULL`；没有创建对象存储制品或持久化虚假媒体。
- 本轮没有启动新的持续进程；既有 Web `3211` 与 API `3220`、PostgreSQL、MinIO 继续运行。

## 114. 2026-09-01 P1 客户审片下载权限与附件交付

本轮封堵了客户审片链接的下载权限绕过，同时保留视频在线播放：

- `GET /review/:linkId/files/:fileId/content-url` 默认继续返回 inline 播放 URL；只有显式 `download=1` 才进入下载分支。
- 下载分支在应用服务内强制校验会话的 `canDownload`，拒绝时返回 `403 REVIEW_SCOPE_DENIED`，不会调用对象存储。
- 允许下载时调用既有 `AssetStorage.createDownloadUrl(..., { attachment: true })`，S3/MinIO 预签名请求使用 `Content-Disposition: attachment`；未新增路由、依赖或本地媒体服务。
- 播放和下载分别记录 `review-file.content-url` 与 `review-file.download-url` 审计，下载拒绝与成功均有独立 outcome。
- 公开审片页不再把播放器 URL 直接作为下载链接；点击下载时单独请求附件 URL，并提供进行中与错误反馈。

工程、数据库与内置浏览器验证：

- 聚焦审片 API `2/2` 通过；完整 `pnpm lint`、`pnpm typecheck`、`pnpm build` 通过，Web `56/56`、API `179/179`、PostgreSQL `53/53`。
- 回归覆盖 `canDownload=false` 时播放 `200`、下载 `403`、storage 零调用和 denied audit；覆盖 `canDownload=true` 时 attachment options 与 success audit。
- 内置浏览器创建带下载权限的临时客户审片链接，完成访客邮箱验证码验证、视频在线播放和下载点击；页面保持在审片工作区，数据库回读到同一会话的 content-url 与 download-url success 审计。

精确清理与运行态：

- 精确删除验收链接 `ab9d987b-b2f2-44eb-892e-08cb54caf87e`、会话、验证码和 `6` 条审计；删除 MinIO 对象 `acceptance/p1-review-download.mp4` 与本地临时视频，`asset-winter-main-v11.object_key` 恢复为 `NULL`。
- 清理后链接、会话、验证码、审计和对象存储残留均为 `0`，本地临时文件不存在。
- 本轮没有启动新的持续进程；既有 Web `3211` / PID `19516` 返回 `200`，API `3220` / PID `20192` 的 `/healthz` 返回 `ok`，按用户要求继续保留运行。

## 115. 2026-09-01 P1 审片评论与批准通知

本轮补齐并验证了成员与客户访客产生审片活动后的团队通知：

- 新增审片意见与版本批准使用独立通知类型，并遵守“审片评论与批准”偏好；动作发起者不会收到自己的通知，只有同时具备项目读取与审片写入或管理权限的成员会收到。
- 客户访客没有内部账号时，通知仍保留访客显示名；幂等回放不会重复创建通知。
- 通知点击后进入对应项目审片页，不再落到拍摄与通告页。

工程、PostgreSQL 与内置浏览器验证：

- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 均通过；Web `56/56`、API `179/179`、PostgreSQL `55/55`。
- 以顾遥在 `winter-coffee/main-v12` 创建临时意见；繁星的通知面板显示“新增审片意见”、来源“顾遥”和正确版本/时间码，通知设置显示已启用的“审片评论与批准”。
- 点击该通知后，地址从 `#/project/north/winter-coffee/schedule` 切换为 `#/project/north/winter-coffee/reviews`。

精确清理与运行态：

- 精确删除临时评论 `6495bd7e-38f4-42a0-8ae3-99a232ad24e9`、两条通知和一条审计；`p1-review-notification-*` 评论、通知、审计与命令回执残留均为 `0`。
- 本轮没有启动新的持续进程；既有 Web `3211` 与 API `3220`、PostgreSQL、MinIO 继续运行。

## 116. 2026-09-01 P1 审片评论线程

本轮补齐审片意见的一层回复线程，并保持第三方多模态 API + 自建 PostgreSQL `pgvector` 的既定架构不变：

- 团队成员与公开审片访客均可回复根评论；回复不能继续嵌套，只有根评论可解决和重新打开。
- 回复不参与跨版本评论对应，避免把讨论内容误判为独立审片意见。
- 新回复生成 `review_comment_replied` 通知并携带 `parentCommentId`；点击通知进入对应项目审片页。
- 迁移 `072_review_comment_threads.sql` 已支持评论父子关系与约束；多模态识别、OCR、转写和 embedding 仍只调用第三方 OpenAI-compatible API，服务器不部署或回退本地模型，PostgreSQL `pgvector` 只保存和检索第三方 API 返回的向量。
- `whisper_cpp_v1` 与 `tesseract_v1` 仅为迁移前已完成记录的读取兼容值，不是可执行工具、部署能力或失败回退路径。

工程、PostgreSQL 与内置浏览器验证：

- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 均通过；Web `56/56`、API `179/179`。
- 认证成员根评论完成解决后重新打开，最终状态为 `open`、revision 为 `3`；公开访客根评论与一层回复均通过真实 HTTP 会话持久化，并同时出现在公开与成员接口。
- 内置浏览器确认成员与访客回复显示在各自根评论下；通知面板显示“新增审片回复”、正确文件版本与时间码，点击后进入 `#/project/north/winter-coffee/reviews`。

精确清理与运行态：

- 精确清理 `4` 条临时审片评论、`10` 条通知、`13` 条审计、`1` 个审片链接、`2` 个访客会话、`2` 个身份验证码和 `5` 个临时认证会话；相关残留均为 `0`。
- 删除 MinIO 临时对象 `teams/north/p1-acceptance/review-thread.mp4`，并将 `asset-winter-main-v11.object_key` 恢复为 `NULL`；本地临时视频与测试脚本均不存在。
- 本轮没有启动新的持续进程；既有 Web `3211` / PID `19516` 与 API `3220` / PID `23156` 按用户要求继续运行。

## 117. 2026-09-01 P1 Worker 运行态与第三方模型边界

本轮确认 P1 的模型与向量链路遵循以下唯一架构：

- 多模态识别、OCR、语音转写和 embedding 生成均调用第三方 OpenAI-compatible API；服务器只运行 FFmpeg / FFprobe 预处理、队列编排、结果校验、审计和持久化，不部署或回退到本地模型。
- “本地向量检索”仅指 PostgreSQL `pgvector` 位于自有服务端。素材向量与查询向量均由第三方 API 生成，`pgvector` 只负责保存和相似度检索。
- 第三方 API 未配置、不可用、超时或限流时进入明确失败或可重试状态，不静默切换到关键词检索、本地 embedding 或其他供应商。
- `whisper_cpp_v1` 与 `tesseract_v1` 仅用于读取迁移前已完成记录，不是当前 Worker 可执行工具。

运行态与验收证据：

- Analysis Worker 根 PID `43924`、Node PID `29024`；Media Worker 根 PID `41712`、Node PID `39188`，均自 `2026-09-01 14:44:45 +08:00` 持续运行。日志分别位于 `output/p1-analysis-worker-live.*.log` 与 `output/p1-media-worker-live.*.log`。
- Analysis Worker 实际消费任务 `7bfac1b4-edb6-4576-9264-18561fe8a45c`，状态从 `queued` 进入 `awaiting_confirmation`，生成 `5` 条拆解候选；随后精确清理该任务、候选、审计和命令回执，`winter-coffee` 原有 `6` 条拆解未受影响。
- Media Worker 在空队列下稳定轮询；`analysis_jobs`、`media_analysis_jobs`、`asset_embedding_jobs`、`media_processing_jobs` 当前记录数均为 `0`。两类 Worker 的 stderr 只有包管理器启动命令，无运行错误。
- 内置浏览器以“雨夜车站”触发语义搜索；未配置 embedding API 时显示“语义检索失败 / 语义检索服务暂时不可用”，没有回退到关键词或本地模型，控制台 warning/error 为 `0`。
- `pnpm lint`、`pnpm typecheck`、`pnpm test` 通过；Web `56/56`、API `179/179`。当前没有真实第三方供应商凭据，因此未宣称模型质量、向量召回质量、供应商限流、费用或生产兼容性已经验收。

运行态复核：

- Web `3211` / PID `19516` 返回 HTML `200`，API `3220` / PID `23156` 的 `/healthz` 返回 `ok`。
- 两个 Worker 按用户要求继续保留运行；四类活动队列在复核时均为 `0`。

## 118. 2026-09-01 P1 Agent 资源语义检索

本轮把既有资源语义检索接入 Agent，同时保持唯一的模型与向量架构：

- Agent 可将“语义搜索 / 多模态搜索资源”解析为 `semantic_search_assets`，并复用既有 `AssetService.semanticSearchText`；没有新增搜索抽象、依赖或本地模型分支。
- preview 只校验 Actor、团队、项目和查询参数，不调用 embedding API；execute 才请求第三方 embedding API，并将返回的查询向量交给 PostgreSQL `pgvector` 检索。
- 素材向量和查询向量都由第三方 API 生成；检索继续按 team、project、provider、model、dimensions 和素材 revision 隔离。第三方 API 未配置时明确返回 `503 SEMANTIC_SEARCH_UNAVAILABLE`，不回退到关键词、本地 embedding 或服务器模型。
- 迁移 `073_agent_semantic_search_command.sql` 已应用，将新 action 纳入数据库约束。

工程与真实运行态验证：

- Agent 自然语言解析聚焦测试 `34/34`，API Agent 聚焦测试 `79/79`；回归确认 preview 零次调用 embedding、首次 execute 一次调用、命令重放不重复调用。
- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test` 通过；Web `57/57`、API `180/180`。
- 内置浏览器使用繁星的真实登录会话提交“在资源库语义搜索‘雨夜车站’，返回 8 条”；API 日志确认 preview `201`、execute `503`，界面显示“语义检索服务暂时不可用”，没有返回关键词结果或本地模型结果。
- 精确删除本次验收 intent `d0c5a9e8-bee4-40a5-b2ba-2080ae8b879d` 及其 `1` 条 `agent.previewed` 审计；两类残留均为 `0`。
- 当前没有真实第三方供应商凭据，因此不能宣称真实模型质量、向量召回质量、供应商限流、费用或生产 API 兼容性已经验收。
- Web `3211` / PID `19516` 与 API `3220` / PID `42512` 均返回 `200`；Analysis Worker PID `29024`、Media Worker PID `39188` 持续运行，四类活动队列均为 `0`。现有服务按用户要求继续保留。

## 119. 2026-09-01 P1 回收站永久删除与模型服务边界

本轮补齐回收站的最终销毁闭环，并再次固化多模态与向量架构：

- 回收站支持对已删除的任务、日程、笔记、团队联系人和供应商执行永久删除；请求必须携带 `expectedRevision` 和确认字面量 `permanent-delete`。
- 服务端继续校验团队写权限和实体 revision；PostgreSQL 在单个事务内删除实体及关联记录，同时保留 `*.permanently-deleted` 审计证据。
- 回收站界面提供“恢复 / 永久删除”，永久删除使用居中确认对话框并明确告知操作不可撤销。
- 多模态识别、OCR、语音转写和 embedding 生成只依赖可配置的第三方 OpenAI-compatible API；服务器不部署、执行或回退到本地模型。
- “本地向量检索”仅指自有 PostgreSQL `pgvector` 存储和检索第三方 API 生成的素材向量与查询向量；第三方 API 未配置或不可用时显式失败，不伪造本地识别或搜索结果。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦 Workspace API `11/11` 与 PostgreSQL Workspace `14/14` 通过；完整 `pnpm lint`、`pnpm typecheck`、`pnpm test` 通过，Web `57/57`、API `180/180`。
- 完整 PostgreSQL 门禁 `9` 个测试文件、`56/56` 通过；`pnpm build` 通过，Next.js `16.3.2` 生产构建成功。
- 内置浏览器验证回收站的两个操作、不可撤销确认和完成后空态；控制台 warning/error 为 `0`。

精确清理与运行态：

- 验收标记 `acceptance-recycle-permanent-20260901-1537` 对应实体与临时审计残留均为 `0`，未删除其他回收站或团队数据。
- `analysis_jobs`、`media_analysis_jobs`、`asset_embedding_jobs`、`media_processing_jobs` 当前均为 `0`。
- Web `3211` / PID `19516` 返回 HTML `200`，API `3220` / PID `40596` 的 `/healthz` 返回 `ok`；Analysis Worker PID `29024` 与 Media Worker PID `39188` 持续运行，现有服务按用户要求继续保留。
- 当前仍未提供真实第三方供应商凭据，因此不声称模型质量、向量召回质量、限流、费用或生产 API 兼容性已验收。

## 120. 2026-09-01 P1 Agent 个人联系人与第三方模型边界复核

本轮补齐 Agent 对个人联系人的完整管理入口，并复用既有联系人业务服务：

- Agent 支持列出、创建、更新、共享、取消共享、删除和恢复个人联系人；没有新增第二套联系人模型、权限规则或持久化路径。
- 列表为只读动作；创建、更新和恢复为普通写入；共享、取消共享和删除为高风险动作，必须先完成确认，服务端会在执行时重新检查 Actor、团队权限和目标 revision。
- 迁移 `074_agent_personal_contact_commands.sql` 已应用，数据库 action 约束包含全部七个个人联系人动作。
- 多模态识别、OCR、转写和 embedding 仍只调用可配置的第三方 OpenAI-compatible API。服务器不部署或回退本地 AI 模型；“本地向量搜索”仅指 PostgreSQL `pgvector` 保存并检索第三方 API 生成的素材向量与查询向量。
- 第三方 API 未配置、不可用、超时或限流时显式失败或进入可重试状态；语义搜索不会自动降级为关键词、本地 embedding 或其他供应商。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `58/58`、API `182/182`、PostgreSQL `56/56`，Next.js `16.3.2` 生产构建成功。
- API 回归覆盖个人联系人列表、创建、更新，以及共享和删除在确认前被拒绝；自然语言解析覆盖全部七个动作。
- 内置浏览器通过 Agent 创建 `P1 Agent 联系人 20260901-1600`，刷新个人联系人页后仍能从服务端读回；精确清理后再次刷新，页面中该标记数量为 `0`，控制台 warning/error 为 `0`。
- 当前数据库四类活动队列 `analysis_jobs`、`media_analysis_jobs`、`asset_embedding_jobs`、`media_processing_jobs` 均为 `0`。

精确清理与运行态：

- 精确删除验收联系人 `d6a18e8f-e0c3-43a3-b741-daa0cf12a93b`、Agent intent `fc76067c-57d0-4b46-9581-3abd6b5567ab`、对应命令回执和审计 `5867`；四类残留均为 `0`。
- Web `3211` / PID `19516` 返回 HTML `200`，API `3220` / PID `17328` 的 `/healthz` 返回 `ok`；Analysis Worker PID `29024` 与 Media Worker PID `39188` 持续运行，PostgreSQL 与 MinIO 均为 `healthy`。
- 当前仍未提供真实第三方供应商凭据，因此不声称模型质量、向量召回质量、限流、费用或生产 API 兼容性已经验收。

## 121. 2026-09-01 P1 Agent 审片反馈读取与跨版本整理

本轮补齐 Agent 的只读审片反馈入口，并复用既有审片业务服务：

- Agent 可列出项目审片版本、读取指定版本评论，并在指定主版本与对比版本时返回双方评论、已确认对应关系和系统建议对应关系。
- preview 只解析并唯一匹配审片版本；execute 复用 `ProductionService.listReviewFiles`、`listReviewComments` 与 `listReviewCommentCorrespondence`，没有新增第二套评论模型、仓储或对应算法。
- 动作为 `read` 风险且不需要确认；执行结果仍持久化到 Agent intent，重放不会再次读取或产生不同结果。
- 迁移 `075_agent_review_feedback_command.sql` 已应用，数据库 action CHECK 约束已实际回读并包含 `list_review_feedback`。
- 多模态识别、OCR、转写和 embedding 继续只依赖第三方 OpenAI-compatible API；服务器不部署或回退本地 AI 模型。本地 PostgreSQL `pgvector` 只存储和检索第三方 API 生成的素材向量与查询向量。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `183/183`、PostgreSQL `56/56`，Next.js `16.3.2` 生产构建成功。
- API 回归覆盖双版本 preview、无需确认的 execute、双方评论过滤、对应建议和已存结果重放；自然语言解析覆盖仅列出版本和主/对比版本整理。
- 内置浏览器以繁星真实会话提交“整理审片反馈；主版本：冬夜咖啡_主片 v12；对比版本：冬夜咖啡_主片 v11”，界面返回 `4` 条根反馈和 `3` 条待处理重点；控制台 warning/error 为 `0`。

精确清理与运行态：

- 精确删除验收 intent `f2b706dd-284d-4dda-863b-44545b3f9245` 及审计 `6157`、`6158`；intent、audit、receipt 残留均为 `0`，没有创建或修改审片业务数据。
- `analysis_jobs`、`media_analysis_jobs`、`asset_embedding_jobs`、`media_processing_jobs` 四类活动队列均为 `0`。
- Web `3211` / PID `19516` 返回 HTML `200`，API `3220` / PID `39104` 的 `/healthz` 返回 `ok`；Analysis Worker PID `29024` 与 Media Worker PID `39188` 持续运行，PostgreSQL 与 MinIO 均为 `healthy`，现有服务按用户要求继续保留。

## 122. 2026-09-01 P1 个人联系人永久删除与外部模型边界

本轮补齐个人联系人回收站的最终销毁闭环，并核对多模态架构没有服务器本地模型路径：

- 个人联系人只有在当前账号拥有、已经软删除且 revision 匹配时才能永久删除；HTTP 请求还必须携带确认字面量 `permanent-delete`。
- PostgreSQL 在同一事务内删除联系人、团队共享投影、供应商引用和制片拆解联系人引用，并保留 `contact.permanently-deleted` 审计证据。
- 已删除联系人界面提供恢复与永久删除两个独立动作；永久删除使用不可撤销确认对话框，完成后重新读取服务端回收站。
- 多模态识别、OCR、语音转写和素材/查询 embedding 均由第三方 OpenAI-compatible API 生成；服务器不部署、执行或回退本地 AI 模型。
- PostgreSQL `pgvector` 只保存第三方 API 返回的向量并执行相似度检索。第三方 API 未配置、不可用、超时或限流时显式失败，不切换关键词、本地模型或其他供应商。
- `whisper_cpp_v1` 与 `tesseract_v1` 仅保留为迁移前已完成记录的读取兼容值，当前任务创建路径和 Worker 均不能执行它们。

工程、PostgreSQL 与内置浏览器验证：

- 聚焦联系人 API `10/10`、PostgreSQL 联系人仓储 `7/7` 通过；回归覆盖缺少确认、错误所有者、过期 revision、关联清理和永久删除审计。
- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `184/184`、PostgreSQL `57/57`，Next.js `16.3.2` 生产构建成功。
- 内置浏览器以繁星真实会话完成“新建联系人 -> 删除 -> 已删除列表 -> 永久删除”；完成后已删除列表回读为空，控制台 warning/error 为 `0`。
- 当前仍没有真实第三方供应商凭据，因此不声称模型识别质量、向量召回质量、限流、费用或生产 API 兼容性已经验收；服务器本地模型不属于待部署项或回退方案。

精确清理与运行态：

- 精确删除验收联系人 `dd2b5f4e-0571-461d-8cc8-a0cefc758c53` 的创建回执和审计 `6359`、`6360`、`6361`；联系人、回执和审计残留均为 `0`。
- `analysis_jobs`、`media_analysis_jobs`、`asset_embedding_jobs`、`media_processing_jobs` 四类活动队列均为 `0`。
- 本轮没有启动新的持续进程；既有 Web、API、Analysis Worker、Media Worker、PostgreSQL 与 MinIO 按用户要求继续运行。

## 123. 2026-09-01 P1 第三方模型与向量检索边界消歧

本轮修正产品表达，不改变已经成立的运行架构：

- 多模态识别、OCR、语音转写以及素材/查询 embedding 均依赖第三方 OpenAI-compatible API；服务器不部署、执行或回退本地 AI 模型。
- 自有 PostgreSQL `pgvector` 只保存并检索第三方 API 生成的向量。第三方 API 未配置、不可用、超时或限流时显式失败，不生成本地向量，也不降级为关键词结果。
- 资源库语义检索加载态明确标注第三方模型 API；识别详情将含歧义的“运行时”拆成“API 提供方”和“API 协议”，继续显示具体模型。
- 历史台账中的“第三方 API + 本地向量数据库”统一改为“第三方模型 API + 自有 PostgreSQL `pgvector`”，避免把服务端向量存储误读成服务器本地模型推理。

验证与运行态：

- Web lint、Web typecheck 通过；第三方 Provider 与素材语义检索聚焦测试 `24/24` 通过。
- 内置浏览器在资源库执行语义搜索；未配置第三方 embedding 凭据时明确显示“语义检索服务暂时不可用”，没有返回关键词或本地模型结果，控制台 warning/error 为 `0`。
- 本轮未创建或修改业务数据，未启动新的持续进程；既有服务继续保留。

## 124. 2026-09-01 P1 作品集永久删除

本轮补齐团队作品集回收站的最终销毁闭环：

- 只有已归档作品集可以永久删除；请求必须包含匹配的 `expectedRevision` 与确认字面量 `permanent-delete`，并继续要求 `portfolio.write` 权限。
- PostgreSQL 在单个事务中锁定归档记录、清理该作品集的发布通知和旧命令回执，再删除 `team_portfolios`。数据库外键级联清理 `portfolio_items`、`portfolio_daily_views` 与 `portfolio_daily_visitors`。
- 共享 `team_assets`、`review_files` 和对象存储文件不随作品集删除；历史审计与 Agent intent 保留，并新增 `portfolio.permanently-deleted` 审计。
- 回收站卡片提供独立的恢复和永久删除操作。永久删除使用项目既有 Radix Dialog 与 Button 契约，包含不可撤销说明、pending/error 状态、键盘焦点管理和无障碍名称。

工程、数据库与内置浏览器验证：

- 作品集 API 聚焦测试 `9/9`、PostgreSQL 作品集仓储 `5/5` 通过；覆盖缺少确认 `400`、只读成员 `403`、未归档/不存在 `404`、过期 revision `409`、成功删除、恢复已删除对象 `404`。
- PostgreSQL 验证作品集内容、访问统计、发布通知和旧回执均清零；共享素材与审片版本仍存在；历史归档审计保留并追加永久删除审计；旧 public slug 与 custom domain 可重新使用。
- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `185/185`、PostgreSQL `58/58`，Next.js `16.3.2` 生产构建成功。
- 内置浏览器在 `1329x912` 验证作品集回收站：删除入口具备明确无障碍名称；确认弹窗中心偏差为 `0/0`；初始焦点落在“取消”，关闭后返回删除触发按钮；控制台 warning/error 为 `0`。

精确清理与运行态：

- 临时作品集 `p1-browser-fed6d358-c2e5-4991-bbe7-8cfb4ba836f6` 已通过真实 repository 永久删除；作品集残留为 `0`，该验收记录产生的审计已精确清理为 `0`，页面刷新后不再出现验收标记。
- Web `3211` / PID `19516` 与 API `3220` / PID `27428` 正常监听，API `/healthz` 返回 `ok`；PostgreSQL 与 MinIO 均为 `healthy`。
- 本轮未启动新的持续进程；既有 Web、API、Analysis Worker、Media Worker、PostgreSQL 与 MinIO 按用户要求继续运行。

## 125. 2026-09-01 P1 审片版本永久删除

本轮补齐审片回收站的最终销毁闭环，并保持共享素材边界不变：

- 只有已归档审片版本可以永久删除；请求必须包含匹配的 `expectedRevision` 与确认字面量 `permanent-delete`，并继续要求 `review.manage` 写权限。
- 作品集仍引用该版本时返回 `409 REVIEW_FILE_IN_USE`，界面明确提示先从作品集中移除，不依赖数据库约束错误作为用户反馈。
- PostgreSQL 在单个事务中清理该版本的审片活动通知、旧命令回执、评论、评论对应关系和审片链接文件映射，再删除 `review_files`；历史审计保留并新增 `review-file.permanently-deleted`。
- 共享 `team_assets`、媒体派生对象、审片链接容器和作品集本身不随审片版本删除。
- 回收站删除入口复用既有 Button 与 Radix Dialog 契约；确认框说明评论会被删除、素材库源文件不受影响且操作无法撤销。

工程、数据库与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `186/186`、PostgreSQL `59/59`，Next.js `16.3.2` 生产构建成功。
- API 与真实 PostgreSQL 回归覆盖未归档/不存在、确认字面量、只读成员、过期 revision、作品集引用阻塞、评论与链接级联、源素材保留和永久删除审计。
- 内置浏览器在 `1329x912` 完成“归档 -> 回收站 -> 作品集引用阻塞 -> 解除引用 -> 永久删除”；确认框中心偏差为 `0/0`，初始焦点落在“取消”，按 `Escape` 后焦点返回永久删除触发按钮。
- 数据库回读确认临时审片版本为 `0`、临时作品集引用为 `0`、旧命令回执为 `0`，源素材在删除审片版本后仍为 `1`，并保留创建、归档和永久删除三条审计证据。

精确清理与运行态：

- 临时审片版本 `8e11a864-259a-444e-a604-d4eb670f474d` 已通过真实 UI 永久删除；临时引用 `acceptance-review-delete-block-20260901-175322`、临时素材 `acceptance-review-delete-20260901-175322` 及对应三条验收审计已精确清理。
- 本轮没有启动新的持续进程；既有服务按用户要求继续保留。
- 多模态识别、OCR、语音转写和 embedding 仍只依赖第三方 OpenAI-compatible API；自有 PostgreSQL `pgvector` 只保存和检索第三方 API 生成的向量，服务器本地模型不属于部署项或回退方案。

## 126. 2026-09-01 P1 联系人共享通知

本轮为已经存在的个人联系人共享业务补齐真实通知写点：

- 个人联系人共享更新与撤销分别写入 `contact_share_updated`、`contact_share_revoked`，仅发送给当前团队的其他成员，明确排除操作者。
- 通知与联系人 revision、共享投影和审计处于同一个 PostgreSQL 事务；失败时整体回滚。
- 通知尊重成员级“联系人共享与撤销”偏好，未建立偏好记录时默认开启；关闭后不再为该成员写入新通知。
- 去重键包含通知类型、团队、联系人和 revision，避免同一收件人在多个团队中发生跨团队误去重。
- 通知点击进入当前团队资源库；没有新增联系人深链、第二套通知服务或空壳业务写点。

工程、数据库与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `186/186`、PostgreSQL `60/60`，Next.js `16.3.2` 生产构建成功。
- PostgreSQL 联系人仓储聚焦测试 `8/8` 通过；覆盖共享、撤销、偏好关闭和包含团队范围的去重键。
- 修正媒体 embedding PostgreSQL 测试的客户端/数据库时间边界：将待领取时间设为确定的过去值；素材聚焦测试 `4/4` 与 PostgreSQL 全套均稳定通过，生产第三方 embedding 任务逻辑未改。
- 内置浏览器以繁星真实会话收到顾遥共享联系人产生的通知，显示来源、共享字段与未读状态；从看板点击通知后进入 `#/team/north/resources`，控制台 error 为 `0`。
- 联系人通知偏好默认显示开启；服务层临时关闭后，第二次共享对繁星写入通知数为 `0`，浏览器显示未勾选；清理偏好记录后恢复默认开启。

精确清理与运行态：

- 两个临时联系人及其共享、通知、审计、命令回执和临时偏好记录均按 ID/幂等键精确删除，六类残留回读均为 `0`；页面中临时通知数量为 `0`，未读强调消失。
- 本轮未启动新的持续进程；既有 Web、API、PostgreSQL 与 MinIO 按用户要求继续运行。
- 多模态识别、OCR、语音转写和 embedding 仍只调用第三方 OpenAI-compatible API；服务器不部署或回退本地模型，自有 PostgreSQL `pgvector` 只存储并检索第三方 API 生成的向量。

## 127. 2026-09-01 P1 第三方模型执行契约收紧

本轮把产品、架构、配置和数据库执行契约统一到同一边界：

- 多模态识别、OCR、语音转写以及素材/查询 embedding 只调用第三方远程模型 API；首版实现采用 OpenAI-compatible `/audio/transcriptions`、`/chat/completions` 和 `/embeddings`。
- 服务器只负责权限与发送策略、FFmpeg / FFprobe 派生输入、任务编排、结果校验、审计、重试，以及 PostgreSQL `pgvector` 存储与相似度检索；服务器不部署、执行或回退本地 AI 模型。
- `CreateMediaAnalysisCommand` 只接受 FFmpeg 派生工具和 OpenAI-compatible API 工具。数据库拒绝 `whisper_cpp_v1`、`tesseract_v1` 进入 `queued`、`processing` 或 `failed` 等可再次执行状态，仅保留不可再次执行的迁移前历史记录可读。
- 第三方供应商未配置、不可用、超时或限流时继续显式失败，不降级为关键词检索、本地向量或未经批准的其他供应商。

工程与真实 PostgreSQL 验证：

- `pnpm db:migrate` 幂等执行通过。
- 新增数据库约束回归：旧本地工具标记写入 `queued` 返回 PostgreSQL `23514`，`completed` 历史记录仍可写入并读取；素材 PostgreSQL 聚焦测试 `5/5` 通过。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres` 全部通过；Web `59/59`、API `186/186`、PostgreSQL `61/61`。
- 本轮没有修改 Web 运行代码，因此没有重复执行生产构建或浏览器交互回归；现有 Web `3211` 返回 HTML `200`，API `3220` 的 `/healthz` 返回 `ok`。
- 当前仍没有真实第三方供应商凭据，因此不声称模型识别质量、向量召回质量、限流、费用或生产 API 兼容性已经验收；这些是环境验收项，不是本地模型部署项。

## 128. 2026-09-01 P1 团队数据导出完整性

本轮补齐团队可见业务数据导出的 P1 字段覆盖，同时保持敏感数据与运行态数据边界：

- 通知偏好导出覆盖任务、日历、作品集发布、审片活动、联系人共享和制片执行六类设置。
- 媒体分析导出包含第三方 API provider、协议版本、模型、语言、结果文本和分段；素材 embedding 导出来源文本、provider、模型、维度和 revision 元数据，不导出高维向量数组。
- 审片版本导出归档时间，评论导出父评论 ID；作品集导出主题、SEO、自定义域名与验证状态，并新增按日聚合访问统计。
- PostgreSQL `date` 以数据库文本值导出为 `YYYY-MM-DD`，避免 JavaScript 时区换算导致日期前移。
- 继续排除对象存储 Key、访客哈希、认证凭据、外部审片 token/session、Agent intent、命令回执、Worker lease 和 embedding 向量数组。

工程、PostgreSQL 与内置浏览器验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres` 全部通过；Web `59/59`、API `186/186`、PostgreSQL `61/61`。
- 内置浏览器在团队“审计记录”点击“导出可见数据”，页面回显 `已下载 北岸影像-2026-09-01-业务数据.json`；下载文件可解析，格式为 `shadowproducer-business-data`、版本 `1`、团队 `north`，包含 `48` 个业务分区。
- 下载结构包含 `notificationPreferences`、`assetEmbeddings` 与 `portfolioDailyViews`；当前真实账号样本中三者均为空数组，因此字段级和值级覆盖由 PostgreSQL 集成测试验证。
- 下载文件扫描确认不含对象存储 Key、访客哈希或 `embedding` 向量数组；浏览器控制台没有新增 warning/error。

运行态：

- 本轮没有启动新的持续进程；现有 Web `3211` 与 API `3220` 保持运行，API `/healthz` 返回 `ok`。
- 当前仍没有真实第三方供应商凭据，因此不声称模型识别质量、向量召回质量、限流、费用或生产 API 兼容性已经验收。

## 129. 2026-09-01 P1 高级脚本版本影响分析

本轮将脚本版本影响分析从“来源版本粗略匹配”收紧为可解释的来源行追踪：

- 正式拆解项只有在来源版本等于基线版本、来源位置可解析为 `第 N 行`、`第 N-M 行` 或 `第 N 至 M 行`，且该范围与修改或删除的基线行重叠时，才进入确定受影响结果。
- 纯新增内容没有基线行范围，不会误报已有拆解项；待确认、已取消、其他项目和未变化来源行继续排除。
- 来源位置无法解析的正式拆解项单列“需要人工核对”，并将处理状态标记为 `limited`，不使用关键词或语义推断补猜。
- 受影响拍摄日和通告通过已受影响拆解项的现有关系去重汇总；分镜镜头仍缺少稳定来源锚点，因此没有虚构分镜影响结果。
- 弹窗分开显示逐行变化、确定受影响拆解项、关联拍摄日/通告数量、人工核对项和覆盖限制。

工程与内置浏览器验证：

- 聚焦制片 API 测试 `35/35` 通过；新增覆盖单行、两种范围格式、人工核对、拍摄日/通告去重以及纯新增不误报。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `187/187`、PostgreSQL `61/61`，Next.js `16.3.2` 生产构建成功。
- 内置浏览器在脚本页打开“版本影响分析”，真实服务端返回 `v6 -> v7` 的 `3` 处逐行变化；弹窗在 `1329x912` 视口居中，双列 diff、关联统计、覆盖限制和关闭操作没有重叠或裁切。
- 当前真实数据的 `v6` 没有正式拆解关联，因此界面正确显示受影响拆解 `0`、拍摄日 `0`、通告 `0`；非空影响、人工核对和关系去重由 API 自动化测试验证。

运行态与模型边界：

- 本轮没有创建或修改业务数据，也没有启动新的持续进程；现有 Web、API、Analysis Worker、Media Worker、PostgreSQL 与 MinIO 按用户要求继续运行。
- 多模态识别、OCR、语音转写以及素材/查询 embedding 仍只调用第三方 OpenAI-compatible API；服务器不部署或回退本地 AI 模型，自有 PostgreSQL `pgvector` 只存储并检索第三方 API 生成的向量。

## 130. 2026-09-01 P1 最终逐项核账

按 `PRODUCT_REQUIREMENTS.md` 第 21.2 节逐项核对当前代码、迁移、API、UI、自动化测试和真实浏览器记录：

- 自定义角色与权限模板：已实现模板创建、更新、分配、能力校验和权限变更通知。
- 跨时区排程与资源冲突：已实现时区保真、DST 边界、人员/演员/场地/设备冲突与跨项目脱敏提示。
- 联系人和供应商批量导入导出：已实现 UTF-8 CSV 往返、字段别名、重复和非法批次校验。
- 高级脚本版本影响分析：已实现来源行重叠、人工核对、拍摄日/通告关联和纯新增防误报。
- 审片反馈智能合并、跨版本对应和客户身份校验：已实现可解释候选、置信度、人工确认/撤销、持久化、幂等、审计和外部审片两步校验；原评论保持不可变。
- 可配置分析型 Agent 工作流：已实现项目级启停、全部/指定脚本文档范围、脚本版本触发、人工确认/置信度阈值审批、CAS、幂等、审计、Worker 处理和失败重试。
- 第三方识别与 embedding API、多模态检索：已实现 OpenAI-compatible 远程转写/OCR/视觉/embedding、显式失败、任务审计和 PostgreSQL `pgvector` 检索；不存在本地模型或关键词回退。
- 作品集增强：已实现主题、访问统计、SEO、自定义域名配置和 DNS 验证逻辑。
- 回收站、数据导出和审计查询：已实现受控恢复/永久删除、完整业务导出、敏感字段排除和筛选审计。
- 并发编辑冲突与通知偏好：已实现脚本三方合并、在线 presence，以及通告、权限、作品集、审片、联系人和制片执行六类通知偏好与生成写点。

本轮新增切片验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm --filter @shadowproducer/api test:postgres`、`pnpm build` 全部通过；Web `59/59`、API `187/187`、PostgreSQL `61/61`。
- 内置浏览器脚本影响弹窗中心偏差为 `0/0`，四边均未溢出，控制台 warning/error 为 `0`；验收标签页已保留。
- 临时浏览器截图目录已精确删除；本轮没有启动新的持续进程，也没有创建或修改业务数据。
- Web `3211` 返回 `200`，API `3220` 的 `/healthz` 返回 `ok`；Analysis Worker、Media Worker、PostgreSQL 和 MinIO 持续运行，PostgreSQL 与 MinIO 均为 `healthy`。

外部环境验收边界：

- 尚未配置真实第三方模型供应商凭据，因此模型识别质量、向量召回质量、限流、费用和生产 API 兼容性仍需在目标环境验收；服务器本地模型不属于部署项或回退方案。
- 自定义域名的真实 DNS、反向代理、TLS 证书和搜索引擎收录仍需在目标域名与部署环境验收；本地配置、校验逻辑和持久化已完成。

## 131. 2026-09-04 P1 onboarding 账号、团队与邀请闭环收口

本轮完成并验证自助注册到团队协作的 onboarding 闭环，AI provider、SMTP 和真实邮件投递不作为本轮执行依赖：

- Better Auth 在非生产环境默认开放注册；生产环境只有 `AUTH_ALLOW_SIGN_UP=true` 才开放，Web 生产注册入口还需要 `NEXT_PUBLIC_ENABLE_REGISTRATION=true`。
- Better Auth 新用户创建后按规范化小写邮箱 provisioning 到 ShadowProducer `accounts`；已登录但没有 domain account 时返回 `ACCOUNT_NOT_PROVISIONED`，生产环境不依赖 `x-shadow-account-id` 开发 header。
- 没有团队的已注册账号进入“选择团队” onboarding，可创建团队并在同一事务中建立 owner membership、六个系统权限模板和 `team.created` 审计；首个项目可同时创建，也可以跳过，项目失败不会伪装成团队和项目都成功。
- 团队管理员可创建 team-scope 一次性邀请链接；明文 token 只在创建响应和创建页面上下文中出现一次，数据库只存 SHA-256 hash，列表、receipt 和数据库记录不保存明文 token。邀请默认 7 天有效，并支持 7、30、90 天选项。
- public invitation preview 不要求登录，返回受邀邮箱、团队、权限模板和有效期，并通过 `cache-control: no-store` 防止缓存；accept route 需要认证，使用 URL path token 和 body 幂等键，认证 session actor 覆盖伪造 header。
- 接受邀请在 PostgreSQL 事务中锁定邀请行并原子写入成员关系、accepted 状态与 revision、inviter notification、`invitation.accepted` audit 和 `invitation.accept` receipt；同一幂等键重放返回已保存结果，不同幂等键重复消费返回 410，邮箱不匹配返回 403，成员已存在返回 409。
- 撤销使用 expected revision，过期、撤销、已接受和重复消费统一进入失效语义；撤销和过期状态均有数据库与 service 层验证。

工程与真实数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；最新 API 单元测试 `204/204`、Web 测试 `59/59`，Next.js `16.3.2` 生产构建成功。构建仅保留 Turbo 对 API 无输出文件的配置警告，不影响退出码。
- `pnpm --filter @shadowproducer/api test:postgres` 通过：10 个 PostgreSQL 测试文件、`66/66` tests；onboarding 专项 `5/5` 通过，覆盖成功接受的 team membership、邀请状态/revision、通知、审计、receipt、幂等重放、邮箱不匹配、成员已存在、撤销、过期和 stale revision。
- `apps/api/src/workspace-onboarding-api.test.ts` 通过 `3/3`，覆盖 v1 与 accept 认证、public preview、`no-store`、受邀邮箱、session actor 防伪、accept `201`、body validation `400`、失效 preview `410` 和 revoke revision conflict `409`。

真实 GUI 回归：

- `pnpm --filter @shadowproducer/web-e2e exec playwright test --workers=1` 通过 `29 passed / 7 skipped`，共 36 项，包含 desktop 和 mobile 两个项目；未注入 cookie、sessionStorage、数据库记录、点击事件或认证绕过。
- desktop onboarding 核心流程全部通过：登录、空提交校验、错误密码、未知账号、自助注册、无团队 onboarding、创建团队与首个项目、跳过项目、seeded team selection、邀请创建、第二账号注册、邀请接受、接受后再次访问失效 token、退出登录。
- mobile onboarding 核心流程全部通过同一组真实 GUI 步骤；移动端工作区导航中依赖既有媒体或特定 viewport 的 7 项按测试设计 skip，不计为 onboarding 失败。desktop workspace 导航、项目切换、团队切换、未知 hash 回退和任务创建通过；client review media path 与既有 compact 条件项保持 skip/按条件执行。
- 当前 Playwright 配置实际验收视口为 desktop `1440x900` 与 Pixel 7 mobile；本轮没有额外建立 `200%` zoom 或 `375x812` 等独立截图证据。受控内置浏览器在本环境返回空 DOM 快照且截图面超时，因此没有虚构其截图、console 或 reduced-motion 证据；GUI 结论以 Playwright 真实可见元素交互结果为准。

明确边界：

- 当前 Web 权限页只接入 team-scope 邀请 UI；后端已支持 project-scope invitation，但 project invitation UI 不宣称在本轮完成。
- 本轮没有配置或依赖真实第三方 AI provider、SMTP、真实邮件发送或媒体处理；不把 AI 能力伪装成可用。模型识别、embedding、邮件投递和目标环境供应商兼容性仍需后续凭据与环境验收。

## 132. 2026-09-04 P1 onboarding project-only 与一致性回归收口

在第 131 章 onboarding 闭环基础上，本轮继续修复项目级外部协作者入口、通知/审计隔离和邀请写入边界；AI provider、SMTP、真实邮件投递和真实媒体处理仍不作为执行依赖：

- project-scope invitation 接受后只写入 `project_memberships`，不会自动伪造 `team_memberships`；项目访问解析在没有团队成员关系时只授予项目模板能力，团队成员的既有“项目权限受团队权限收紧”规则保持不变。
- project-only 账号的 workspace context 返回 `role: null`、`memberCount: 0`，仅包含实际可访问的项目；默认路由进入项目空间。桌面侧边栏、移动端菜单、全局搜索和通知跳转均过滤团队级入口，不能借项目入口进入团队资源、权限或团队写操作。
- project-only 通知查询只返回存在对应项目成员关系的项目通知，不能读取 `project_id IS NULL` 的团队通知；team-scope audit 对 project-only 账号返回空结果，project-scope audit 仅返回其项目范围记录。权限工作区仍可供团队管理员查看项目-only 成员，但团队权限槽位明确显示“仅项目成员，无团队权限”，分配团队模板不会伪造成员身份。
- 邀请创建入口继续执行邮箱 trim/lowercase 和格式校验；同作用域已有成员返回 `INVITATION_MEMBER_EXISTS`/409，pending partial unique index 竞争稳定映射为 `INVITATION_PENDING_EXISTS`/409。一次性明文 token 只在首次创建响应和当前创建页面上下文出现，数据库、列表和 receipt 只保存 hash/summary。

自动化与真实数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API 单元测试 `206/206`、Web 单元测试 `66/66`，Next.js `16.3.2` 生产构建成功。构建仅保留 Turbo 对 API 无输出文件的配置警告，不影响退出码。
- `pnpm --filter @shadowproducer/api test:postgres` 通过：10 个 PostgreSQL 测试文件、`67/67` tests；onboarding 专项 `6/6` 通过，新增覆盖 project-only 接受后的无团队成员关系、项目访问、团队访问拒绝、项目-only context、通知隔离、团队/项目审计隔离、权限工作区合并、团队模板分配拒绝、同作用域成员拒绝和并发 pending 邀请冲突。
- `apps/api/src/workspace-onboarding-api.test.ts` 通过 `5/5`，新增覆盖 project preview/accept、非法邮箱边界 `400`、pending conflict `409`，并保留 session actor 覆盖伪造 header 的断言。

真实 GUI 回归与视觉证据：

- `pnpm --filter @shadowproducer/web-e2e exec playwright test --workers=1` 通过 `29 passed / 7 skipped`，共 36 项，包含 desktop 和 mobile；核心登录、注册、无团队 onboarding、创建团队/首个项目、跳过项目、团队邀请创建、匹配邮箱接受、失效 token 和退出登录流程均由真实可见 GUI 完成，未注入 cookie、sessionStorage、数据库记录、点击事件或认证绕过。
- 受控浏览器人工检查真实打开了登录和注册页面；DOM 快照与截图均显示产品标识、中文表单、最小密码长度提示和注册/返回登录控件。证据：`output/playwright/onboarding-live-login-1280x720.png`、`output/playwright/onboarding-live-registration-1280x720.png`。`1280x720` 页面几何无横向溢出；`768x1024` 只读几何检查返回 `scrollWidth=768`、`bodyWidth=768`，无横向溢出。
- 本轮内置浏览器的部分截图请求因后端截图任务超时，未取得 `375x812`、`1024x768`、`1440x1000`、`1920x1080` 和 `200%` zoom 的独立截图证据；这些尺寸不计为已完成的人工视觉验收。Playwright 已有 desktop `1440x900` 与 Pixel 7 mobile 的真实交互结果，7 个条件性 workspace/media 流程按测试设计跳过。

已知未收口风险与明确边界：

- 首次创建邀请响应丢失后的明文 token 恢复/resend 通道仍未实现；由于安全边界禁止把 token 写入 receipt、列表或数据库，不能把新幂等键重试当作 token 找回方案。
- 相同幂等键并发请求在 receipt 写入竞争时的重读/回放语义仍需单独处理和验证；本轮已覆盖 pending invitation 的并发唯一性，但不把它等同于 receipt race 已解决。
- 团队回收站 restore/permanent delete 权限的更细粒度策略、日期 `date-time`/`date` 的 AJV 实际格式校验和通知分页仍属于后续风险评估项。
- 权限页本轮仍只接入 team-scope 邀请创建 UI；project-scope invitation 后端已可用，但 project invitation UI 不宣称完成。真实 AI provider、SMTP、邮件发送和媒体处理仍未配置或接入。

## 133. 2026-09-04 P1 onboarding 与 workspace 最终一致性验证

本轮收口第 131、132 章遗留的幂等竞态、邀请生命周期、共享回收站、日期输入、通知分页和 E2E 环境问题；AI provider、SMTP、真实邮件投递和真实媒体处理继续明确排除：

- 邀请 token rotate/resend 已接入 API、service、PostgreSQL 和 team-scope 权限页。新 token 使用安全随机源，旧 token 立即失效；明文 token 只在首次响应和当前页面状态出现，receipt、列表、审计 metadata 和数据库只保存 hash/summary。accepted、revoked、expired、not-found 和 stale revision 分别映射到稳定的 410、404 或 409 语义。
- 通用 receipt-backed 命令在同一事务内、读取 receipt 前取得 `pg_advisory_xact_lock`；相同幂等键并发创建/接受邀请只产生一次业务写入、一次审计、一次通知和一次 receipt，另一响应稳定 replay；同 key 换 payload 返回 `IDEMPOTENCY_KEY_REUSED`/409。
- 团队共享回收站新增 `team.recycle.manage` capability。普通 team member 仍可处理个人 task/calendar/note 回收项，但不能恢复或永久删除 team-contact/supplier；管理员和 legacy owner/admin/producer/director 保留共享资源管理能力，Web 同步按 `canManageShared` 隐藏受保护按钮，后端 gate 仍是最终边界。
- workspace context 的 `memberCount` 改为按团队一次 `GROUP BY` 聚合回填；project-only 虚拟团队继续返回 `role: null`、`memberCount: 0`，不扩大团队访问范围。
- 通知列表支持 `page`/`pageSize`、稳定排序、`total` 和全量可见范围的 `unreadCount`；Web popover 支持追加去重、加载更多、刷新回第一页和操作后失效缓存。project-only 过滤继续排除 `project_id IS NULL` 团队通知。
- 日期输入同时检查 ISO 形状和真实年月日组合；`2026-02-31` 以及对应日期时间被拒绝为 `INVALID_DATE`/400，合法但倒序的日程范围仍返回 `INVALID_CALENDAR_RANGE`/400；Agent 日程更新不再把非法日期泄漏为 `RangeError`/500。
- 迁移兼容性修复统一了历史通知 CHECK 约束重放路径：`026`、`070`、`071`、`072`、`076` 均接受当前契约的 13 种通知 kind，避免早期中间约束在重放时拒绝已存在的后续合法数据。真实数据库连续执行 `pnpm db:migrate` 两次成功；通知从 15 条历史 `team_invitation_accepted` 保留为 E2E 后的 17 条，非法 kind 查询为 0，没有删除或改写通知数据。

工程与真实数据库验证：

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过；API 单元测试 `211/211`，Web 原生测试 `66/66`，Next.js `16.3.2` 生产构建成功。构建只保留 Turbo 关于 API 没有输出文件的配置警告，不影响退出码。
- `pnpm --filter @shadowproducer/api test:postgres` 通过：10 个 PostgreSQL 测试文件、`69/69` tests；onboarding 专项 `8/8`，workspace repository 专项 `15/15`，覆盖 rotate/revoke 状态、同 key 并发 create/accept、不同 payload 冲突、通知分页和 project-only 隔离、共享回收站 capability、memberCount 聚合与日期边界。
- `apps/api/src/workspace-onboarding.test.ts`、`apps/api/src/workspace-onboarding-api.test.ts` 和 `apps/api/src/app.test.ts` 的 rotate、revoke、project invitation 与 Agent 非法日期回归均通过；未配置或调用真实 AI provider、SMTP、邮件投递、转写/OCR/视觉分析/embedding 或媒体处理服务。

真实 GUI 回归：

- 使用隔离的 API `3230`、Web `3011` 和真实 PostgreSQL 执行 `E2E_WEB_PORT=3011 E2E_API_PORT=3230 pnpm --filter @shadowproducer/web-e2e exec playwright test --workers=1`，结果为 `29 passed / 7 skipped`，共 36 项，无失败，覆盖 desktop 与 Pixel 7 mobile。
- 登录、空提交原生校验、错误密码、未知账号、自助注册、无团队 onboarding、创建团队及首个项目、跳过项目、seed 账号团队选择、team-scope 邀请创建、匹配邮箱注册/接受、旧 token 失效、退出登录、桌面导航/项目切换/团队切换/未知 hash 回退、任务创建和移动端紧凑导航均通过真实可见 UI 操作完成；未注入 cookie、sessionStorage、数据库记录、点击事件或认证绕过。
- 7 项跳过均为既有条件：桌面专属/移动端专属导航互斥项，以及 deterministic seed 没有 media-ready review version 时的客户审片流程；不把条件性 skip 记作失败。为支持真正隔离运行，E2E 配置让 Next rewrite 跟随 `API_INTERNAL_URL`，并使用独立 `.next-e2e-*` 构建目录，避免复用旧 dev server 的认证和 API 端口。
- 本轮没有新增 200% zoom 或 `375x812`、`768x1024`、`1024x768`、`1440x1000`、`1920x1080` 的独立截图验收证据；受控内置浏览器截图后端仍有超时，因此不虚构视觉截图、console 或 reduced-motion 证据。Playwright 的真实元素交互结果是本轮 onboarding GUI 的验收依据。

明确未完成范围：

- 当前 Web 权限页仍只提供 team-scope invitation 创建 UI；project-scope invitation 的后端 contract/service/API 已可用，但 project invitation UI 不计入本轮完成。
- 首次创建响应丢失后的明文 token 找回仍没有安全通道；由于 token 不持久化，不能通过新幂等键伪造 resend 或找回旧 token。真实 AI provider、SMTP、真实邮件、真实媒体处理和目标环境供应商兼容性仍需后续凭据与环境验收。
